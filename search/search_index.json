{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"MBTI Personality Classifier","text":"<p>An end-to-end MLOps pipeline for predicting Myers-Briggs personality types from text using deep learning.</p>"},{"location":"#overview","title":"Overview","text":"<p>This project implements a production-ready machine learning system that analyzes writing samples to predict personality types based on the Myers-Briggs Type Indicator (MBTI). Unlike traditional 16-class classification approaches, we use a multi-task learning framework with 4 independent binary classifiers for better performance and interpretability.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>\ud83e\udde0 Multi-task Binary Classification: 4 independent classifiers for E/I, S/N, T/F, J/P dimensions</li> <li>\ud83e\udd16 Transformer-based Model: DistilBERT (67M parameters) for contextual understanding</li> <li>\ud83d\udcca Advanced Data Handling: Random window sampling for sequences &gt;512 tokens</li> <li>\ud83d\ude80 Production-ready Serving: FastAPI backend with Streamlit UI</li> <li>\u2699\ufe0f Modular Configuration: Hydra-based configs for reproducible experiments</li> <li>\ud83d\udce6 Data Versioning: DVC integration with Google Cloud Storage</li> <li>\ud83d\udc33 Containerization: Docker Compose for seamless deployment</li> <li>\u2705 Comprehensive Testing: Unit and integration tests with coverage</li> </ul>"},{"location":"#architecture-highlights","title":"Architecture Highlights","text":""},{"location":"#model-architecture","title":"Model Architecture","text":"<pre><code>Input Text (up to 512 tokens)\n         \u2193\nDistilBERT Encoder (pretrained)\n         \u2193\n[CLS] Token Representation\n         \u2193\nShared Dense Layer (768 \u2192 768)\n         \u2193\n    \u250c\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2510\n    \u2193         \u2193    \u2193    \u2193\n  E/I       S/N  T/F  J/P\n  Head      Head Head Head\n    \u2193         \u2193    \u2193    \u2193\n Binary  Binary Binary Binary\n Output  Output Output Output\n    \u2514\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2518\n         \u2193\n   MBTI Type (e.g., INTJ)\n</code></pre>"},{"location":"#system-architecture","title":"System Architecture","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502   Training  \u2502 (Docker container)\n\u2502   Service   \u2502 \u2192 Saves model to ./models/\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2193\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     HTTP        \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 FastAPI API \u2502 \u2190\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2502 Streamlit\u2502\n\u2502  (port 8000)\u2502                 \u2502    UI    \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                 \u2502(port 8501)\u2502\n       \u2191                        \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2502 Loads model from\n   ./models/best.ckpt\n</code></pre>"},{"location":"#dataset","title":"Dataset","text":"<ul> <li>Source: Kaggle MBTI Dataset</li> <li>Size: ~8,675 samples from PersonalityCafe forum</li> <li>Format: Concatenated posts per user with self-reported MBTI type</li> <li>Challenge: Severe class imbalance (75% Introverts, 85% Intuitives)</li> </ul>"},{"location":"#performance","title":"Performance","text":"<p>The model treats MBTI as 4 binary classification tasks:</p> Dimension Task Metric E/I Extraversion vs Introversion Binary F1 S/N Sensing vs Intuition Binary F1 T/F Thinking vs Feeling Binary F1 J/P Judging vs Perceiving Binary F1 <p>This approach provides: - Better handling of class imbalance - More interpretable predictions - Dimension-level probability scores</p>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Installation Guide</li> <li>Quick Start Tutorial</li> <li>Architecture Overview</li> <li>API Reference</li> </ul>"},{"location":"#tech-stack","title":"Tech Stack","text":"<ul> <li>ML Framework: PyTorch Lightning</li> <li>Model: Hugging Face Transformers (DistilBERT)</li> <li>Configuration: Hydra</li> <li>API: FastAPI</li> <li>UI: Streamlit</li> <li>Data Versioning: DVC + Google Cloud Storage</li> <li>Containerization: Docker + Docker Compose</li> <li>Testing: pytest</li> <li>Package Management: uv</li> </ul>"},{"location":"#project-status","title":"Project Status","text":"<p>This project is actively maintained and ready for production deployment.</p> <p>Built with \u2764\ufe0f using MLOps Template</p>"},{"location":"api/api/","title":"API Server","text":"<p>Documentation for the FastAPI inference server.</p>"},{"location":"api/api/#endpoints","title":"Endpoints","text":""},{"location":"api/api/#root","title":"Root","text":"<p><code>GET /</code></p> <p>Returns service information and available endpoints.</p> <p>Response: <pre><code>{\n  \"message\": \"MBTI Personality Classifier API\",\n  \"version\": \"1.0.0\",\n  \"endpoints\": {\n    \"predict\": \"/predict\",\n    \"health\": \"/health\",\n    \"docs\": \"/docs\"\n  }\n}\n</code></pre></p>"},{"location":"api/api/#health-check","title":"Health Check","text":"<p><code>GET /health</code></p> <p>Returns health status and model loading state.</p> <p>Response: <pre><code>{\n  \"status\": \"healthy\",\n  \"model_loaded\": true,\n  \"tokenizer_loaded\": true,\n  \"device\": \"cpu\"\n}\n</code></pre></p>"},{"location":"api/api/#predict","title":"Predict","text":"<p><code>POST /predict</code></p> <p>Predicts MBTI personality type from text.</p> <p>Request Body: <pre><code>{\n  \"text\": \"I enjoy quiet evenings reading philosophy books...\"\n}\n</code></pre></p> <p>Response: <pre><code>{\n  \"mbti_type\": \"INFP\",\n  \"dimensions\": [\n    {\n      \"dimension\": \"E/I\",\n      \"score\": 0.15,\n      \"label\": \"I\"\n    },\n    {\n      \"dimension\": \"S/N\",\n      \"score\": 0.72,\n      \"label\": \"N\"\n    },\n    {\n      \"dimension\": \"T/F\",\n      \"score\": 0.35,\n      \"label\": \"F\"\n    },\n    {\n      \"dimension\": \"J/P\",\n      \"score\": 0.42,\n      \"label\": \"P\"\n    }\n  ],\n  \"probabilities\": {\n    \"E\": 0.15,\n    \"I\": 0.85,\n    \"S\": 0.28,\n    \"N\": 0.72,\n    \"T\": 0.35,\n    \"F\": 0.65,\n    \"J\": 0.42,\n    \"P\": 0.58\n  }\n}\n</code></pre></p> <p>Error Responses:</p> <p>400 Bad Request: <pre><code>{\n  \"detail\": \"Text too short after cleaning. Please provide more text.\"\n}\n</code></pre></p> <p>503 Service Unavailable: <pre><code>{\n  \"detail\": \"Model not loaded\"\n}\n</code></pre></p>"},{"location":"api/api/#python-client-example","title":"Python Client Example","text":"<pre><code>import requests\n\n# Create client\nclass MBTIClient:\n    def __init__(self, base_url=\"http://localhost:8000\"):\n        self.base_url = base_url\n\n    def predict(self, text: str) -&gt; dict:\n        \"\"\"Predict MBTI type from text.\"\"\"\n        response = requests.post(\n            f\"{self.base_url}/predict\",\n            json={\"text\": text}\n        )\n        response.raise_for_status()\n        return response.json()\n\n    def health(self) -&gt; dict:\n        \"\"\"Check API health.\"\"\"\n        response = requests.get(f\"{self.base_url}/health\")\n        response.raise_for_status()\n        return response.json()\n\n# Usage\nclient = MBTIClient()\n\n# Make prediction\nresult = client.predict(\n    \"I love abstract theories and philosophical discussions!\"\n)\n\nprint(f\"Type: {result['mbti_type']}\")\nprint(f\"Probabilities: {result['probabilities']}\")\n</code></pre>"},{"location":"api/api/#javascript-client-example","title":"JavaScript Client Example","text":"<pre><code>class MBTIClient {\n  constructor(baseUrl = 'http://localhost:8000') {\n    this.baseUrl = baseUrl;\n  }\n\n  async predict(text) {\n    const response = await fetch(`${this.baseUrl}/predict`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({ text }),\n    });\n\n    if (!response.ok) {\n      throw new Error(`HTTP error! status: ${response.status}`);\n    }\n\n    return await response.json();\n  }\n\n  async health() {\n    const response = await fetch(`${this.baseUrl}/health`);\n    return await response.json();\n  }\n}\n\n// Usage\nconst client = new MBTIClient();\n\nconst result = await client.predict(\n  'I prefer concrete facts over abstract theories.'\n);\n\nconsole.log(`Type: ${result.mbti_type}`);\n</code></pre>"},{"location":"api/api/#cors-configuration","title":"CORS Configuration","text":"<p>By default, CORS is enabled for all origins:</p> <pre><code>app.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n</code></pre> <p>For production, specify exact origins:</p> <pre><code>allow_origins=[\n    \"https://your-frontend.com\",\n    \"https://app.your-domain.com\"\n]\n</code></pre>"},{"location":"api/data/","title":"Data Module","text":"<p>               Bases: <code>Dataset</code></p> <p>PyTorch Dataset for MBTI personality types with tokenization.</p> <p>Treats MBTI as 4 independent binary classification tasks: - E/I (Extraversion vs Introversion) - S/N (Sensing vs Intuition) - T/F (Thinking vs Feeling) - J/P (Judging vs Perceiving)</p> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>class MBTIDataset(Dataset):\n    \"\"\"PyTorch Dataset for MBTI personality types with tokenization.\n\n    Treats MBTI as 4 independent binary classification tasks:\n    - E/I (Extraversion vs Introversion)\n    - S/N (Sensing vs Intuition)\n    - T/F (Thinking vs Feeling)\n    - J/P (Judging vs Perceiving)\n    \"\"\"\n\n    def __init__(self, texts, binary_labels, tokenizer, max_length: int = 512, use_random_window: bool = False):\n        \"\"\"\n        Args:\n            texts: List or array of text posts\n            binary_labels: Dict with keys ['E', 'S', 'T', 'J'], values are binary tensors (0 or 1)\n            tokenizer: HuggingFace tokenizer instance\n            max_length: Maximum sequence length for tokenization\n            use_random_window: If True, uses random windows for data augmentation (training only)\n        \"\"\"\n        self.texts = texts\n        self.binary_labels = binary_labels\n        self.tokenizer = tokenizer\n        self.max_length = max_length\n        self.use_random_window = use_random_window\n\n    def __len__(self):\n        return len(self.texts)\n\n    def __getitem__(self, idx):\n        text = str(self.texts[idx])\n\n        # Get binary labels for all 4 dimensions\n        labels = torch.tensor(\n            [\n                self.binary_labels[\"E\"][idx],\n                self.binary_labels[\"S\"][idx],\n                self.binary_labels[\"T\"][idx],\n                self.binary_labels[\"J\"][idx],\n            ],\n            dtype=torch.float32,\n        )\n\n        # Tokenize with random window augmentation if enabled\n        if self.use_random_window:\n            # First tokenize without truncation to get full length\n            full_encoding = self.tokenizer(\n                text,\n                truncation=False,\n                return_tensors=\"pt\",\n            )\n\n            input_ids = full_encoding[\"input_ids\"].squeeze(0)\n            total_length = len(input_ids)\n\n            # If sequence is longer than max_length, take a random window\n            # BUT always keep [CLS] at start and [SEP] at end\n            if total_length &gt; self.max_length:\n                # Extract [CLS] token (first token)\n                cls_token = input_ids[0:1]  # [CLS]\n                # Extract [SEP] token (last token)\n                sep_token = input_ids[-1:]  # [SEP]\n\n                # Content tokens (excluding [CLS] and [SEP])\n                content_tokens = input_ids[1:-1]\n                content_length = len(content_tokens)\n\n                # Available space for content (max_length - 2 for [CLS] and [SEP])\n                max_content_length = self.max_length - 2\n\n                if content_length &gt; max_content_length:\n                    # Random start position in content\n                    max_start = content_length - max_content_length\n                    start_idx = random.randint(0, max_start)\n                    end_idx = start_idx + max_content_length\n\n                    # Extract random window from content\n                    content_window = content_tokens[start_idx:end_idx]\n                else:\n                    # Content fits, use all of it\n                    content_window = content_tokens\n\n                # Reconstruct: [CLS] + content_window + [SEP]\n                input_ids = torch.cat([cls_token, content_window, sep_token])\n                attention_mask = torch.ones(len(input_ids), dtype=torch.long)\n\n                # Pad if necessary\n                current_length = len(input_ids)\n                if current_length &lt; self.max_length:\n                    padding_length = self.max_length - current_length\n                    input_ids = torch.cat(\n                        [input_ids, torch.full((padding_length,), self.tokenizer.pad_token_id, dtype=torch.long)]\n                    )\n                    attention_mask = torch.cat([attention_mask, torch.zeros(padding_length, dtype=torch.long)])\n            else:\n                # If shorter than max_length, pad normally\n                attention_mask = torch.ones(total_length, dtype=torch.long)\n                padding_length = self.max_length - total_length\n                if padding_length &gt; 0:\n                    input_ids = torch.cat(\n                        [input_ids, torch.full((padding_length,), self.tokenizer.pad_token_id, dtype=torch.long)]\n                    )\n                    attention_mask = torch.cat([attention_mask, torch.zeros(padding_length, dtype=torch.long)])\n        else:\n            # Standard tokenization with truncation from the beginning\n            encoding = self.tokenizer(\n                text,\n                max_length=self.max_length,\n                padding=\"max_length\",\n                truncation=True,\n                return_tensors=\"pt\",\n            )\n            input_ids = encoding[\"input_ids\"].squeeze(0)\n            attention_mask = encoding[\"attention_mask\"].squeeze(0)\n\n        return {\n            \"input_ids\": input_ids,\n            \"attention_mask\": attention_mask,\n            \"labels\": labels,  # [4] binary labels for E, S, T, J\n        }\n</code></pre> <p>               Bases: <code>LightningDataModule</code></p> <p>PyTorch Lightning DataModule for MBTI personality classification.</p> <p>Handles data downloading, cleaning, preprocessing, tokenization, and DataLoader creation. Optimized for fine-tuning transformer models like DistilBERT.</p> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>class MBTIDataModule(pl.LightningDataModule):\n    \"\"\"\n    PyTorch Lightning DataModule for MBTI personality classification.\n\n    Handles data downloading, cleaning, preprocessing, tokenization, and DataLoader creation.\n    Optimized for fine-tuning transformer models like DistilBERT.\n    \"\"\"\n\n    def __init__(\n        self,\n        raw_data_path: str = \"data/raw\",\n        processed_data_path: str = \"data/processed\",\n        batch_size: int = 16,\n        num_workers: int = 4,\n        test_size: float = 0.15,\n        val_size: float = 0.15,\n        random_seed: int = 42,\n        model_name: str = \"distilbert-base-uncased\",\n        max_length: int = 512,\n        cache_dir: Optional[str] = None,\n    ):\n        \"\"\"\n        Args:\n            raw_data_path: Path to store raw downloaded data\n            processed_data_path: Path to store processed data\n            batch_size: Batch size for DataLoaders (default 16 for transformer models)\n            num_workers: Number of workers for DataLoaders\n            test_size: Proportion of data for test set\n            val_size: Proportion of train data for validation set\n            random_seed: Random seed for reproducibility\n            model_name: HuggingFace model name for tokenizer\n            max_length: Maximum sequence length for tokenization\n            cache_dir: Optional directory to cache tokenizer and models\n        \"\"\"\n        super().__init__()\n\n        # Convert paths to Path objects\n        self.raw_data_path = Path(raw_data_path)\n        self.processed_data_path = Path(processed_data_path)\n\n        self.batch_size = batch_size\n        self.num_workers = num_workers\n        self.test_size = test_size\n        self.val_size = val_size\n        self.random_seed = random_seed\n        self.model_name = model_name\n        self.max_length = max_length\n        self.cache_dir = cache_dir\n\n        # Initialize tokenizer\n        self.tokenizer = None\n\n        # Will be populated during setup\n        self.train_dataset = None\n        self.val_dataset = None\n        self.test_dataset = None\n        self.type_to_idx = None\n        self.idx_to_type = None\n        self.num_classes = None\n\n    def prepare_data(self):\n        \"\"\"\n        Download data and tokenizer if needed. Called only on 1 GPU in distributed settings.\n        \"\"\"\n        # Download dataset\n        self._ensure_data()\n\n        # Download tokenizer (will be cached)\n        logger.info(f\"Loading tokenizer: {self.model_name}\")\n        AutoTokenizer.from_pretrained(self.model_name, cache_dir=self.cache_dir)\n\n    def setup(self, stage: Optional[str] = None):\n        \"\"\"\n        Load and split data. Called on every GPU in distributed settings.\n\n        Args:\n            stage: 'fit', 'validate', 'test', or 'predict'\n        \"\"\"\n        # Initialize tokenizer if not already done\n        if self.tokenizer is None:\n            logger.info(f\"Initializing tokenizer: {self.model_name}\")\n            self.tokenizer = AutoTokenizer.from_pretrained(self.model_name, cache_dir=self.cache_dir)\n\n        # Load processed data or create it\n        df = self._load_or_process_data()\n\n        # Create label mappings\n        unique_types = sorted(df[\"type\"].unique())\n        self.type_to_idx = {t: i for i, t in enumerate(unique_types)}\n        self.idx_to_type = {i: t for t, i in self.type_to_idx.items()}\n        self.num_classes = len(unique_types)\n\n        logger.info(f\"Number of classes: {self.num_classes}\")\n        logger.info(f\"Class distribution:\\n{df['type'].value_counts().sort_index()}\")\n\n        df[\"type_idx\"] = df[\"type\"].map(self.type_to_idx)\n\n        # Split into train+val and test\n        if self.test_size &gt; 0:\n            train_val_df, test_df = train_test_split(\n                df, test_size=self.test_size, random_state=self.random_seed, stratify=df[\"type\"]\n            )\n        else:\n            train_val_df = df\n            test_df = pd.DataFrame()\n\n        # Split train into train and validation\n        if self.val_size &gt; 0:\n            train_df, val_df = train_test_split(\n                train_val_df, test_size=self.val_size, random_state=self.random_seed, stratify=train_val_df[\"type\"]\n            )\n        else:\n            train_df = train_val_df\n            val_df = pd.DataFrame()\n\n        # Create datasets with binary labels for multi-task learning\n        if stage == \"fit\" or stage is None:\n            train_binary_labels = {\n                \"E\": train_df[\"is_E\"].values,\n                \"S\": train_df[\"is_S\"].values,\n                \"T\": train_df[\"is_T\"].values,\n                \"J\": train_df[\"is_J\"].values,\n            }\n            # Use random window augmentation for training\n            self.train_dataset = MBTIDataset(\n                train_df[\"posts\"].values,\n                train_binary_labels,\n                self.tokenizer,\n                self.max_length,\n                use_random_window=True,  # Enable random windows for training\n            )\n            if len(val_df) &gt; 0:\n                val_binary_labels = {\n                    \"E\": val_df[\"is_E\"].values,\n                    \"S\": val_df[\"is_S\"].values,\n                    \"T\": val_df[\"is_T\"].values,\n                    \"J\": val_df[\"is_J\"].values,\n                }\n                # Use standard truncation for validation (consistent evaluation)\n                self.val_dataset = MBTIDataset(\n                    val_df[\"posts\"].values,\n                    val_binary_labels,\n                    self.tokenizer,\n                    self.max_length,\n                    use_random_window=False,  # No augmentation for validation\n                )\n\n        if stage == \"test\" or stage is None:\n            if len(test_df) &gt; 0:\n                test_binary_labels = {\n                    \"E\": test_df[\"is_E\"].values,\n                    \"S\": test_df[\"is_S\"].values,\n                    \"T\": test_df[\"is_T\"].values,\n                    \"J\": test_df[\"is_J\"].values,\n                }\n                # Use standard truncation for testing (consistent evaluation)\n                self.test_dataset = MBTIDataset(\n                    test_df[\"posts\"].values,\n                    test_binary_labels,\n                    self.tokenizer,\n                    self.max_length,\n                    use_random_window=False,  # No augmentation for testing\n                )\n\n        logger.info(f\"Data split: Train={len(train_df)}, Val={len(val_df)}, Test={len(test_df)}\")\n        logger.info(f\"Max sequence length: {self.max_length}\")\n\n    def train_dataloader(self):\n        \"\"\"Create training DataLoader.\"\"\"\n        return DataLoader(\n            self.train_dataset,\n            batch_size=self.batch_size,\n            shuffle=True,\n            num_workers=self.num_workers,\n            pin_memory=True,\n            persistent_workers=self.num_workers &gt; 0,\n        )\n\n    def val_dataloader(self):\n        \"\"\"Create validation DataLoader.\"\"\"\n        if self.val_dataset is None:\n            return None\n\n        return DataLoader(\n            self.val_dataset,\n            batch_size=self.batch_size,\n            shuffle=False,\n            num_workers=self.num_workers,\n            pin_memory=True,\n            persistent_workers=self.num_workers &gt; 0,\n        )\n\n    def test_dataloader(self):\n        \"\"\"Create test DataLoader.\"\"\"\n        if self.test_dataset is None:\n            return None\n\n        return DataLoader(\n            self.test_dataset,\n            batch_size=self.batch_size,\n            shuffle=False,\n            num_workers=self.num_workers,\n            pin_memory=True,\n            persistent_workers=self.num_workers &gt; 0,\n        )\n\n    def _ensure_data(self):\n        \"\"\"Download dataset if not present locally.\"\"\"\n        csv_path = self.raw_data_path / \"mbti_1.csv\"\n\n        if csv_path.exists():\n            logger.info(f\"Raw data found at: {csv_path}\")\n            return csv_path\n\n        logger.info(\"Downloading dataset via mlcroissant...\")\n        self.raw_data_path.mkdir(parents=True, exist_ok=True)\n\n        try:\n            url = \"https://www.kaggle.com/datasets/datasnaek/mbti-type/croissant/download\"\n            dataset = mlc.Dataset(url)\n            record_sets = dataset.metadata.record_sets\n            records = dataset.records(record_set=record_sets[0].uuid)\n            df = pd.DataFrame(records)\n\n            # Clean column names\n            df.columns = [col.split(\"/\")[-1] for col in df.columns]\n\n            df.to_csv(csv_path, index=False)\n            logger.info(f\"Dataset saved to: {csv_path}\")\n        except Exception as e:\n            logger.error(f\"Error downloading dataset: {e}\")\n            raise\n\n        return csv_path\n\n    def _load_or_process_data(self):\n        \"\"\"\n        Load processed data if available, otherwise download, process, and save it.\n\n        Returns:\n            Processed DataFrame with cleaned text and binary labels\n        \"\"\"\n        processed_file = self.processed_data_path / \"processed_mbti.csv\"\n\n        # Check if processed data exists\n        if processed_file.exists():\n            logger.info(f\"Loading processed data from: {processed_file}\")\n            df = pd.read_csv(processed_file)\n            logger.info(f\"Loaded {len(df)} preprocessed rows\")\n            return df\n\n        # Download and process data\n        logger.info(\"Processed data not found. Downloading and preprocessing...\")\n        self._ensure_data()\n        df = self._load_and_clean_data()\n\n        # Save processed data\n        self.processed_data_path.mkdir(parents=True, exist_ok=True)\n        df.to_csv(processed_file, index=False)\n        logger.info(f\"Saved processed data to: {processed_file}\")\n\n        return df\n\n    def _load_and_clean_data(self):\n        \"\"\"Load and clean the MBTI dataset.\"\"\"\n        csv_path = self.raw_data_path / \"mbti_1.csv\"\n\n        if not csv_path.exists():\n            raise FileNotFoundError(\n                f\"Data file not found at {csv_path}. \" \"Run prepare_data() first or ensure the file exists.\"\n            )\n\n        df = pd.read_csv(csv_path)\n        logger.info(f\"Loaded {len(df)} rows from {csv_path}\")\n\n        # Clean 'type' column (remove byte string artifacts)\n        if df[\"type\"].dtype == object:\n            df[\"type\"] = df[\"type\"].astype(str).str.replace(r\"^b'|'$\", \"\", regex=True).str.upper()\n\n        # Clean 'posts' column\n        df[\"posts\"] = df[\"posts\"].astype(str).apply(self._clean_text)\n\n        # Remove rows with empty posts after cleaning\n        initial_len = len(df)\n        df = df[df[\"posts\"].str.len() &gt; 0].reset_index(drop=True)\n        removed = initial_len - len(df)\n        if removed &gt; 0:\n            logger.warning(f\"Removed {removed} rows with empty posts after cleaning\")\n\n        # Add binary features (optional, can be used for multi-task learning)\n        df[\"is_E\"] = df[\"type\"].apply(lambda x: 1 if \"E\" in x else 0)\n        df[\"is_S\"] = df[\"type\"].apply(lambda x: 1 if \"S\" in x else 0)\n        df[\"is_T\"] = df[\"type\"].apply(lambda x: 1 if \"T\" in x else 0)\n        df[\"is_J\"] = df[\"type\"].apply(lambda x: 1 if \"J\" in x else 0)\n\n        logger.info(f\"Final dataset size: {len(df)} rows\")\n        return df\n\n    @staticmethod\n    def _clean_text(text: str) -&gt; str:\n        \"\"\"\n        Clean text data by removing URLs, byte artifacts, and normalizing whitespace.\n        Optimized for transformer models which can handle more natural text.\n\n        Args:\n            text: Raw text string\n\n        Returns:\n            Cleaned text string\n        \"\"\"\n        # Remove byte string prefixes\n        if text.startswith(\"b'\") or text.startswith('b\"'):\n            text = text[2:-1]\n\n        # Remove post separators\n        text = text.replace(\"|||\", \" \")\n\n        # Remove URLs (keep URL text is sometimes meaningful, but typically not)\n        text = re.sub(r\"http[s]?://(?:[a-zA-Z]|[0-9]|[$-_@.&amp;+]|[!*\\\\(\\\\),]|(?:%[0-9a-fA-F][0-9a-fA-F]))+\", \"\", text)\n\n        # Remove markdown image syntax\n        text = re.sub(r\"!\\[.*?\\]\\(.*?\\)\", \"\", text)\n\n        # Remove excessive special characters but keep punctuation for sentiment\n        # Keep: letters, numbers, spaces, and common punctuation\n        text = re.sub(r\"[^a-zA-Z0-9\\s\\.\\,\\!\\?\\'\\-\\:\\;]\", \" \", text)\n\n        # Normalize whitespace\n        text = re.sub(r\"\\s+\", \" \", text).strip()\n\n        # Keep original casing - transformers can learn from it\n        return text\n</code></pre>"},{"location":"api/data/#mbti_classifier.data.MBTIDataset.__init__","title":"<code>__init__(texts, binary_labels, tokenizer, max_length=512, use_random_window=False)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>texts</code> <p>List or array of text posts</p> required <code>binary_labels</code> <p>Dict with keys ['E', 'S', 'T', 'J'], values are binary tensors (0 or 1)</p> required <code>tokenizer</code> <p>HuggingFace tokenizer instance</p> required <code>max_length</code> <code>int</code> <p>Maximum sequence length for tokenization</p> <code>512</code> <code>use_random_window</code> <code>bool</code> <p>If True, uses random windows for data augmentation (training only)</p> <code>False</code> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>def __init__(self, texts, binary_labels, tokenizer, max_length: int = 512, use_random_window: bool = False):\n    \"\"\"\n    Args:\n        texts: List or array of text posts\n        binary_labels: Dict with keys ['E', 'S', 'T', 'J'], values are binary tensors (0 or 1)\n        tokenizer: HuggingFace tokenizer instance\n        max_length: Maximum sequence length for tokenization\n        use_random_window: If True, uses random windows for data augmentation (training only)\n    \"\"\"\n    self.texts = texts\n    self.binary_labels = binary_labels\n    self.tokenizer = tokenizer\n    self.max_length = max_length\n    self.use_random_window = use_random_window\n</code></pre>"},{"location":"api/data/#mbti_classifier.data.MBTIDataModule.__init__","title":"<code>__init__(raw_data_path='data/raw', processed_data_path='data/processed', batch_size=16, num_workers=4, test_size=0.15, val_size=0.15, random_seed=42, model_name='distilbert-base-uncased', max_length=512, cache_dir=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>raw_data_path</code> <code>str</code> <p>Path to store raw downloaded data</p> <code>'data/raw'</code> <code>processed_data_path</code> <code>str</code> <p>Path to store processed data</p> <code>'data/processed'</code> <code>batch_size</code> <code>int</code> <p>Batch size for DataLoaders (default 16 for transformer models)</p> <code>16</code> <code>num_workers</code> <code>int</code> <p>Number of workers for DataLoaders</p> <code>4</code> <code>test_size</code> <code>float</code> <p>Proportion of data for test set</p> <code>0.15</code> <code>val_size</code> <code>float</code> <p>Proportion of train data for validation set</p> <code>0.15</code> <code>random_seed</code> <code>int</code> <p>Random seed for reproducibility</p> <code>42</code> <code>model_name</code> <code>str</code> <p>HuggingFace model name for tokenizer</p> <code>'distilbert-base-uncased'</code> <code>max_length</code> <code>int</code> <p>Maximum sequence length for tokenization</p> <code>512</code> <code>cache_dir</code> <code>Optional[str]</code> <p>Optional directory to cache tokenizer and models</p> <code>None</code> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>def __init__(\n    self,\n    raw_data_path: str = \"data/raw\",\n    processed_data_path: str = \"data/processed\",\n    batch_size: int = 16,\n    num_workers: int = 4,\n    test_size: float = 0.15,\n    val_size: float = 0.15,\n    random_seed: int = 42,\n    model_name: str = \"distilbert-base-uncased\",\n    max_length: int = 512,\n    cache_dir: Optional[str] = None,\n):\n    \"\"\"\n    Args:\n        raw_data_path: Path to store raw downloaded data\n        processed_data_path: Path to store processed data\n        batch_size: Batch size for DataLoaders (default 16 for transformer models)\n        num_workers: Number of workers for DataLoaders\n        test_size: Proportion of data for test set\n        val_size: Proportion of train data for validation set\n        random_seed: Random seed for reproducibility\n        model_name: HuggingFace model name for tokenizer\n        max_length: Maximum sequence length for tokenization\n        cache_dir: Optional directory to cache tokenizer and models\n    \"\"\"\n    super().__init__()\n\n    # Convert paths to Path objects\n    self.raw_data_path = Path(raw_data_path)\n    self.processed_data_path = Path(processed_data_path)\n\n    self.batch_size = batch_size\n    self.num_workers = num_workers\n    self.test_size = test_size\n    self.val_size = val_size\n    self.random_seed = random_seed\n    self.model_name = model_name\n    self.max_length = max_length\n    self.cache_dir = cache_dir\n\n    # Initialize tokenizer\n    self.tokenizer = None\n\n    # Will be populated during setup\n    self.train_dataset = None\n    self.val_dataset = None\n    self.test_dataset = None\n    self.type_to_idx = None\n    self.idx_to_type = None\n    self.num_classes = None\n</code></pre>"},{"location":"api/data/#mbti_classifier.data.MBTIDataModule.prepare_data","title":"<code>prepare_data()</code>","text":"<p>Download data and tokenizer if needed. Called only on 1 GPU in distributed settings.</p> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>def prepare_data(self):\n    \"\"\"\n    Download data and tokenizer if needed. Called only on 1 GPU in distributed settings.\n    \"\"\"\n    # Download dataset\n    self._ensure_data()\n\n    # Download tokenizer (will be cached)\n    logger.info(f\"Loading tokenizer: {self.model_name}\")\n    AutoTokenizer.from_pretrained(self.model_name, cache_dir=self.cache_dir)\n</code></pre>"},{"location":"api/data/#mbti_classifier.data.MBTIDataModule.setup","title":"<code>setup(stage=None)</code>","text":"<p>Load and split data. Called on every GPU in distributed settings.</p> <p>Parameters:</p> Name Type Description Default <code>stage</code> <code>Optional[str]</code> <p>'fit', 'validate', 'test', or 'predict'</p> <code>None</code> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>def setup(self, stage: Optional[str] = None):\n    \"\"\"\n    Load and split data. Called on every GPU in distributed settings.\n\n    Args:\n        stage: 'fit', 'validate', 'test', or 'predict'\n    \"\"\"\n    # Initialize tokenizer if not already done\n    if self.tokenizer is None:\n        logger.info(f\"Initializing tokenizer: {self.model_name}\")\n        self.tokenizer = AutoTokenizer.from_pretrained(self.model_name, cache_dir=self.cache_dir)\n\n    # Load processed data or create it\n    df = self._load_or_process_data()\n\n    # Create label mappings\n    unique_types = sorted(df[\"type\"].unique())\n    self.type_to_idx = {t: i for i, t in enumerate(unique_types)}\n    self.idx_to_type = {i: t for t, i in self.type_to_idx.items()}\n    self.num_classes = len(unique_types)\n\n    logger.info(f\"Number of classes: {self.num_classes}\")\n    logger.info(f\"Class distribution:\\n{df['type'].value_counts().sort_index()}\")\n\n    df[\"type_idx\"] = df[\"type\"].map(self.type_to_idx)\n\n    # Split into train+val and test\n    if self.test_size &gt; 0:\n        train_val_df, test_df = train_test_split(\n            df, test_size=self.test_size, random_state=self.random_seed, stratify=df[\"type\"]\n        )\n    else:\n        train_val_df = df\n        test_df = pd.DataFrame()\n\n    # Split train into train and validation\n    if self.val_size &gt; 0:\n        train_df, val_df = train_test_split(\n            train_val_df, test_size=self.val_size, random_state=self.random_seed, stratify=train_val_df[\"type\"]\n        )\n    else:\n        train_df = train_val_df\n        val_df = pd.DataFrame()\n\n    # Create datasets with binary labels for multi-task learning\n    if stage == \"fit\" or stage is None:\n        train_binary_labels = {\n            \"E\": train_df[\"is_E\"].values,\n            \"S\": train_df[\"is_S\"].values,\n            \"T\": train_df[\"is_T\"].values,\n            \"J\": train_df[\"is_J\"].values,\n        }\n        # Use random window augmentation for training\n        self.train_dataset = MBTIDataset(\n            train_df[\"posts\"].values,\n            train_binary_labels,\n            self.tokenizer,\n            self.max_length,\n            use_random_window=True,  # Enable random windows for training\n        )\n        if len(val_df) &gt; 0:\n            val_binary_labels = {\n                \"E\": val_df[\"is_E\"].values,\n                \"S\": val_df[\"is_S\"].values,\n                \"T\": val_df[\"is_T\"].values,\n                \"J\": val_df[\"is_J\"].values,\n            }\n            # Use standard truncation for validation (consistent evaluation)\n            self.val_dataset = MBTIDataset(\n                val_df[\"posts\"].values,\n                val_binary_labels,\n                self.tokenizer,\n                self.max_length,\n                use_random_window=False,  # No augmentation for validation\n            )\n\n    if stage == \"test\" or stage is None:\n        if len(test_df) &gt; 0:\n            test_binary_labels = {\n                \"E\": test_df[\"is_E\"].values,\n                \"S\": test_df[\"is_S\"].values,\n                \"T\": test_df[\"is_T\"].values,\n                \"J\": test_df[\"is_J\"].values,\n            }\n            # Use standard truncation for testing (consistent evaluation)\n            self.test_dataset = MBTIDataset(\n                test_df[\"posts\"].values,\n                test_binary_labels,\n                self.tokenizer,\n                self.max_length,\n                use_random_window=False,  # No augmentation for testing\n            )\n\n    logger.info(f\"Data split: Train={len(train_df)}, Val={len(val_df)}, Test={len(test_df)}\")\n    logger.info(f\"Max sequence length: {self.max_length}\")\n</code></pre>"},{"location":"api/data/#mbti_classifier.data.MBTIDataModule.test_dataloader","title":"<code>test_dataloader()</code>","text":"<p>Create test DataLoader.</p> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>def test_dataloader(self):\n    \"\"\"Create test DataLoader.\"\"\"\n    if self.test_dataset is None:\n        return None\n\n    return DataLoader(\n        self.test_dataset,\n        batch_size=self.batch_size,\n        shuffle=False,\n        num_workers=self.num_workers,\n        pin_memory=True,\n        persistent_workers=self.num_workers &gt; 0,\n    )\n</code></pre>"},{"location":"api/data/#mbti_classifier.data.MBTIDataModule.train_dataloader","title":"<code>train_dataloader()</code>","text":"<p>Create training DataLoader.</p> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>def train_dataloader(self):\n    \"\"\"Create training DataLoader.\"\"\"\n    return DataLoader(\n        self.train_dataset,\n        batch_size=self.batch_size,\n        shuffle=True,\n        num_workers=self.num_workers,\n        pin_memory=True,\n        persistent_workers=self.num_workers &gt; 0,\n    )\n</code></pre>"},{"location":"api/data/#mbti_classifier.data.MBTIDataModule.val_dataloader","title":"<code>val_dataloader()</code>","text":"<p>Create validation DataLoader.</p> Source code in <code>src/mbti_classifier/data.py</code> <pre><code>def val_dataloader(self):\n    \"\"\"Create validation DataLoader.\"\"\"\n    if self.val_dataset is None:\n        return None\n\n    return DataLoader(\n        self.val_dataset,\n        batch_size=self.batch_size,\n        shuffle=False,\n        num_workers=self.num_workers,\n        pin_memory=True,\n        persistent_workers=self.num_workers &gt; 0,\n    )\n</code></pre>"},{"location":"api/model/","title":"Model","text":"<p>               Bases: <code>Module</code></p> <p>Core MBTI classification model architecture.</p> <p>Multi-task binary classification model with shared encoder and task-specific heads for predicting MBTI dimensions: E/I, S/N, T/F, J/P.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>class MBTIModel(nn.Module):\n    \"\"\"\n    Core MBTI classification model architecture.\n\n    Multi-task binary classification model with shared encoder and task-specific heads\n    for predicting MBTI dimensions: E/I, S/N, T/F, J/P.\n    \"\"\"\n\n    def __init__(\n        self,\n        model_name: str = \"distilbert-base-uncased\",\n        dropout: float = 0.1,\n    ):\n        \"\"\"\n        Args:\n            model_name: HuggingFace model name to load\n            dropout: Dropout probability for classification head\n        \"\"\"\n        super().__init__()\n\n        # Load pretrained transformer model\n        logger.info(f\"Loading pretrained model: {model_name}\")\n        self.encoder = AutoModel.from_pretrained(model_name)\n        hidden_size = self.encoder.config.hidden_size\n\n        # Multi-task classification heads (4 binary classifiers)\n        # Shared representation\n        self.shared_layers = nn.Sequential(\n            nn.Dropout(dropout),\n            nn.Linear(hidden_size, hidden_size),\n            nn.GELU(),\n            nn.Dropout(dropout),\n        )\n\n        # Task-specific heads for E/I, S/N, T/F, J/P\n        self.ei_head = nn.Linear(hidden_size, 1)  # E vs I\n        self.sn_head = nn.Linear(hidden_size, 1)  # S vs N\n        self.tf_head = nn.Linear(hidden_size, 1)  # T vs F\n        self.jp_head = nn.Linear(hidden_size, 1)  # J vs P\n\n    def forward(self, input_ids: torch.Tensor, attention_mask: torch.Tensor) -&gt; torch.Tensor:\n        \"\"\"\n        Forward pass through the model.\n\n        Args:\n            input_ids: Input token IDs [batch_size, seq_length]\n            attention_mask: Attention mask [batch_size, seq_length]\n\n        Returns:\n            Logits [batch_size, 4] for E/I, S/N, T/F, J/P tasks\n        \"\"\"\n        # Get encoder outputs\n        outputs = self.encoder(input_ids=input_ids, attention_mask=attention_mask)\n\n        # Use [CLS] token representation (first token)\n        cls_output = outputs.last_hidden_state[:, 0, :]\n\n        # Pass through shared layers\n        shared_repr = self.shared_layers(cls_output)\n\n        # Task-specific predictions\n        ei_logits = self.ei_head(shared_repr)  # [batch_size, 1]\n        sn_logits = self.sn_head(shared_repr)  # [batch_size, 1]\n        tf_logits = self.tf_head(shared_repr)  # [batch_size, 1]\n        jp_logits = self.jp_head(shared_repr)  # [batch_size, 1]\n\n        # Concatenate all task logits\n        logits = torch.cat([ei_logits, sn_logits, tf_logits, jp_logits], dim=1)  # [batch_size, 4]\n\n        return logits\n</code></pre> <p>               Bases: <code>LightningModule</code></p> <p>PyTorch Lightning module for MBTI personality classification using DistilBERT.</p> <p>Treats MBTI as 4 independent binary classification tasks (multi-task learning): - E/I (Extraversion vs Introversion) - S/N (Sensing vs Intuition) - T/F (Thinking vs Feeling) - J/P (Judging vs Perceiving)</p> <p>This approach is more effective than 16-class classification and more interpretable.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>class MBTIClassifier(pl.LightningModule):\n    \"\"\"\n    PyTorch Lightning module for MBTI personality classification using DistilBERT.\n\n    Treats MBTI as 4 independent binary classification tasks (multi-task learning):\n    - E/I (Extraversion vs Introversion)\n    - S/N (Sensing vs Intuition)\n    - T/F (Thinking vs Feeling)\n    - J/P (Judging vs Perceiving)\n\n    This approach is more effective than 16-class classification and more interpretable.\n    \"\"\"\n\n    def __init__(\n        self,\n        model_name: str = \"distilbert-base-uncased\",\n        learning_rate: float = 2e-5,\n        weight_decay: float = 0.01,\n        warmup_steps: int = 500,\n        dropout: float = 0.1,\n        freeze_encoder: bool = False,\n        freeze_layers: int = 0,\n        pos_weight: Optional[torch.Tensor] = None,\n    ):\n        \"\"\"\n        Args:\n            model_name: HuggingFace model name to load\n            learning_rate: Peak learning rate for optimizer\n            weight_decay: Weight decay for AdamW optimizer\n            warmup_steps: Number of warmup steps for learning rate scheduler\n            dropout: Dropout probability for classification head\n            freeze_encoder: Whether to freeze the entire encoder\n            freeze_layers: Number of encoder layers to freeze (from bottom)\n            pos_weight: Optional positive class weights for each task [4] to handle imbalance\n        \"\"\"\n        super().__init__()\n        self.save_hyperparameters(ignore=[\"pos_weight\"])\n\n        # Create the model\n        self.model = MBTIModel(model_name=model_name, dropout=dropout)\n\n        # Compile the model if supported (requires Python &lt; 3.12)\n        self.model = torch.compile(self.model)\n        logger.info(\"Model compiled with torch.compile\")\n\n\n        # Loss function - Binary Cross Entropy with Logits (includes sigmoid)\n        self.criterion = nn.BCEWithLogitsLoss(pos_weight=pos_weight, reduction=\"none\")\n\n        # Metrics - use nn.ModuleDict to ensure proper device placement\n        self.train_metrics = nn.ModuleDict(self._create_metrics(prefix=\"train\"))\n        self.val_metrics = nn.ModuleDict(self._create_metrics(prefix=\"val\"))\n        self.test_metrics = nn.ModuleDict(self._create_metrics(prefix=\"test\"))\n\n        # Apply freezing if specified\n        self._freeze_parameters()\n\n        logger.info(f\"Model initialized with {self.count_parameters():,} parameters\")\n        logger.info(f\"Trainable parameters: {self.count_trainable_parameters():,}\")\n\n    def _create_metrics(self, prefix: str) -&gt; Dict[str, MetricCollection]:\n        \"\"\"Create metrics for each binary classification task.\"\"\"\n        task_names = [\"EI\", \"SN\", \"TF\", \"JP\"]\n        metrics = {}\n\n        for task_name in task_names:\n            metrics[task_name] = MetricCollection(\n                {\n                    f\"{prefix}/{task_name}_acc\": Accuracy(task=\"binary\"),\n                    f\"{prefix}/{task_name}_f1\": F1Score(task=\"binary\"),\n                }\n            )\n\n        # Overall metrics (average across all tasks)\n        metrics[\"overall\"] = MetricCollection(\n            {\n                f\"{prefix}/avg_acc\": Accuracy(task=\"binary\"),\n                f\"{prefix}/avg_f1\": F1Score(task=\"binary\"),\n            }\n        )\n\n        return metrics\n\n    def _freeze_parameters(self):\n        \"\"\"Freeze encoder parameters based on configuration.\"\"\"\n        if self.hparams.freeze_encoder:\n            logger.info(\"Freezing entire encoder\")\n            for param in self.model.encoder.parameters():\n                param.requires_grad = False\n        elif self.hparams.freeze_layers &gt; 0:\n            logger.info(f\"Freezing first {self.hparams.freeze_layers} layers\")\n            # For DistilBERT, freeze transformer layers\n            if hasattr(self.model.encoder, \"transformer\"):\n                for layer in self.model.encoder.transformer.layer[: self.hparams.freeze_layers]:\n                    for param in layer.parameters():\n                        param.requires_grad = False\n\n    def forward(self, input_ids: torch.Tensor, attention_mask: torch.Tensor) -&gt; torch.Tensor:\n        \"\"\"\n        Forward pass through the model.\n\n        Args:\n            input_ids: Input token IDs [batch_size, seq_length]\n            attention_mask: Attention mask [batch_size, seq_length]\n\n        Returns:\n            Logits [batch_size, 4] for E/I, S/N, T/F, J/P tasks\n        \"\"\"\n        return self.model(input_ids, attention_mask)\n\n    def _shared_step(self, batch: Dict[str, torch.Tensor], stage: str) -&gt; Dict[str, torch.Tensor]:\n        \"\"\"\n        Shared step for train/val/test.\n\n        Args:\n            batch: Batch dictionary with input_ids, attention_mask, labels\n            stage: One of 'train', 'val', 'test'\n\n        Returns:\n            Dictionary with loss and logits\n        \"\"\"\n        input_ids = batch[\"input_ids\"]\n        attention_mask = batch[\"attention_mask\"]\n        labels = batch[\"labels\"]  # [batch_size, 4] binary labels\n\n        # Forward pass\n        logits = self(input_ids, attention_mask)  # [batch_size, 4]\n\n        # Compute loss for each task\n        task_losses = self.criterion(logits, labels)  # [batch_size, 4]\n        loss = task_losses.mean()  # Average across all tasks and batch\n\n        # Get predictions (apply sigmoid and threshold at 0.5)\n        probs = torch.sigmoid(logits)\n        preds = (probs &gt; 0.5).float()\n\n        # Update metrics for each task\n        metrics = getattr(self, f\"{stage}_metrics\")\n        task_names = [\"EI\", \"SN\", \"TF\", \"JP\"]\n\n        for i, task_name in enumerate(task_names):\n            metrics[task_name].update(preds[:, i], labels[:, i].long())\n\n        # Update overall metrics (flatten all predictions)\n        metrics[\"overall\"].update(preds.flatten(), labels.flatten().long())\n\n        return {\"loss\": loss, \"logits\": logits, \"preds\": preds, \"labels\": labels}\n\n    def training_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; torch.Tensor:\n        \"\"\"Training step.\"\"\"\n        outputs = self._shared_step(batch, \"train\")\n        self.log(\"train/loss\", outputs[\"loss\"], on_step=True, on_epoch=True, prog_bar=True, sync_dist=True)\n        return outputs[\"loss\"]\n\n    def on_train_epoch_end(self):\n        \"\"\"Log training metrics at epoch end.\"\"\"\n        all_metrics = {}\n        for task_name, metric_collection in self.train_metrics.items():\n            all_metrics.update(metric_collection.compute())\n            metric_collection.reset()\n        self.log_dict(all_metrics, sync_dist=True)\n\n    def validation_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; torch.Tensor:\n        \"\"\"Validation step.\"\"\"\n        outputs = self._shared_step(batch, \"val\")\n        self.log(\"val/loss\", outputs[\"loss\"], on_step=False, on_epoch=True, prog_bar=True, sync_dist=True)\n        return outputs[\"loss\"]\n\n    def on_validation_epoch_end(self):\n        \"\"\"Log validation metrics at epoch end.\"\"\"\n        all_metrics = {}\n        for task_name, metric_collection in self.val_metrics.items():\n            all_metrics.update(metric_collection.compute())\n            metric_collection.reset()\n        self.log_dict(all_metrics, sync_dist=True)\n\n    def test_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; torch.Tensor:\n        \"\"\"Test step.\"\"\"\n        outputs = self._shared_step(batch, \"test\")\n        self.log(\"test/loss\", outputs[\"loss\"], on_step=False, on_epoch=True, sync_dist=True)\n        return outputs[\"loss\"]\n\n    def on_test_epoch_end(self):\n        \"\"\"Log test metrics at epoch end.\"\"\"\n        all_metrics = {}\n        for task_name, metric_collection in self.test_metrics.items():\n            all_metrics.update(metric_collection.compute())\n            metric_collection.reset()\n        self.log_dict(all_metrics, sync_dist=True)\n\n    def configure_optimizers(self):\n        \"\"\"Configure optimizer and learning rate scheduler.\"\"\"\n        # Separate parameters for weight decay\n        no_decay = [\"bias\", \"LayerNorm.weight\", \"LayerNorm.bias\"]\n        optimizer_grouped_parameters = [\n            {\n                \"params\": [p for n, p in self.named_parameters() if not any(nd in n for nd in no_decay)],\n                \"weight_decay\": self.hparams.weight_decay,\n            },\n            {\n                \"params\": [p for n, p in self.named_parameters() if any(nd in n for nd in no_decay)],\n                \"weight_decay\": 0.0,\n            },\n        ]\n\n        optimizer = torch.optim.AdamW(optimizer_grouped_parameters, lr=self.hparams.learning_rate)\n\n        # Learning rate scheduler\n        scheduler = get_linear_schedule_with_warmup(\n            optimizer,\n            num_warmup_steps=self.hparams.warmup_steps,\n            num_training_steps=self.trainer.estimated_stepping_batches,\n        )\n\n        return {\n            \"optimizer\": optimizer,\n            \"lr_scheduler\": {\n                \"scheduler\": scheduler,\n                \"interval\": \"step\",\n                \"frequency\": 1,\n            },\n        }\n\n    def count_parameters(self) -&gt; int:\n        \"\"\"Count total parameters.\"\"\"\n        return sum(p.numel() for p in self.parameters())\n\n    def count_trainable_parameters(self) -&gt; int:\n        \"\"\"Count trainable parameters.\"\"\"\n        return sum(p.numel() for p in self.parameters() if p.requires_grad)\n\n    def predict_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; Dict[str, torch.Tensor]:\n        \"\"\"Prediction step for inference.\"\"\"\n        input_ids = batch[\"input_ids\"]\n        attention_mask = batch[\"attention_mask\"]\n\n        logits = self(input_ids, attention_mask)  # [batch_size, 4]\n        probs = torch.sigmoid(logits)  # [batch_size, 4] probabilities\n        preds = (probs &gt; 0.5).long()  # [batch_size, 4] binary predictions\n\n        # Convert binary predictions to MBTI types\n        mbti_types = self._binary_to_mbti(preds)\n\n        return {\n            \"predictions\": preds,  # [batch_size, 4] binary for E/I, S/N, T/F, J/P\n            \"probabilities\": probs,  # [batch_size, 4]\n            \"mbti_types\": mbti_types,  # List of MBTI strings like ['INTJ', 'ENFP', ...]\n            \"logits\": logits,\n        }\n\n    def _binary_to_mbti(self, binary_preds: torch.Tensor) -&gt; list:\n        \"\"\"Convert binary predictions to MBTI type strings.\n\n        Args:\n            binary_preds: [batch_size, 4] tensor with binary values\n\n        Returns:\n            List of MBTI type strings\n        \"\"\"\n        mbti_types = []\n        for pred in binary_preds:\n            # pred is [E, S, T, J] where 1 means E/S/T/J, 0 means I/N/F/P\n            ei = \"E\" if pred[0] == 1 else \"I\"\n            sn = \"S\" if pred[1] == 1 else \"N\"\n            tf = \"T\" if pred[2] == 1 else \"F\"\n            jp = \"J\" if pred[3] == 1 else \"P\"\n            mbti_types.append(f\"{ei}{sn}{tf}{jp}\")\n        return mbti_types\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIModel.__init__","title":"<code>__init__(model_name='distilbert-base-uncased', dropout=0.1)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>HuggingFace model name to load</p> <code>'distilbert-base-uncased'</code> <code>dropout</code> <code>float</code> <p>Dropout probability for classification head</p> <code>0.1</code> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def __init__(\n    self,\n    model_name: str = \"distilbert-base-uncased\",\n    dropout: float = 0.1,\n):\n    \"\"\"\n    Args:\n        model_name: HuggingFace model name to load\n        dropout: Dropout probability for classification head\n    \"\"\"\n    super().__init__()\n\n    # Load pretrained transformer model\n    logger.info(f\"Loading pretrained model: {model_name}\")\n    self.encoder = AutoModel.from_pretrained(model_name)\n    hidden_size = self.encoder.config.hidden_size\n\n    # Multi-task classification heads (4 binary classifiers)\n    # Shared representation\n    self.shared_layers = nn.Sequential(\n        nn.Dropout(dropout),\n        nn.Linear(hidden_size, hidden_size),\n        nn.GELU(),\n        nn.Dropout(dropout),\n    )\n\n    # Task-specific heads for E/I, S/N, T/F, J/P\n    self.ei_head = nn.Linear(hidden_size, 1)  # E vs I\n    self.sn_head = nn.Linear(hidden_size, 1)  # S vs N\n    self.tf_head = nn.Linear(hidden_size, 1)  # T vs F\n    self.jp_head = nn.Linear(hidden_size, 1)  # J vs P\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIModel.forward","title":"<code>forward(input_ids, attention_mask)</code>","text":"<p>Forward pass through the model.</p> <p>Parameters:</p> Name Type Description Default <code>input_ids</code> <code>Tensor</code> <p>Input token IDs [batch_size, seq_length]</p> required <code>attention_mask</code> <code>Tensor</code> <p>Attention mask [batch_size, seq_length]</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Logits [batch_size, 4] for E/I, S/N, T/F, J/P tasks</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def forward(self, input_ids: torch.Tensor, attention_mask: torch.Tensor) -&gt; torch.Tensor:\n    \"\"\"\n    Forward pass through the model.\n\n    Args:\n        input_ids: Input token IDs [batch_size, seq_length]\n        attention_mask: Attention mask [batch_size, seq_length]\n\n    Returns:\n        Logits [batch_size, 4] for E/I, S/N, T/F, J/P tasks\n    \"\"\"\n    # Get encoder outputs\n    outputs = self.encoder(input_ids=input_ids, attention_mask=attention_mask)\n\n    # Use [CLS] token representation (first token)\n    cls_output = outputs.last_hidden_state[:, 0, :]\n\n    # Pass through shared layers\n    shared_repr = self.shared_layers(cls_output)\n\n    # Task-specific predictions\n    ei_logits = self.ei_head(shared_repr)  # [batch_size, 1]\n    sn_logits = self.sn_head(shared_repr)  # [batch_size, 1]\n    tf_logits = self.tf_head(shared_repr)  # [batch_size, 1]\n    jp_logits = self.jp_head(shared_repr)  # [batch_size, 1]\n\n    # Concatenate all task logits\n    logits = torch.cat([ei_logits, sn_logits, tf_logits, jp_logits], dim=1)  # [batch_size, 4]\n\n    return logits\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.__init__","title":"<code>__init__(model_name='distilbert-base-uncased', learning_rate=2e-05, weight_decay=0.01, warmup_steps=500, dropout=0.1, freeze_encoder=False, freeze_layers=0, pos_weight=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>HuggingFace model name to load</p> <code>'distilbert-base-uncased'</code> <code>learning_rate</code> <code>float</code> <p>Peak learning rate for optimizer</p> <code>2e-05</code> <code>weight_decay</code> <code>float</code> <p>Weight decay for AdamW optimizer</p> <code>0.01</code> <code>warmup_steps</code> <code>int</code> <p>Number of warmup steps for learning rate scheduler</p> <code>500</code> <code>dropout</code> <code>float</code> <p>Dropout probability for classification head</p> <code>0.1</code> <code>freeze_encoder</code> <code>bool</code> <p>Whether to freeze the entire encoder</p> <code>False</code> <code>freeze_layers</code> <code>int</code> <p>Number of encoder layers to freeze (from bottom)</p> <code>0</code> <code>pos_weight</code> <code>Optional[Tensor]</code> <p>Optional positive class weights for each task [4] to handle imbalance</p> <code>None</code> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def __init__(\n    self,\n    model_name: str = \"distilbert-base-uncased\",\n    learning_rate: float = 2e-5,\n    weight_decay: float = 0.01,\n    warmup_steps: int = 500,\n    dropout: float = 0.1,\n    freeze_encoder: bool = False,\n    freeze_layers: int = 0,\n    pos_weight: Optional[torch.Tensor] = None,\n):\n    \"\"\"\n    Args:\n        model_name: HuggingFace model name to load\n        learning_rate: Peak learning rate for optimizer\n        weight_decay: Weight decay for AdamW optimizer\n        warmup_steps: Number of warmup steps for learning rate scheduler\n        dropout: Dropout probability for classification head\n        freeze_encoder: Whether to freeze the entire encoder\n        freeze_layers: Number of encoder layers to freeze (from bottom)\n        pos_weight: Optional positive class weights for each task [4] to handle imbalance\n    \"\"\"\n    super().__init__()\n    self.save_hyperparameters(ignore=[\"pos_weight\"])\n\n    # Create the model\n    self.model = MBTIModel(model_name=model_name, dropout=dropout)\n\n    # Compile the model if supported (requires Python &lt; 3.12)\n    self.model = torch.compile(self.model)\n    logger.info(\"Model compiled with torch.compile\")\n\n\n    # Loss function - Binary Cross Entropy with Logits (includes sigmoid)\n    self.criterion = nn.BCEWithLogitsLoss(pos_weight=pos_weight, reduction=\"none\")\n\n    # Metrics - use nn.ModuleDict to ensure proper device placement\n    self.train_metrics = nn.ModuleDict(self._create_metrics(prefix=\"train\"))\n    self.val_metrics = nn.ModuleDict(self._create_metrics(prefix=\"val\"))\n    self.test_metrics = nn.ModuleDict(self._create_metrics(prefix=\"test\"))\n\n    # Apply freezing if specified\n    self._freeze_parameters()\n\n    logger.info(f\"Model initialized with {self.count_parameters():,} parameters\")\n    logger.info(f\"Trainable parameters: {self.count_trainable_parameters():,}\")\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.configure_optimizers","title":"<code>configure_optimizers()</code>","text":"<p>Configure optimizer and learning rate scheduler.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def configure_optimizers(self):\n    \"\"\"Configure optimizer and learning rate scheduler.\"\"\"\n    # Separate parameters for weight decay\n    no_decay = [\"bias\", \"LayerNorm.weight\", \"LayerNorm.bias\"]\n    optimizer_grouped_parameters = [\n        {\n            \"params\": [p for n, p in self.named_parameters() if not any(nd in n for nd in no_decay)],\n            \"weight_decay\": self.hparams.weight_decay,\n        },\n        {\n            \"params\": [p for n, p in self.named_parameters() if any(nd in n for nd in no_decay)],\n            \"weight_decay\": 0.0,\n        },\n    ]\n\n    optimizer = torch.optim.AdamW(optimizer_grouped_parameters, lr=self.hparams.learning_rate)\n\n    # Learning rate scheduler\n    scheduler = get_linear_schedule_with_warmup(\n        optimizer,\n        num_warmup_steps=self.hparams.warmup_steps,\n        num_training_steps=self.trainer.estimated_stepping_batches,\n    )\n\n    return {\n        \"optimizer\": optimizer,\n        \"lr_scheduler\": {\n            \"scheduler\": scheduler,\n            \"interval\": \"step\",\n            \"frequency\": 1,\n        },\n    }\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.count_parameters","title":"<code>count_parameters()</code>","text":"<p>Count total parameters.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def count_parameters(self) -&gt; int:\n    \"\"\"Count total parameters.\"\"\"\n    return sum(p.numel() for p in self.parameters())\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.count_trainable_parameters","title":"<code>count_trainable_parameters()</code>","text":"<p>Count trainable parameters.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def count_trainable_parameters(self) -&gt; int:\n    \"\"\"Count trainable parameters.\"\"\"\n    return sum(p.numel() for p in self.parameters() if p.requires_grad)\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.forward","title":"<code>forward(input_ids, attention_mask)</code>","text":"<p>Forward pass through the model.</p> <p>Parameters:</p> Name Type Description Default <code>input_ids</code> <code>Tensor</code> <p>Input token IDs [batch_size, seq_length]</p> required <code>attention_mask</code> <code>Tensor</code> <p>Attention mask [batch_size, seq_length]</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Logits [batch_size, 4] for E/I, S/N, T/F, J/P tasks</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def forward(self, input_ids: torch.Tensor, attention_mask: torch.Tensor) -&gt; torch.Tensor:\n    \"\"\"\n    Forward pass through the model.\n\n    Args:\n        input_ids: Input token IDs [batch_size, seq_length]\n        attention_mask: Attention mask [batch_size, seq_length]\n\n    Returns:\n        Logits [batch_size, 4] for E/I, S/N, T/F, J/P tasks\n    \"\"\"\n    return self.model(input_ids, attention_mask)\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.on_test_epoch_end","title":"<code>on_test_epoch_end()</code>","text":"<p>Log test metrics at epoch end.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def on_test_epoch_end(self):\n    \"\"\"Log test metrics at epoch end.\"\"\"\n    all_metrics = {}\n    for task_name, metric_collection in self.test_metrics.items():\n        all_metrics.update(metric_collection.compute())\n        metric_collection.reset()\n    self.log_dict(all_metrics, sync_dist=True)\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.on_train_epoch_end","title":"<code>on_train_epoch_end()</code>","text":"<p>Log training metrics at epoch end.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def on_train_epoch_end(self):\n    \"\"\"Log training metrics at epoch end.\"\"\"\n    all_metrics = {}\n    for task_name, metric_collection in self.train_metrics.items():\n        all_metrics.update(metric_collection.compute())\n        metric_collection.reset()\n    self.log_dict(all_metrics, sync_dist=True)\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.on_validation_epoch_end","title":"<code>on_validation_epoch_end()</code>","text":"<p>Log validation metrics at epoch end.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def on_validation_epoch_end(self):\n    \"\"\"Log validation metrics at epoch end.\"\"\"\n    all_metrics = {}\n    for task_name, metric_collection in self.val_metrics.items():\n        all_metrics.update(metric_collection.compute())\n        metric_collection.reset()\n    self.log_dict(all_metrics, sync_dist=True)\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.predict_step","title":"<code>predict_step(batch, batch_idx)</code>","text":"<p>Prediction step for inference.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def predict_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; Dict[str, torch.Tensor]:\n    \"\"\"Prediction step for inference.\"\"\"\n    input_ids = batch[\"input_ids\"]\n    attention_mask = batch[\"attention_mask\"]\n\n    logits = self(input_ids, attention_mask)  # [batch_size, 4]\n    probs = torch.sigmoid(logits)  # [batch_size, 4] probabilities\n    preds = (probs &gt; 0.5).long()  # [batch_size, 4] binary predictions\n\n    # Convert binary predictions to MBTI types\n    mbti_types = self._binary_to_mbti(preds)\n\n    return {\n        \"predictions\": preds,  # [batch_size, 4] binary for E/I, S/N, T/F, J/P\n        \"probabilities\": probs,  # [batch_size, 4]\n        \"mbti_types\": mbti_types,  # List of MBTI strings like ['INTJ', 'ENFP', ...]\n        \"logits\": logits,\n    }\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.test_step","title":"<code>test_step(batch, batch_idx)</code>","text":"<p>Test step.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def test_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; torch.Tensor:\n    \"\"\"Test step.\"\"\"\n    outputs = self._shared_step(batch, \"test\")\n    self.log(\"test/loss\", outputs[\"loss\"], on_step=False, on_epoch=True, sync_dist=True)\n    return outputs[\"loss\"]\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.training_step","title":"<code>training_step(batch, batch_idx)</code>","text":"<p>Training step.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def training_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; torch.Tensor:\n    \"\"\"Training step.\"\"\"\n    outputs = self._shared_step(batch, \"train\")\n    self.log(\"train/loss\", outputs[\"loss\"], on_step=True, on_epoch=True, prog_bar=True, sync_dist=True)\n    return outputs[\"loss\"]\n</code></pre>"},{"location":"api/model/#mbti_classifier.model.MBTIClassifier.validation_step","title":"<code>validation_step(batch, batch_idx)</code>","text":"<p>Validation step.</p> Source code in <code>src/mbti_classifier/model.py</code> <pre><code>def validation_step(self, batch: Dict[str, torch.Tensor], batch_idx: int) -&gt; torch.Tensor:\n    \"\"\"Validation step.\"\"\"\n    outputs = self._shared_step(batch, \"val\")\n    self.log(\"val/loss\", outputs[\"loss\"], on_step=False, on_epoch=True, prog_bar=True, sync_dist=True)\n    return outputs[\"loss\"]\n</code></pre>"},{"location":"api/ui/","title":"Streamlit UI","text":"<p>Documentation for the interactive Streamlit user interface.</p>"},{"location":"api/ui/#overview","title":"Overview","text":"<p>The Streamlit UI provides a user-friendly interface for MBTI personality predictions with:</p> <ul> <li>Text input for prediction</li> <li>Real-time API calls</li> <li>Interactive radar plot visualization</li> <li>Probability scores display</li> </ul>"},{"location":"api/ui/#features","title":"Features","text":""},{"location":"api/ui/#text-input","title":"Text Input","text":"<p>Large text area for entering writing samples:</p> <pre><code>text_input = st.text_area(\n    \"Enter your text\",\n    height=200,\n    placeholder=\"Write about your thoughts, interests, and personality...\"\n)\n</code></pre> <p>Recommendations: - Minimum 50 characters - Optimal: 200-500 words - Include varied topics for best results</p>"},{"location":"api/ui/#prediction-display","title":"Prediction Display","text":"<p>Results shown in three sections:</p> <ol> <li>Predicted Type: Large heading with MBTI type</li> <li>Radar Plot: Visual representation of dimensions</li> <li>Probability Scores: Detailed percentages</li> </ol>"},{"location":"api/ui/#radar-plot","title":"Radar Plot","text":"<p>Interactive Plotly radar chart showing:</p> <ul> <li>4 axes (E/I, S/N, T/F, J/P)</li> <li>Scores from 0-1</li> <li>Color-coded areas</li> <li>Hover tooltips</li> </ul>"},{"location":"api/ui/#configuration","title":"Configuration","text":""},{"location":"api/ui/#api-url","title":"API URL","text":"<p>Set via environment variable:</p> <pre><code># Local development\nexport API_URL=http://localhost:8000\n\n# Docker\nexport API_URL=http://api:8000\n\n# Remote\nexport API_URL=https://api.your-domain.com\n</code></pre> <p>In <code>docker-compose.yaml</code>:</p> <pre><code>ui:\n  environment:\n    - API_URL=http://api:8000\n</code></pre>"},{"location":"api/ui/#page-config","title":"Page Config","text":"<pre><code>st.set_page_config(\n    page_title=\"MBTI Classifier\",\n    page_icon=\"\ud83e\udde0\",\n    layout=\"wide\",\n)\n</code></pre>"},{"location":"api/ui/#usage-examples","title":"Usage Examples","text":""},{"location":"api/ui/#running-locally","title":"Running Locally","text":"<pre><code># Start API first\nuv run invoke api\n\n# Then start UI\nuv run invoke ui\n\n# Access at http://localhost:8501\n</code></pre>"},{"location":"api/ui/#running-in-docker","title":"Running in Docker","text":"<pre><code># Start both services\ndocker compose up -d api ui\n\n# Access at http://localhost:8501\n</code></pre>"},{"location":"api/ui/#custom-port","title":"Custom Port","text":"<pre><code># Use different port\nuv run invoke ui --port 8502\n\n# Or directly\nuv run streamlit run src/mbti_classifier/ui.py --server.port 8502\n</code></pre>"},{"location":"api/ui/#customization","title":"Customization","text":""},{"location":"api/ui/#theme","title":"Theme","text":"<p>Create <code>.streamlit/config.toml</code>:</p> <pre><code>[theme]\nprimaryColor = \"#FF4B4B\"\nbackgroundColor = \"#FFFFFF\"\nsecondaryBackgroundColor = \"#F0F2F6\"\ntextColor = \"#262730\"\nfont = \"sans serif\"\n</code></pre>"},{"location":"api/ui/#layout","title":"Layout","text":"<p>Modify <code>ui.py</code>:</p> <pre><code># Sidebar for additional info\nwith st.sidebar:\n    st.header(\"About\")\n    st.write(\"MBTI personality prediction using DistilBERT\")\n\n# Main area\ncol1, col2 = st.columns([2, 1])\n\nwith col1:\n    # Prediction area\n    pass\n\nwith col2:\n    # Additional info\n    pass\n</code></pre>"},{"location":"api/ui/#error-handling","title":"Error Handling","text":"<p>The UI handles various errors gracefully:</p>"},{"location":"api/ui/#api-unreachable","title":"API Unreachable","text":"<pre><code>try:\n    response = requests.post(...)\nexcept requests.ConnectionError:\n    st.error(\"\u274c Cannot connect to API. Make sure it's running on port 8000.\")\n</code></pre>"},{"location":"api/ui/#invalid-input","title":"Invalid Input","text":"<pre><code>if len(text_input) &lt; 10:\n    st.warning(\"\u26a0\ufe0f Please enter more text for accurate prediction.\")\n</code></pre>"},{"location":"api/ui/#api-errors","title":"API Errors","text":"<pre><code>if response.status_code != 200:\n    st.error(f\"\u274c Prediction failed: {response.json()['detail']}\")\n</code></pre>"},{"location":"api/ui/#deployment","title":"Deployment","text":""},{"location":"api/ui/#streamlit-cloud","title":"Streamlit Cloud","text":"<ol> <li>Push to GitHub</li> <li>Go to share.streamlit.io</li> <li>Connect repository</li> <li>Set <code>API_URL</code> in secrets</li> </ol>"},{"location":"api/ui/#docker","title":"Docker","text":"<p>Already configured in <code>docker-compose.yaml</code>:</p> <pre><code>ui:\n  build:\n    context: .\n    dockerfile: dockerfiles/ui.dockerfile\n  ports:\n    - \"8501:8501\"\n  environment:\n    - API_URL=http://api:8000\n</code></pre>"},{"location":"api/ui/#performance","title":"Performance","text":""},{"location":"api/ui/#caching","title":"Caching","text":"<p>Streamlit caches unchanged data:</p> <pre><code>@st.cache_data\ndef get_api_health():\n    response = requests.get(f\"{API_URL}/health\")\n    return response.json()\n</code></pre>"},{"location":"api/ui/#session-state","title":"Session State","text":"<p>Preserve user input across reruns:</p> <pre><code>if 'prediction_history' not in st.session_state:\n    st.session_state.prediction_history = []\n\n# Add new prediction\nst.session_state.prediction_history.append(result)\n</code></pre>"},{"location":"api/ui/#monitoring","title":"Monitoring","text":""},{"location":"api/ui/#user-analytics","title":"User Analytics","text":"<p>Add analytics to track usage:</p> <pre><code>import streamlit.components.v1 as components\n\n# Google Analytics\ncomponents.html(\"\"\"\n&lt;script async src=\"https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX\"&gt;&lt;/script&gt;\n&lt;script&gt;\n  window.dataLayer = window.dataLayer || [];\n  function gtag(){dataLayer.push(arguments);}\n  gtag('js', new Date());\n  gtag('config', 'G-XXXXXXXXXX');\n&lt;/script&gt;\n\"\"\", height=0)\n</code></pre>"},{"location":"api/ui/#error-logging","title":"Error Logging","text":"<pre><code>import logging\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\ntry:\n    response = requests.post(...)\nexcept Exception as e:\n    logger.error(f\"Prediction failed: {e}\")\n    st.error(\"Something went wrong. Please try again.\")\n</code></pre>"},{"location":"api/ui/#next-steps","title":"Next Steps","text":"<ul> <li>Read API Reference for backend details</li> <li>Explore Docker deployment</li> <li>Learn about Configuration</li> </ul>"},{"location":"architecture/api-ui/","title":"API and UI Integration","text":"<p>This page describes how the API (FastAPI) and the UI (Streamlit) are integrated into the project.</p>"},{"location":"architecture/api-ui/#architecture","title":"Architecture","text":"<pre><code>graph LR\n    A[User Browser] --&gt;|HTTP| B[Streamlit UI :8501]\n    B --&gt;|HTTP POST| C[FastAPI API :8000]\n    C --&gt;|Load Model| D[models/best.ckpt]\n    C --&gt;|Return Prediction| B\n    B --&gt;|Display Result| A\n</code></pre>"},{"location":"architecture/api-ui/#fastapi-backend","title":"FastAPI Backend","text":""},{"location":"architecture/api-ui/#components","title":"Components","text":""},{"location":"architecture/api-ui/#1-model-loading","title":"1. Model Loading","text":"<pre><code># src/mbti_classifier/api.py\nfrom src.mbti_classifier.model import MBTIClassifier\n\n# Load model at startup\nclassifier = MBTIClassifier.load_from_checkpoint('models/best.ckpt')\n</code></pre> <p>Features: - Loads the model only once at startup - Keeps the model in memory for fast inference - Uses the best checkpoint saved during training</p>"},{"location":"architecture/api-ui/#2-endpoints","title":"2. Endpoints","text":""},{"location":"architecture/api-ui/#health-check","title":"Health Check","text":"<pre><code>@app.get(\"/health\")\ndef health():\n    \"\"\"Checks that the API is running.\"\"\"\n    return {\"status\": \"healthy\"}\n</code></pre> <p>Usage:</p> <pre><code>curl http://localhost:8000/health\n# {\"status\": \"healthy\"}\n</code></pre>"},{"location":"architecture/api-ui/#single-prediction","title":"Single Prediction","text":"<pre><code>@app.post(\"/predict\")\ndef predict(request: PredictRequest):\n    \"\"\"Predicts the MBTI type of a text.\"\"\"\n    mbti_type = classifier.predict(request.text)\n    probs = classifier.predict_proba(request.text)\n\n    return {\n        \"mbti_type\": mbti_type,\n        \"probabilities\": {\n            \"E_I\": float(probs[0]),\n            \"S_N\": float(probs[1]),\n            \"T_F\": float(probs[2]),\n            \"J_P\": float(probs[3])\n        }\n    }\n</code></pre> <p>Request:</p> <pre><code>{\n  \"text\": \"I love analyzing complex problems and finding patterns.\"\n}\n</code></pre> <p>Response:</p> <pre><code>{\n  \"mbti_type\": \"INTP\",\n  \"probabilities\": {\n    \"E_I\": 0.15,\n    \"S_N\": 0.23,\n    \"T_F\": 0.82,\n    \"J_P\": 0.38\n  }\n}\n</code></pre>"},{"location":"architecture/api-ui/#batch-prediction","title":"Batch Prediction","text":"<pre><code>@app.post(\"/predict_batch\")\ndef predict_batch(request: PredictBatchRequest):\n    \"\"\"Predicts MBTI types for multiple texts.\"\"\"\n    predictions = []\n    for text in request.texts:\n        mbti_type = classifier.predict(text)\n        probs = classifier.predict_proba(text)\n        predictions.append({\n            \"text\": text,\n            \"mbti_type\": mbti_type,\n            \"probabilities\": {...}\n        })\n    return {\"predictions\": predictions}\n</code></pre>"},{"location":"architecture/api-ui/#pydantic-models","title":"Pydantic Models","text":"<pre><code>from pydantic import BaseModel, Field\n\nclass PredictRequest(BaseModel):\n    text: str = Field(..., min_length=1, description=\"Text to classify\")\n\nclass PredictBatchRequest(BaseModel):\n    texts: list[str] = Field(..., min_items=1, max_items=100)\n</code></pre> <p>Automatic validation: - <code>text</code> cannot be empty - <code>texts</code> must have 1-100 items - Returns HTTP 422 if validation fails</p>"},{"location":"architecture/api-ui/#cors","title":"CORS","text":"<pre><code>from fastapi.middleware.cors import CORSMiddleware\n\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],  # Allow all origins\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n</code></pre> <p>Allows: - Requests from any domain - Necessary for Streamlit (port 8501) to access API (port 8000)</p>"},{"location":"architecture/api-ui/#streamlit-frontend","title":"Streamlit Frontend","text":""},{"location":"architecture/api-ui/#components_1","title":"Components","text":""},{"location":"architecture/api-ui/#1-configuration","title":"1. Configuration","text":"<pre><code># src/mbti_classifier/ui.py\nimport streamlit as st\nimport os\n\n# API URL\nAPI_URL = os.getenv(\"API_URL\", \"http://localhost:8000\")\n\n# Configure page\nst.set_page_config(\n    page_title=\"MBTI Classifier\",\n    page_icon=\"\ud83e\udde0\",\n    layout=\"wide\"\n)\n</code></pre> <p>Environment Variables: - Local: <code>API_URL=http://localhost:8000</code> - Docker: <code>API_URL=http://api:8000</code> (uses service name)</p>"},{"location":"architecture/api-ui/#2-ui-layout","title":"2. UI Layout","text":"<pre><code># Header\nst.title(\"\ud83e\udde0 MBTI Personality Classifier\")\nst.markdown(\"Predict your personality type based on text.\")\n\n# Input\ntext = st.text_area(\n    \"Enter text:\",\n    placeholder=\"Write something about yourself...\",\n    height=150\n)\n\n# Button\nif st.button(\"Classify\", type=\"primary\"):\n    # Call API\n    ...\n</code></pre>"},{"location":"architecture/api-ui/#3-api-call","title":"3. API Call","text":"<pre><code>import requests\n\ndef predict(text: str) -&gt; dict:\n    \"\"\"Calls the API to predict.\"\"\"\n    try:\n        response = requests.post(\n            f\"{API_URL}/predict\",\n            json={\"text\": text},\n            timeout=10\n        )\n        response.raise_for_status()\n        return response.json()\n    except requests.exceptions.RequestException as e:\n        st.error(f\"Error connecting to the API: {e}\")\n        return None\n</code></pre>"},{"location":"architecture/api-ui/#4-result-visualization","title":"4. Result Visualization","text":"<pre><code># Show MBTI type\nst.success(f\"Your personality type is: **{result['mbti_type']}**\")\n\n# Show probabilities\nprobs = result['probabilities']\n\ncol1, col2, col3, col4 = st.columns(4)\nwith col1:\n    st.metric(\"E/I\", f\"{probs['E_I']:.0%}\", \n              \"E\" if probs['E_I'] &gt; 0.5 else \"I\")\nwith col2:\n    st.metric(\"S/N\", f\"{probs['S_N']:.0%}\",\n              \"S\" if probs['S_N'] &gt; 0.5 else \"N\")\n# ...\n\n# Progress bars\nst.progress(probs['E_I'], text=\"Extraversion\")\nst.progress(probs['S_N'], text=\"Sensing\")\n# ...\n</code></pre>"},{"location":"architecture/api-ui/#service-communication","title":"Service Communication","text":""},{"location":"architecture/api-ui/#local-development","title":"Local Development","text":"<pre><code># Terminal 1: API\nuv run python src/mbti_classifier/api.py\n# Running on http://localhost:8000\n\n# Terminal 2: UI\nuv run python src/mbti_classifier/ui.py\n# Running on http://localhost:8501\n</code></pre> <p>Flow: 1. User opens <code>http://localhost:8501</code> 2. Streamlit runs in browser 3. User enters text and clicks \"Classify\" 4. Streamlit sends POST to <code>http://localhost:8000/predict</code> 5. FastAPI processes and returns result 6. Streamlit displays result</p>"},{"location":"architecture/api-ui/#docker-production","title":"Docker (Production)","text":"<pre><code># docker-compose.yaml\nservices:\n  api:\n    build:\n      context: .\n      dockerfile: dockerfiles/api.dockerfile\n    ports:\n      - \"8000:8000\"\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8000/health\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n      start_period: 120s\n\n  ui:\n    build:\n      context: .\n      dockerfile: dockerfiles/ui.dockerfile\n    ports:\n      - \"8501:8501\"\n    environment:\n      - API_URL=http://api:8000  # Uses service name\n    depends_on:\n      api:\n        condition: service_healthy\n</code></pre> <p>Key differences: - Networking: Docker Compose creates an internal network - Service Discovery: UI accesses API via <code>http://api:8000</code> (service name) - Healthcheck: UI waits for API to be healthy before starting - Isolation: Each service in its own container</p>"},{"location":"architecture/api-ui/#error-handling","title":"Error Handling","text":""},{"location":"architecture/api-ui/#api","title":"API","text":"<pre><code>from fastapi import HTTPException\n\n@app.post(\"/predict\")\ndef predict(request: PredictRequest):\n    try:\n        mbti_type = classifier.predict(request.text)\n        return {\"mbti_type\": mbti_type, ...}\n    except Exception as e:\n        raise HTTPException(\n            status_code=500,\n            detail=f\"Prediction error: {str(e)}\"\n        )\n</code></pre>"},{"location":"architecture/api-ui/#ui","title":"UI","text":"<pre><code>try:\n    response = requests.post(f\"{API_URL}/predict\", json={\"text\": text})\n    response.raise_for_status()\n    result = response.json()\nexcept requests.exceptions.ConnectionError:\n    st.error(\"\u274c Cannot connect to API. Is it running?\")\nexcept requests.exceptions.Timeout:\n    st.error(\"\u23f1\ufe0f The API took too long to respond.\")\nexcept requests.exceptions.HTTPError as e:\n    st.error(f\"\u274c HTTP Error: {e.response.status_code}\")\nexcept Exception as e:\n    st.error(f\"\u274c Unexpected error: {str(e)}\")\n</code></pre>"},{"location":"architecture/api-ui/#testing","title":"Testing","text":""},{"location":"architecture/api-ui/#api-tests","title":"API Tests","text":"<pre><code># tests/unit/test_api.py\nfrom fastapi.testclient import TestClient\nfrom src.mbti_classifier.api import app\n\nclient = TestClient(app)\n\ndef test_health():\n    response = client.get(\"/health\")\n    assert response.status_code == 200\n    assert response.json() == {\"status\": \"healthy\"}\n\ndef test_predict():\n    response = client.post(\n        \"/predict\",\n        json={\"text\": \"I love solving complex problems.\"}\n    )\n    assert response.status_code == 200\n    data = response.json()\n    assert \"mbti_type\" in data\n    assert len(data[\"mbti_type\"]) == 4\n</code></pre>"},{"location":"architecture/api-ui/#ui-tests","title":"UI Tests","text":"<pre><code># tests/integration/test_ui.py\nimport requests\n\ndef test_ui_api_integration():\n    \"\"\"Test that UI can communicate with API.\"\"\"\n    # Assume API is running\n    API_URL = \"http://localhost:8000\"\n\n    # Call API as UI does\n    response = requests.post(\n        f\"{API_URL}/predict\",\n        json={\"text\": \"Test text\"}\n    )\n\n    assert response.status_code == 200\n    assert \"mbti_type\" in response.json()\n</code></pre>"},{"location":"architecture/api-ui/#deployment","title":"Deployment","text":""},{"location":"architecture/api-ui/#local","title":"Local","text":"<pre><code># Terminal 1\nuv run invoke api\n\n# Terminal 2\nuv run invoke ui\n</code></pre>"},{"location":"architecture/api-ui/#docker","title":"Docker","text":"<pre><code># Build\nuv run invoke docker-build\n\n# Start services\nuv run invoke docker-up\n\n# Check logs\nuv run invoke docker-logs --service=api\nuv run invoke docker-logs --service=ui\n\n# Stop\nuv run invoke docker-down\n</code></pre>"},{"location":"architecture/api-ui/#cloud-example-with-flyio","title":"Cloud (example with Fly.io)","text":"<pre><code># API\nfly launch --dockerfile=dockerfiles/api.dockerfile --name mbti-api\n\n# UI\nfly launch --dockerfile=dockerfiles/ui.dockerfile --name mbti-ui \\\n    --env API_URL=[https://mbti-api.fly.dev](https://mbti-api.fly.dev)\n</code></pre>"},{"location":"architecture/api-ui/#monitoring","title":"Monitoring","text":""},{"location":"architecture/api-ui/#api-metrics","title":"API Metrics","text":"<p>FastAPI provides automatic metrics at <code>/docs</code>:</p> <pre><code>http://localhost:8000/docs\n</code></pre> <p>Useful metrics: - Requests per second - Average latency - 4xx/5xx errors</p>"},{"location":"architecture/api-ui/#ui-analytics","title":"UI Analytics","text":"<p>Streamlit Cloud provides automatic analytics:</p> <pre><code># Optional: add Google Analytics\nst.markdown(\"\"\"\n&lt;script async src=\"[https://www.googletagmanager.com/gtag/js?id=GA_ID](https://www.googletagmanager.com/gtag/js?id=GA_ID)\"&gt;&lt;/script&gt;\n\"\"\", unsafe_allow_html=True)\n</code></pre>"},{"location":"architecture/api-ui/#performance","title":"Performance","text":""},{"location":"architecture/api-ui/#api-optimization","title":"API Optimization","text":"<pre><code># 1. Batch predictions (more efficient)\n@app.post(\"/predict_batch\")\ndef predict_batch(request: PredictBatchRequest):\n    # Process in batch instead of one by one\n    texts = request.texts\n    predictions = classifier.predict_batch(texts)  # Vectorized\n    return {\"predictions\": predictions}\n\n# 2. Caching (for repeated requests)\nfrom functools import lru_cache\n\n@lru_cache(maxsize=128)\ndef cached_predict(text: str) -&gt; str:\n    return classifier.predict(text)\n</code></pre>"},{"location":"architecture/api-ui/#ui-optimization","title":"UI Optimization","text":"<pre><code># 1. Cache in Streamlit\n@st.cache_data\ndef load_data():\n    # Data that doesn't change\n    return ...\n\n# 2. Session state (avoid recomputation)\nif \"result\" not in st.session_state:\n    st.session_state.result = None\n</code></pre>"},{"location":"architecture/api-ui/#security","title":"Security","text":""},{"location":"architecture/api-ui/#api_1","title":"API","text":"<pre><code># 1. Rate limiting (requires slowapi)\nfrom slowapi import Limiter\nfrom slowapi.util import get_remote_address\n\nlimiter = Limiter(key_func=get_remote_address)\napp.state.limiter = limiter\n\n@app.post(\"/predict\")\n@limiter.limit(\"100/minute\")\ndef predict(request: PredictRequest):\n    ...\n\n# 2. API Key authentication\nfrom fastapi import Header, HTTPException\n\nasync def verify_api_key(x_api_key: str = Header()):\n    if x_api_key != os.getenv(\"API_KEY\"):\n        raise HTTPException(status_code=401, detail=\"Invalid API key\")\n\n@app.post(\"/predict\", dependencies=[Depends(verify_api_key)])\ndef predict(request: PredictRequest):\n    ...\n</code></pre>"},{"location":"architecture/api-ui/#ui_1","title":"UI","text":"<pre><code># Input sanitization\nimport html\n\ndef sanitize_input(text: str) -&gt; str:\n    \"\"\"Sanitize user input.\"\"\"\n    return html.escape(text)\n\n# Usage:\nsafe_text = sanitize_input(user_input)\nresult = predict(safe_text)\n</code></pre>"},{"location":"architecture/api-ui/#troubleshooting","title":"Troubleshooting","text":""},{"location":"architecture/api-ui/#ui-cannot-connect-to-api","title":"UI cannot connect to API","text":"<p>Problem: <pre><code>Error connecting to the API: Connection refused\n</code></pre></p> <p>Solutions:</p> <pre><code># 1. Verify that API is running\ncurl http://localhost:8000/health\n\n# 2. Verify API_URL in UI\n# In ui.py:\nAPI_URL = os.getenv(\"API_URL\", \"http://localhost:8000\")\n\n# 3. In Docker, verify networking\ndocker compose logs api\ndocker compose exec ui ping api\n</code></pre>"},{"location":"architecture/api-ui/#api-returns-500","title":"API returns 500","text":"<p>Problem: <pre><code>{\"detail\": \"Internal Server Error\"}\n</code></pre></p> <p>Solutions:</p> <pre><code># 1. Check API logs\ndocker compose logs api\n\n# 2. Verify model exists\nls -lh models/best.ckpt\n\n# 3. Direct test\ncurl -X POST http://localhost:8000/predict \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"text\": \"test\"}'\n</code></pre>"},{"location":"architecture/api-ui/#prediction-is-very-slow","title":"Prediction is very slow","text":"<p>Problem: Each prediction takes &gt;10 seconds</p> <p>Solutions:</p> <pre><code># 1. Move model to GPU (in api.py)\nimport torch\nclassifier = classifier.to('cuda' if torch.cuda.is_available() else 'cpu')\n\n# 2. Use batch prediction\n# Accumulate requests and process in batch\n\n# 3. Reduce max_length\n# In MBTIClassifier, tokenize with max_length=256 instead of 512\n</code></pre>"},{"location":"architecture/api-ui/#references","title":"References","text":"<ul> <li>FastAPI Documentation</li> <li>Streamlit Documentation</li> <li>Docker Compose Networking</li> <li>API Development</li> <li>UI Development</li> </ul>"},{"location":"architecture/data-pipeline/","title":"Data Pipeline","text":"<p>This page describes the complete data flow from download to processing and loading into the model.</p>"},{"location":"architecture/data-pipeline/#overview","title":"Overview","text":"<pre><code>graph TD\n    A[Kaggle Dataset] --&gt;|Download| B[data/raw/mbti_1.csv]\n    B --&gt;|Preprocessing| C[data/processed/processed_mbti.csv]\n    C --&gt;|DVC| D[GCS Bucket]\n    C --&gt;|DataModule| E[Train/Val/Test Splits]\n    E --&gt;|DataLoader| F[Batches]\n    F --&gt;|Model| G[Training]\n</code></pre>"},{"location":"architecture/data-pipeline/#pipeline-stages","title":"Pipeline Stages","text":""},{"location":"architecture/data-pipeline/#1-data-download","title":"1. Data Download","text":"<p>Data is automatically downloaded from Kaggle if it doesn't exist:</p> <pre><code>from src.mbti_classifier.data import download_mbti_data\n\n# Downloads to data/raw/mbti_1.csv\ndownload_mbti_data()\n</code></pre> <p>Original Dataset: - Source: Kaggle MBTI Dataset - Format: CSV with <code>type</code> and <code>posts</code> columns - Size: ~8,600 users - Content: <code>posts</code> contains 50 posts separated by <code>|||</code></p>"},{"location":"architecture/data-pipeline/#2-preprocessing","title":"2. Preprocessing","text":"<p>The <code>preprocess_mbti_data()</code> function performs the following transformations:</p> <pre><code>def preprocess_mbti_data(\n    raw_data_path: str = \"data/raw/mbti_1.csv\",\n    processed_data_path: str = \"data/processed/processed_mbti.csv\"\n) -&gt; pd.DataFrame:\n    # 1. Read data\n    # 2. Split MBTI into binary dimensions\n    # 3. Split posts (50 posts \u2192 1 per row)\n    # 4. Clean text\n    # 5. Save result\n</code></pre>"},{"location":"architecture/data-pipeline/#21-mbti-dimension-separation","title":"2.1 MBTI Dimension Separation","text":"<p>The 4-letter MBTI type is converted into 4 binary labels:</p> MBTI Type E/I S/N T/F J/P INTJ 0 0 1 0 ENFP 1 0 0 1 ISTP 0 1 1 1 <pre><code>df['E_I'] = df['type'].str[0].map({'E': 1, 'I': 0})\ndf['S_N'] = df['type'].str[1].map({'S': 1, 'N': 0})\ndf['T_F'] = df['type'].str[2].map({'T': 1, 'F': 0})\ndf['J_P'] = df['type'].str[3].map({'J': 1, 'P': 0})\n</code></pre>"},{"location":"architecture/data-pipeline/#22-post-separation","title":"2.2 Post Separation","text":"<p>Each user has 50 concatenated posts. They are separated to have 1 post per row:</p> <pre><code># Before: 1 row with 50 posts\n\"Hello world|||How are you?|||Great day!\"\n\n# After: 3 rows (1 post each)\n\"Hello world\"\n\"How are you?\"\n\"Great day!\"\n</code></pre> <p>This increases the dataset from ~8,600 rows to ~430,000 rows.</p>"},{"location":"architecture/data-pipeline/#23-text-cleaning","title":"2.3 Text Cleaning","text":"<p>The following transformations are applied:</p> <pre><code>def clean_text(text: str) -&gt; str:\n    # 1. Convert to lowercase\n    text = text.lower()\n\n    # 2. Remove URLs\n    text = re.sub(r'http\\S+|www\\S+', '', text)\n\n    # 3. Remove @username mentions\n    text = re.sub(r'@\\w+', '', text)\n\n    # 4. Remove emojis and special characters\n    text = re.sub(r'[^\\w\\s]', ' ', text)\n\n    # 5. Normalize spaces\n    text = re.sub(r'\\s+', ' ', text).strip()\n\n    return text\n</code></pre>"},{"location":"architecture/data-pipeline/#3-storage-with-dvc","title":"3. Storage with DVC","text":"<p>Preprocessed data is versioned with DVC:</p> <pre><code># Add to DVC\nuv run dvc add data/processed\n\n# Upload to GCS\nuv run dvc push\n</code></pre> <p>This creates <code>data/processed.dvc</code> with the file hash:</p> <pre><code>outs:\n- md5: a1b2c3d4e5f6...\n  size: 45678901\n  hash: md5\n  path: processed\n</code></pre>"},{"location":"architecture/data-pipeline/#4-data-loading-datamodule","title":"4. Data Loading (DataModule)","text":"<p>The <code>MBTIDataModule</code> manages data loading and splitting:</p> <pre><code>class MBTIDataModule(pl.LightningDataModule):\n    def prepare_data(self):\n        # Download and preprocess if it doesn't exist\n        if not os.path.exists(self.processed_data_path):\n            download_mbti_data(self.raw_data_path)\n            preprocess_mbti_data(self.raw_data_path, self.processed_data_path)\n\n    def setup(self, stage: str):\n        # Load preprocessed data\n        df = pd.read_csv(self.processed_data_path)\n\n        # Split train/val/test (80/10/10)\n        train_df, temp_df = train_test_split(df, test_size=0.2)\n        val_df, test_df = train_test_split(temp_df, test_size=0.5)\n\n        # Create datasets\n        self.train_dataset = MBTIDataset(train_df, self.tokenizer, self.max_length)\n        self.val_dataset = MBTIDataset(val_df, self.tokenizer, self.max_length)\n        self.test_dataset = MBTIDataset(test_df, self.tokenizer, self.max_length)\n</code></pre>"},{"location":"architecture/data-pipeline/#5-dataset-and-tokenization","title":"5. Dataset and Tokenization","text":"<p>The <code>MBTIDataset</code> implements window sampling:</p> <pre><code>class MBTIDataset(Dataset):\n    def __getitem__(self, idx: int):\n        # Get post\n        text = self.data.iloc[idx]['posts']\n\n        # Tokenize WITHOUT truncation\n        tokens = self.tokenizer(text, truncation=False, add_special_tokens=False)\n\n        # If too long, take random window\n        if len(tokens['input_ids']) &gt; self.max_length - 2:\n            start_idx = random.randint(0, len(tokens['input_ids']) - (self.max_length - 2))\n            tokens['input_ids'] = tokens['input_ids'][start_idx:start_idx + self.max_length - 2]\n            tokens['attention_mask'] = tokens['attention_mask'][start_idx:start_idx + self.max_length - 2]\n\n        # Add [CLS] and [SEP]\n        input_ids = [self.tokenizer.cls_token_id] + tokens['input_ids'] + [self.tokenizer.sep_token_id]\n        attention_mask = [1] + tokens['attention_mask'] + [1]\n\n        # Padding if necessary\n        if len(input_ids) &lt; self.max_length:\n            padding_length = self.max_length - len(input_ids)\n            input_ids += [self.tokenizer.pad_token_id] * padding_length\n            attention_mask += [0] * padding_length\n\n        return {\n            'input_ids': torch.tensor(input_ids),\n            'attention_mask': torch.tensor(attention_mask),\n            'labels': torch.tensor([\n                self.data.iloc[idx]['E_I'],\n                self.data.iloc[idx]['S_N'],\n                self.data.iloc[idx]['T_F'],\n                self.data.iloc[idx]['J_P']\n            ])\n        }\n</code></pre> <p>Advantages of Random Sampling: - Avoids losing information from long posts - Increases training variability - Improves generalization</p>"},{"location":"architecture/data-pipeline/#6-dataloaders","title":"6. DataLoaders","text":"<p>The <code>DataModule</code> creates the dataloaders:</p> <pre><code>def train_dataloader(self):\n    return DataLoader(\n        self.train_dataset,\n        batch_size=self.batch_size,\n        shuffle=True,  # Important for training\n        num_workers=self.num_workers,\n        pin_memory=True  # Speeds up GPU transfer\n    )\n\ndef val_dataloader(self):\n    return DataLoader(\n        self.val_dataset,\n        batch_size=self.batch_size,\n        shuffle=False,  # Not needed for validation\n        num_workers=self.num_workers,\n        pin_memory=True\n    )\n</code></pre>"},{"location":"architecture/data-pipeline/#configuration","title":"Configuration","text":"<p>Pipeline configuration is found in <code>configs/data/default.yaml</code>:</p> <pre><code>_target_: src.mbti_classifier.data.MBTIDataModule\nraw_data_path: data/raw/mbti_1.csv\nprocessed_data_path: data/processed/processed_mbti.csv\nbatch_size: 16\nmax_length: 512\nnum_workers: 4\n</code></pre>"},{"location":"architecture/data-pipeline/#cache-and-performance","title":"Cache and Performance","text":""},{"location":"architecture/data-pipeline/#cache-system","title":"Cache System","text":"<p>The pipeline implements caching at two levels:</p> <ol> <li> <p>Preprocessing Cache: <pre><code>if not os.path.exists(processed_data_path):\n    # Only preprocess if it doesn't exist\n    preprocess_mbti_data(raw_data_path, processed_data_path)\n</code></pre></p> </li> <li> <p>DVC Cache: <pre><code># First download from GCS\nuv run dvc pull  # Slow (~800MB)\n\n# Subsequent uses\nuv run dvc pull  # Instant if no changes\n</code></pre></p> </li> </ol>"},{"location":"architecture/data-pipeline/#optimizations","title":"Optimizations","text":"<ul> <li>num_workers: Parallelizes data loading (4 workers by default)</li> <li>pin_memory: Speeds up CPU \u2192 GPU transfer</li> <li>shuffle: Only in train, saves time in val/test</li> <li>Random window: Avoids recomputing the entire sequence</li> </ul>"},{"location":"architecture/data-pipeline/#complete-example-flow","title":"Complete Example Flow","text":"<pre><code># 1. Preparation (one time only)\npython -c \"from src.mbti_classifier.data import download_mbti_data, preprocess_mbti_data; download_mbti_data(); preprocess_mbti_data()\"\n\n# 2. Version with DVC\nuv run dvc add data/raw data/processed\nuv run dvc push\n\n# 3. In another environment\nuv run dvc pull  # Download preprocessed data\n\n# 4. Train\nuv run python src/mbti_classifier/train.py\n\n# The DataModule handles:\n# - Verifying data exists\n# - Downloading if it doesn't exist\n# - Preprocessing if not already processed\n# - Creating train/val/test splits\n# - Creating dataloaders\n# - Tokenization and batching\n</code></pre>"},{"location":"architecture/data-pipeline/#monitoring","title":"Monitoring","text":""},{"location":"architecture/data-pipeline/#dataset-statistics","title":"Dataset Statistics","text":"<pre><code>import pandas as pd\n\ndf = pd.read_csv('data/processed/processed_mbti.csv')\n\nprint(f\"Total posts: {len(df)}\")\nprint(f\"Distribution by dimension:\")\nprint(f\"  E/I: {df['E_I'].mean():.2%} extroverts\")\nprint(f\"  S/N: {df['S_N'].mean():.2%} sensing\")\nprint(f\"  T/F: {df['T_F'].mean():.2%} thinking\")\nprint(f\"  J/P: {df['J_P'].mean():.2%} judging\")\nprint(f\"Average length: {df['posts'].str.len().mean():.0f} chars\")\n</code></pre>"},{"location":"architecture/data-pipeline/#data-validation","title":"Data Validation","text":"<p>The pipeline includes automatic validations:</p> <pre><code># In MBTIDataModule.setup()\nassert not df.isnull().any().any(), \"Data contains NaN\"\nassert all(df[col].isin([0, 1]) for col in ['E_I', 'S_N', 'T_F', 'J_P']), \"Labels out of range\"\nassert len(df) &gt; 0, \"Empty dataset\"\n</code></pre>"},{"location":"architecture/data-pipeline/#troubleshooting","title":"Troubleshooting","text":""},{"location":"architecture/data-pipeline/#problem-data-doesnt-download","title":"Problem: Data doesn't download","text":"<p>Solution: <pre><code># Download manually from Kaggle\nkaggle datasets download -d datasnaek/mbti-type\nunzip mbti-type.zip -d data/raw/\n\n# Or use DVC\nuv run dvc pull\n</code></pre></p>"},{"location":"architecture/data-pipeline/#problem-slow-preprocessing","title":"Problem: Slow preprocessing","text":"<p>Solution: <pre><code># Use fewer posts per user\ndf_sample = df.groupby('type').sample(n=10)  # 10 posts instead of 50\n</code></pre></p>"},{"location":"architecture/data-pipeline/#problem-out-of-memory-in-dataloader","title":"Problem: Out of memory in DataLoader","text":"<p>Solution: <pre><code># In configs/data/default.yaml\nbatch_size: 8  # Reduce from 16\nnum_workers: 2  # Reduce from 4\n</code></pre></p>"},{"location":"architecture/data-pipeline/#references","title":"References","text":"<ul> <li>Hydra Configuration</li> <li>Data Configuration</li> <li>DVC Workflow</li> </ul>"},{"location":"architecture/model/","title":"Model Architecture","text":"<p>This page describes the architecture of the MBTI classification model based on DistilBERT.</p>"},{"location":"architecture/model/#overview","title":"Overview","text":"<p>The model uses a multi-task binary classification approach instead of 16-class classification:</p> <pre><code>graph TD\n    A[Input Text] --&gt;|Tokenizer| B[DistilBERT]\n    B --&gt;|Hidden States| C[Pooling CLS token]\n    C --&gt; D[Dropout 0.1]\n    D --&gt; E1[Linear E/I]\n    D --&gt; E2[Linear S/N]\n    D --&gt; E3[Linear T/F]\n    D --&gt; E4[Linear J/P]\n    E1 --&gt; F1[Sigmoid]\n    E2 --&gt; F2[Sigmoid]\n    E3 --&gt; F3[Sigmoid]\n    E4 --&gt; F4[Sigmoid]\n    F1 --&gt; G[MBTI Prediction]\n    F2 --&gt; G\n    F3 --&gt; G\n    F4 --&gt; G\n</code></pre>"},{"location":"architecture/model/#why-multi-task","title":"Why Multi-Task?","text":""},{"location":"architecture/model/#issue-with-16-classes","title":"Issue with 16 Classes","text":"<p>Direct classification of 16 MBTI types has issues:</p> <pre><code># 16-class Classification\n# Problem 1: Extreme imbalance\nINTJ: 2000 samples\nESFP: 50 samples  # 40x less!\n\n# Problem 2: Incorrect predictions are equally bad\nPrediction: INTJ\nActual:     INTP\n# Only 1 dimension wrong, but loss is the same as if all were wrong\n</code></pre>"},{"location":"architecture/model/#solution-4-binary-classifiers","title":"Solution: 4 Binary Classifiers","text":"<pre><code># Multi-task binary classification\n# Advantage 1: More uniform balance\nE/I: 45% vs 55%\nS/N: 25% vs 75%\nT/F: 53% vs 47%\nJ/P: 48% vs 52%\n\n# Advantage 2: Loss proportional to error\nPrediction: I=1, N=1, T=0, J=1  \u2192 INFJ\nActual:     I=1, N=1, T=1, P=1  \u2192 INTP\n# Loss only penalizes the 2 incorrect dimensions (T/F and J/P)\n</code></pre>"},{"location":"architecture/model/#model-components","title":"Model Components","text":""},{"location":"architecture/model/#1-encoder-distilbert","title":"1. Encoder: DistilBERT","text":"<p>DistilBERT is a distilled version of BERT:</p> Feature BERT-base DistilBERT Parameters 110M 66M Layers 12 6 Speed 1x 2x Memory 1x 0.6x Performance 100% 97% <pre><code>from transformers import DistilBertModel\n\nencoder = DistilBertModel.from_pretrained('distilbert-base-uncased')\n# 6 transformer layers\n# 768 hidden dimensions\n# 12 attention heads per layer\n</code></pre> <p>Advantages: - \u2705 Faster than BERT - \u2705 Less memory (fits on small GPUs) - \u2705 Retains 97% of BERT's performance - \u2705 Trained on 60GB of English text</p>"},{"location":"architecture/model/#2-pooling-cls-token","title":"2. Pooling: [CLS] Token","text":"<p>The special [CLS] token is used as the sequence representation:</p> <pre><code># Input: [CLS] Hello world [SEP] [PAD] [PAD]\n# Tokens:  0     1     2      3     4     5\n\n# DistilBERT output\noutputs = encoder(input_ids, attention_mask)\n# Shape: (batch_size, seq_len, 768)\n\n# Extract [CLS] token (first position)\ncls_output = outputs.last_hidden_state[:, 0, :]\n# Shape: (batch_size, 768)\n</code></pre> <p>Why [CLS] and not mean pooling?</p> <pre><code># [CLS] Pooling (used in this project)\ncls = outputs.last_hidden_state[:, 0, :]  # Only first position\n\n# Mean Pooling (alternative)\nmean = (outputs.last_hidden_state * attention_mask.unsqueeze(-1)).sum(1)\nmean = mean / attention_mask.sum(1).unsqueeze(-1)\n</code></pre> <p>Advantages of [CLS]: - Specifically trained to represent the entire sequence - Faster (no averaging required) - Standard in BERT-based classification</p>"},{"location":"architecture/model/#3-dropout-regularization","title":"3. Dropout: Regularization","text":"<pre><code>dropout = nn.Dropout(p=0.1)\n# With probability 0.1, sets each neuron to 0\n# Scales the others by 1/(1-0.1) = 1.11\n</code></pre> <p>Effect: <pre><code># Without dropout (overfitting)\nx = [1.0, 2.0, 3.0, 4.0]\n\n# With dropout (training)\nx = [0.0, 2.22, 3.33, 0.0]  # 2 neurons turned off randomly\n\n# During inference: dropout is disabled\nx = [1.0, 2.0, 3.0, 4.0]\n</code></pre></p> <p>Prevents: - Overfitting - Co-adaptation of neurons - Improves generalization</p>"},{"location":"architecture/model/#4-classification-heads-4-binaries","title":"4. Classification Heads: 4 Binaries","text":"<p>Each MBTI dimension has its own classification layer:</p> <pre><code>self.classifiers = nn.ModuleList([\n    nn.Linear(768, 1),  # E/I\n    nn.Linear(768, 1),  # S/N\n    nn.Linear(768, 1),  # T/F\n    nn.Linear(768, 1)   # J/P\n])\n</code></pre> <p>Forward pass:</p> <pre><code>def forward(self, input_ids, attention_mask):\n    # 1. Encoder\n    outputs = self.encoder(input_ids, attention_mask)\n\n    # 2. Pooling\n    cls_output = outputs.last_hidden_state[:, 0, :]\n\n    # 3. Dropout\n    cls_output = self.dropout(cls_output)\n\n    # 4. Classification (4 independent classifiers)\n    logits = [classifier(cls_output) for classifier in self.classifiers]\n    logits = torch.cat(logits, dim=1)  # Shape: (batch, 4)\n\n    # 5. Sigmoid\n    probs = torch.sigmoid(logits)\n\n    return probs\n</code></pre>"},{"location":"architecture/model/#training-details","title":"Training Details","text":""},{"location":"architecture/model/#loss-function-binary-cross-entropy","title":"Loss Function: Binary Cross-Entropy","text":"<pre><code>criterion = nn.BCEWithLogitsLoss()\n\n# For each dimension\nfor i in range(4):\n    loss_i = -[y_i * log(p_i) + (1-y_i) * log(1-p_i)]\n\n# Total loss = average of the 4 dimensions\ntotal_loss = mean(loss_0, loss_1, loss_2, loss_3)\n</code></pre> <p>Example:</p> <pre><code># Prediction: [0.8, 0.6, 0.4, 0.7]  (E, N, F, J)\n# Actual:     [1,   1,   0,   1  ]  (E, N, F, J)\n\n# Loss per dimension:\n# E/I: -(1*log(0.8) + 0*log(0.2)) = 0.22\n# S/N: -(1*log(0.6) + 0*log(0.4)) = 0.51\n# T/F: -(0*log(0.4) + 1*log(0.6)) = 0.51\n# J/P: -(1*log(0.7) + 0*log(0.3)) = 0.36\n\n# Total: (0.22 + 0.51 + 0.51 + 0.36) / 4 = 0.40\n</code></pre>"},{"location":"architecture/model/#optimizer-adamw","title":"Optimizer: AdamW","text":"<pre><code>optimizer = torch.optim.AdamW(\n    model.parameters(),\n    lr=2e-5,  # Small learning rate for fine-tuning\n    weight_decay=0.01  # L2 regularization\n)\n</code></pre> <p>Important Parameters: - lr=2e-5: Lower than training from scratch (typically 1e-3) - weight_decay: Prevents very large weights</p>"},{"location":"architecture/model/#learning-rate-scheduler","title":"Learning Rate Scheduler","text":"<pre><code>from transformers import get_linear_schedule_with_warmup\n\nscheduler = get_linear_schedule_with_warmup(\n    optimizer,\n    num_warmup_steps=500,  # Warmup for 500 steps\n    num_training_steps=total_steps\n)\n</code></pre> <p>Effect:</p> <pre><code>Learning Rate\n    |\n2e-5|     ________\n    |    /         \\\n    |   /           \\___\n    |  /                \\___\n 0  |_____________________\\___\n    0   500            total_steps\n       warmup\n</code></pre> <p>Advantages: - Warmup avoids large gradients at the start - Decay improves convergence at the end</p>"},{"location":"architecture/model/#metrics","title":"Metrics","text":"<p>4 accuracies (one per dimension) + total accuracy are calculated:</p> <pre><code>def compute_metrics(predictions, labels):\n    # Binary predictions\n    preds = (predictions &gt; 0.5).float()\n\n    # Accuracy per dimension\n    acc_ei = (preds[:, 0] == labels[:, 0]).float().mean()\n    acc_sn = (preds[:, 1] == labels[:, 1]).float().mean()\n    acc_tf = (preds[:, 2] == labels[:, 2]).float().mean()\n    acc_jp = (preds[:, 3] == labels[:, 3]).float().mean()\n\n    # Total accuracy (all dimensions correct)\n    acc_total = (preds == labels).all(dim=1).float().mean()\n\n    return {\n        'acc_ei': acc_ei,\n        'acc_sn': acc_sn,\n        'acc_tf': acc_tf,\n        'acc_jp': acc_jp,\n        'acc_total': acc_total\n    }\n</code></pre> <p>Example:</p> <pre><code># Predictions: [[1, 1, 0, 1],   # ENFJ predicted\n#               [0, 1, 1, 0]]   # INTP predicted\n\n# Labels:      [[1, 1, 0, 1],   # ENFJ actual \u2713\n#               [0, 1, 1, 1]]   # INTJ actual (J/P incorrect)\n\n# Results:\nacc_ei = 2/2 = 1.00  # Both correct\nacc_sn = 2/2 = 1.00\nacc_tf = 2/2 = 1.00\nacc_jp = 1/2 = 0.50  # Second one incorrect\nacc_total = 1/2 = 0.50  # Only first one completely correct\n</code></pre>"},{"location":"architecture/model/#model-variants","title":"Model Variants","text":""},{"location":"architecture/model/#1-default-model","title":"1. Default Model","text":"<pre><code># configs/model/default.yaml\n_target_: src.mbti_classifier.model.MBTIModel\nmodel_name: distilbert-base-uncased\nlearning_rate: 2e-5\ndropout_rate: 0.1\nweight_decay: 0.01\n</code></pre> <p>Usage: <pre><code>uv run python src/mbti_classifier/train.py\n</code></pre></p>"},{"location":"architecture/model/#2-quick-model-for-testing","title":"2. Quick Model (for testing)","text":"<pre><code># configs/model/quick.yaml\n_target_: src.mbti_classifier.model.MBTIModel\nmodel_name: distilbert-base-uncased\nlearning_rate: 5e-5  # Higher for faster convergence\ndropout_rate: 0.1\nweight_decay: 0.01\n</code></pre> <p>Usage: <pre><code>uv run python src/mbti_classifier/train.py model=quick trainer=quick\n# Trains quickly (1 epoch) to validate pipeline\n</code></pre></p>"},{"location":"architecture/model/#inference","title":"Inference","text":""},{"location":"architecture/model/#simple-prediction","title":"Simple Prediction","text":"<pre><code>from src.mbti_classifier.model import MBTIClassifier\n\n# Load model\nclassifier = MBTIClassifier.load_from_checkpoint('models/best.ckpt')\n\n# Predict\nmbti_type = classifier.predict(\"I love analyzing complex problems...\")\nprint(mbti_type)  # Output: \"INTJ\"\n</code></pre>"},{"location":"architecture/model/#batch-prediction","title":"Batch Prediction","text":"<pre><code>texts = [\n    \"I love meeting new people!\",\n    \"I prefer staying home and reading\",\n    \"Logic above all else\"\n]\n\npredictions = [classifier.predict(text) for text in texts]\n# Output: [\"ENFP\", \"INFP\", \"INTP\"]\n</code></pre>"},{"location":"architecture/model/#with-probabilities","title":"With Probabilities","text":"<pre><code>probs = classifier.predict_proba(text)\n# Output: tensor([0.85, 0.62, 0.78, 0.55])\n#         (E=0.85, N=0.62, T=0.78, J=0.55)\n\n# Convert to types\nmbti = \"\"\nmbti += \"E\" if probs[0] &gt; 0.5 else \"I\"\nmbti += \"N\" if probs[1] &gt; 0.5 else \"S\"\nmbti += \"T\" if probs[2] &gt; 0.5 else \"F\"\nmbti += \"J\" if probs[3] &gt; 0.5 else \"P\"\n# Output: \"ENTJ\"\n</code></pre>"},{"location":"architecture/model/#advanced-topics","title":"Advanced Topics","text":""},{"location":"architecture/model/#fine-tuning-strategy","title":"Fine-Tuning Strategy","text":"<p>The model uses fine-tuning of pre-trained DistilBERT:</p> <pre><code># Option 1: Fine-tune all layers (currently used)\nfor param in model.encoder.parameters():\n    param.requires_grad = True\n\n# Option 2: Freeze encoder, train classifiers only\nfor param in model.encoder.parameters():\n    param.requires_grad = False\nfor classifier in model.classifiers:\n    for param in classifier.parameters():\n        param.requires_grad = True\n\n# Option 3: Gradual unfreezing\n# Epoch 1: Classifiers only\n# Epoch 2: Last DistilBERT layer + classifiers\n# Epoch 3: All layers\n</code></pre>"},{"location":"architecture/model/#gradient-accumulation","title":"Gradient Accumulation","text":"<p>To train with a larger effective batch size:</p> <pre><code># configs/trainer/default.yaml\naccumulate_grad_batches: 4  # Effective batch = 16 * 4 = 64\n</code></pre> <pre><code># Equivalent to:\nfor i, batch in enumerate(dataloader):\n    loss = model(batch)\n    loss = loss / 4  # Scale loss\n    loss.backward()\n\n    if (i + 1) % 4 == 0:\n        optimizer.step()\n        optimizer.zero_grad()\n</code></pre>"},{"location":"architecture/model/#mixed-precision-training","title":"Mixed Precision Training","text":"<p>Reduces memory and speeds up training:</p> <pre><code># configs/trainer/default.yaml\nprecision: 16-mixed  # Uses float16 for calculations, float32 for weights\n</code></pre> <p>Advantages: - 50% less memory - 2-3x faster on modern GPUs - No loss of precision thanks to loss scaling</p>"},{"location":"architecture/model/#model-size-and-performance","title":"Model Size and Performance","text":"Component Parameters Memory DistilBERT 66M 250 MB 4x Linear 3072 12 KB Total 66M 250 MB <p>Training: - GPU Memory: ~4 GB (batch_size=16) - Training Time: ~2h on V100 (10 epochs) - Inference: ~50ms per text on CPU</p>"},{"location":"architecture/model/#troubleshooting","title":"Troubleshooting","text":""},{"location":"architecture/model/#problem-overfitting","title":"Problem: Overfitting","text":"<p>Symptoms: <pre><code>Train Acc: 0.95\nVal Acc: 0.65  # Large gap\n</code></pre></p> <p>Solutions: <pre><code># 1. Increase dropout\ndropout_rate: 0.3  # From 0.1\n\n# 2. Increase weight decay\nweight_decay: 0.05  # From 0.01\n\n# 3. Reduce learning rate\nlearning_rate: 1e-5  # From 2e-5\n\n# 4. More aggressive early stopping\npatience: 3  # From 5\n</code></pre></p>"},{"location":"architecture/model/#problem-underfitting","title":"Problem: Underfitting","text":"<p>Symptoms: <pre><code>Train Acc: 0.60\nVal Acc: 0.58  # Both low\n</code></pre></p> <p>Solutions: <pre><code># 1. Increase learning rate\nlearning_rate: 5e-5  # From 2e-5\n\n# 2. More epochs\nmax_epochs: 20  # From 10\n\n# 3. Reduce dropout\ndropout_rate: 0.05  # From 0.1\n</code></pre></p>"},{"location":"architecture/model/#problem-one-dimension-not-converging","title":"Problem: One dimension not converging","text":"<p>Example: <pre><code>acc_ei: 0.85\nacc_sn: 0.75  # This dimension is stuck\nacc_tf: 0.80\nacc_jp: 0.82\n</code></pre></p> <p>Possible causes: - Data imbalance (e.g., 90% N, 10% S) - Weak signal in texts for that dimension</p> <p>Solutions: <pre><code># 1. Class weights to balance loss\npos_weight = torch.tensor([1.0, 3.0, 1.0, 1.0])  # More weight to S/N\ncriterion = nn.BCEWithLogitsLoss(pos_weight=pos_weight)\n\n# 2. Focal loss (focuses on hard examples)\ndef focal_loss(pred, target, gamma=2.0):\n    bce = nn.BCEWithLogitsLoss(reduction='none')(pred, target)\n    pt = torch.exp(-bce)\n    return ((1 - pt) ** gamma * bce).mean()\n</code></pre></p>"},{"location":"architecture/model/#references","title":"References","text":"<ul> <li>DistilBERT Paper</li> <li>BERT Paper</li> <li>Training Configuration</li> <li>Data Pipeline</li> </ul>"},{"location":"architecture/overview/","title":"Architecture Overview","text":"<p>This page provides a high-level overview of the MBTI Personality Classifier system architecture.</p>"},{"location":"architecture/overview/#system-components","title":"System Components","text":"<p>The system consists of three main components:</p> <pre><code>graph LR\n    A[Data Pipeline] --&gt; B[Model Training]\n    B --&gt; C[Model Checkpoint]\n    C --&gt; D[API Server]\n    D --&gt; E[Streamlit UI]\n    F[User] --&gt; E\n    E --&gt; D\n</code></pre>"},{"location":"architecture/overview/#1-data-pipeline","title":"1. Data Pipeline","text":"<p>Location: <code>src/mbti_classifier/data.py</code></p> <p>Handles all data-related operations:</p> <ul> <li>Download: Fetches MBTI dataset from Kaggle via <code>mlcroissant</code></li> <li>Preprocessing: Cleans text, creates binary labels for 4 dimensions</li> <li>Tokenization: Converts text to DistilBERT-compatible tokens</li> <li>Augmentation: Random window sampling for sequences &gt;512 tokens</li> <li>Caching: Saves processed data to avoid re-processing</li> </ul> <p>Key Classes: - <code>MBTIDataset</code>: PyTorch Dataset with tokenization - <code>MBTIDataModule</code>: Lightning DataModule managing the full pipeline</p>"},{"location":"architecture/overview/#2-model-training","title":"2. Model Training","text":"<p>Location: <code>src/mbti_classifier/model.py</code>, <code>src/mbti_classifier/train.py</code></p> <p>Multi-task binary classification system:</p> <p>Architecture: <pre><code>DistilBERT Encoder (67M params)\n        \u2193\n[CLS] Representation (768-dim)\n        \u2193\nShared Dense Layer (768 \u2192 768)\n        \u2193\n    \u250c\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2510\n    \u2193       \u2193   \u2193   \u2193\n  E/I     S/N T/F J/P\n  Head    Head Head Head\n    \u2193       \u2193   \u2193   \u2193\n Logit   Logit Logit Logit\n</code></pre></p> <p>Key Features: - 4 independent binary classifiers - Shared representation layer - BCE loss with optional pos_weight for class imbalance - AdamW optimizer with linear warmup</p>"},{"location":"architecture/overview/#3-api-server","title":"3. API Server","text":"<p>Location: <code>src/mbti_classifier/api.py</code></p> <p>FastAPI server providing RESTful inference:</p> <p>Endpoints: - <code>GET /</code>: Service information - <code>GET /health</code>: Health check (used by Docker) - <code>POST /predict</code>: MBTI prediction from text</p> <p>Features: - Automatic model loading on startup - Input validation with Pydantic - CORS enabled for Streamlit UI - Comprehensive error handling</p>"},{"location":"architecture/overview/#4-streamlit-ui","title":"4. Streamlit UI","text":"<p>Location: <code>src/mbti_classifier/ui.py</code></p> <p>Interactive web interface:</p> <p>Features: - Text input for prediction - Real-time API calls - Radar plot visualization of dimensions - Probability scores display - Responsive design</p>"},{"location":"architecture/overview/#data-flow","title":"Data Flow","text":""},{"location":"architecture/overview/#training-pipeline","title":"Training Pipeline","text":"<pre><code>1. Raw Data (data/raw/mbti_1.csv)\n   \u2193\n2. Preprocessing (clean text, binary labels)\n   \u2193\n3. Cached Data (data/processed/processed_mbti.csv)\n   \u2193\n4. Train/Val/Test Split (stratified)\n   \u2193\n5. Tokenization (DistilBERT tokenizer)\n   \u2193\n6. DataLoaders (batch_size=16, random windows)\n   \u2193\n7. Model Training (Lightning Trainer)\n   \u2193\n8. Checkpoint (models/best.ckpt)\n</code></pre>"},{"location":"architecture/overview/#inference-pipeline","title":"Inference Pipeline","text":"<pre><code>1. User Text Input\n   \u2193\n2. Text Cleaning (remove URLs, normalize)\n   \u2193\n3. Tokenization (max_length=512)\n   \u2193\n4. Model Forward Pass\n   \u2193\n5. Sigmoid Activation (4 probabilities)\n   \u2193\n6. Threshold (&gt;0.5 = 1, else 0)\n   \u2193\n7. MBTI Type Construction (e.g., \"INTJ\")\n   \u2193\n8. Response (JSON with type + probabilities)\n</code></pre>"},{"location":"architecture/overview/#configuration-management","title":"Configuration Management","text":"<p>Uses Hydra for composable configs:</p> <pre><code>configs/\n\u251c\u2500\u2500 train.yaml              # Main config\n\u251c\u2500\u2500 train_quick.yaml        # Fast testing\n\u251c\u2500\u2500 train_cpu.yaml          # CPU-only\n\u251c\u2500\u2500 train_production.yaml   # Extended training\n\u251c\u2500\u2500 data/default.yaml       # DataModule settings\n\u251c\u2500\u2500 model/default.yaml      # Model hyperparameters\n\u251c\u2500\u2500 trainer/default.yaml    # Trainer options\n\u251c\u2500\u2500 callbacks/default.yaml  # Checkpoints, early stopping\n\u2514\u2500\u2500 logger/wandb.yaml       # Logging configuration\n</code></pre> <p>Override pattern: <pre><code>python train.py model.learning_rate=5e-5 data.batch_size=32\n</code></pre></p>"},{"location":"architecture/overview/#docker-architecture","title":"Docker Architecture","text":"<p>Three separate containers orchestrated with Docker Compose:</p>"},{"location":"architecture/overview/#training-container","title":"Training Container","text":"<p>Image: <code>mbti-train:latest</code></p> <ul> <li>Purpose: One-time model training</li> <li>Base: <code>python3.12-bookworm-slim</code></li> <li>Volumes: <code>./models</code>, <code>./data</code>, <code>./configs</code></li> <li>Entrypoint: <code>train.py</code></li> </ul>"},{"location":"architecture/overview/#api-container","title":"API Container","text":"<p>Image: <code>mbti-api:latest</code></p> <ul> <li>Purpose: Inference server</li> <li>Base: <code>python3.12-bookworm-slim</code></li> <li>Port: 8000</li> <li>Volumes: <code>./models</code> (read-only)</li> <li>Health Check: <code>curl http://localhost:8000/health</code></li> <li>Dependencies: Build tools (g++) for PyTorch</li> </ul>"},{"location":"architecture/overview/#ui-container","title":"UI Container","text":"<p>Image: <code>mbti-ui:latest</code></p> <ul> <li>Purpose: User interface</li> <li>Base: <code>python3.12-bookworm-slim</code></li> <li>Port: 8501</li> <li>Environment: <code>API_URL=http://api:8000</code></li> <li>Depends On: API (healthy)</li> </ul> <p>Inter-container Communication: - UI \u2192 API via internal Docker network - Shared volumes for model persistence - Health checks ensure startup order</p>"},{"location":"architecture/overview/#data-versioning-dvc","title":"Data Versioning (DVC)","text":"<pre><code>Git Repository\n\u251c\u2500\u2500 .dvc/config          # DVC configuration\n\u251c\u2500\u2500 data/raw.dvc         # Metadata (109 bytes)\n\u251c\u2500\u2500 data/processed.dvc   # Metadata\n\u2514\u2500\u2500 models.dvc           # Metadata\n\nGoogle Cloud Storage (mlops70_bucket)\n\u2514\u2500\u2500 dvcstore/\n    \u2514\u2500\u2500 files/md5/\n        \u251c\u2500\u2500 34/...       # Raw data chunks\n        \u251c\u2500\u2500 f3/...       # Model checkpoint (803 MB)\n        \u2514\u2500\u2500 ...\n</code></pre> <p>Benefits: - Git tracks small metadata files - GCS stores large binary files - Content-addressable (deduplication) - Checksum verification for integrity</p>"},{"location":"architecture/overview/#scalability-considerations","title":"Scalability Considerations","text":""},{"location":"architecture/overview/#horizontal-scaling","title":"Horizontal Scaling","text":"<ul> <li>API: Can run multiple instances behind load balancer</li> <li>UI: Stateless, easily replicated</li> <li>Training: Distributed with PyTorch Lightning (multi-GPU)</li> </ul>"},{"location":"architecture/overview/#vertical-scaling","title":"Vertical Scaling","text":"<ul> <li>Training: Supports GPU acceleration (CUDA)</li> <li>API: Can enable mixed-precision inference</li> <li>Data: Streaming for datasets larger than RAM</li> </ul>"},{"location":"architecture/overview/#performance-optimization","title":"Performance Optimization","text":"<ul> <li>Model: DistilBERT (6x faster than BERT-base)</li> <li>Caching: Processed data cached to disk</li> <li>Batching: Configurable batch sizes</li> <li>Workers: Multi-process data loading</li> </ul>"},{"location":"architecture/overview/#security","title":"Security","text":"<ul> <li>API: Input validation with Pydantic</li> <li>Docker: Read-only model volumes in API</li> <li>Credentials: GCS auth via gcloud CLI (no keys in repo)</li> <li>CORS: Configurable allowed origins</li> </ul>"},{"location":"architecture/overview/#monitoring","title":"Monitoring","text":"<ul> <li>Training: WandB integration for experiment tracking</li> <li>API: Health check endpoint</li> <li>Logs: Structured logging with loguru</li> <li>Metrics: Per-dimension F1 scores, accuracy, loss</li> </ul>"},{"location":"architecture/overview/#next-steps","title":"Next Steps","text":"<ul> <li>Deep dive into Data Pipeline</li> <li>Understand Model Architecture</li> <li>Explore API &amp; UI</li> </ul>"},{"location":"configuration/data/","title":"API and UI Integration","text":"<p>This page describes how the API (FastAPI) and the UI (Streamlit) are integrated into the project.</p>"},{"location":"configuration/data/#architecture","title":"Architecture","text":"<pre><code>graph LR\n    A[User Browser] --&gt;|HTTP| B[Streamlit UI :8501]\n    B --&gt;|HTTP POST| C[FastAPI API :8000]\n    C --&gt;|Load Model| D[models/best.ckpt]\n    C --&gt;|Return Prediction| B\n    B --&gt;|Display Result| A\n</code></pre>"},{"location":"configuration/data/#fastapi-backend","title":"FastAPI Backend","text":""},{"location":"configuration/data/#components","title":"Components","text":""},{"location":"configuration/data/#1-model-loading","title":"1. Model Loading","text":"<pre><code># src/mbti_classifier/api.py\nfrom src.mbti_classifier.model import MBTIClassifier\n\n# Load model at startup\nclassifier = MBTIClassifier.load_from_checkpoint('models/best.ckpt')\n</code></pre> <p>Features: - Loads the model only once at startup - Keeps the model in memory for fast inference - Uses the best checkpoint saved during training</p>"},{"location":"configuration/data/#2-endpoints","title":"2. Endpoints","text":""},{"location":"configuration/data/#health-check","title":"Health Check","text":"<pre><code>@app.get(\"/health\")\ndef health():\n    \"\"\"Checks that the API is running.\"\"\"\n    return {\"status\": \"healthy\"}\n</code></pre> <p>Usage:</p> <pre><code>curl http://localhost:8000/health\n# {\"status\": \"healthy\"}\n</code></pre>"},{"location":"configuration/data/#single-prediction","title":"Single Prediction","text":"<pre><code>@app.post(\"/predict\")\ndef predict(request: PredictRequest):\n    \"\"\"Predicts the MBTI type of a text.\"\"\"\n    mbti_type = classifier.predict(request.text)\n    probs = classifier.predict_proba(request.text)\n\n    return {\n        \"mbti_type\": mbti_type,\n        \"probabilities\": {\n            \"E_I\": float(probs[0]),\n            \"S_N\": float(probs[1]),\n            \"T_F\": float(probs[2]),\n            \"J_P\": float(probs[3])\n        }\n    }\n</code></pre> <p>Request:</p> <pre><code>{\n  \"text\": \"I love analyzing complex problems and finding patterns.\"\n}\n</code></pre> <p>Response:</p> <pre><code>{\n  \"mbti_type\": \"INTP\",\n  \"probabilities\": {\n    \"E_I\": 0.15,\n    \"S_N\": 0.23,\n    \"T_F\": 0.82,\n    \"J_P\": 0.38\n  }\n}\n</code></pre>"},{"location":"configuration/data/#batch-prediction","title":"Batch Prediction","text":"<pre><code>@app.post(\"/predict_batch\")\ndef predict_batch(request: PredictBatchRequest):\n    \"\"\"Predicts MBTI types for multiple texts.\"\"\"\n    predictions = []\n    for text in request.texts:\n        mbti_type = classifier.predict(text)\n        probs = classifier.predict_proba(text)\n        predictions.append({\n            \"text\": text,\n            \"mbti_type\": mbti_type,\n            \"probabilities\": {...}\n        })\n    return {\"predictions\": predictions}\n</code></pre>"},{"location":"configuration/data/#pydantic-models","title":"Pydantic Models","text":"<pre><code>from pydantic import BaseModel, Field\n\nclass PredictRequest(BaseModel):\n    text: str = Field(..., min_length=1, description=\"Text to classify\")\n\nclass PredictBatchRequest(BaseModel):\n    texts: list[str] = Field(..., min_items=1, max_items=100)\n</code></pre> <p>Automatic validation: - <code>text</code> cannot be empty - <code>texts</code> must have 1-100 items - Returns HTTP 422 if validation fails</p>"},{"location":"configuration/data/#cors","title":"CORS","text":"<pre><code>from fastapi.middleware.cors import CORSMiddleware\n\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],  # Allow all origins\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n</code></pre> <p>Allows: - Requests from any domain - Necessary for Streamlit (port 8501) to access API (port 8000)</p>"},{"location":"configuration/data/#streamlit-frontend","title":"Streamlit Frontend","text":""},{"location":"configuration/data/#components_1","title":"Components","text":""},{"location":"configuration/data/#1-configuration","title":"1. Configuration","text":"<pre><code># src/mbti_classifier/ui.py\nimport streamlit as st\nimport os\n\n# API URL\nAPI_URL = os.getenv(\"API_URL\", \"http://localhost:8000\")\n\n# Configure page\nst.set_page_config(\n    page_title=\"MBTI Classifier\",\n    page_icon=\"\ud83e\udde0\",\n    layout=\"wide\"\n)\n</code></pre> <p>Environment Variables: - Local: <code>API_URL=http://localhost:8000</code> - Docker: <code>API_URL=http://api:8000</code> (uses service name)</p>"},{"location":"configuration/data/#2-ui-layout","title":"2. UI Layout","text":"<pre><code># Header\nst.title(\"\ud83e\udde0 MBTI Personality Classifier\")\nst.markdown(\"Predict your personality type based on text.\")\n\n# Input\ntext = st.text_area(\n    \"Enter text:\",\n    placeholder=\"Write something about yourself...\",\n    height=150\n)\n\n# Button\nif st.button(\"Classify\", type=\"primary\"):\n    # Call API\n    ...\n</code></pre>"},{"location":"configuration/data/#3-api-call","title":"3. API Call","text":"<pre><code>import requests\n\ndef predict(text: str) -&gt; dict:\n    \"\"\"Calls the API to predict.\"\"\"\n    try:\n        response = requests.post(\n            f\"{API_URL}/predict\",\n            json={\"text\": text},\n            timeout=10\n        )\n        response.raise_for_status()\n        return response.json()\n    except requests.exceptions.RequestException as e:\n        st.error(f\"Error connecting to the API: {e}\")\n        return None\n</code></pre>"},{"location":"configuration/data/#4-result-visualization","title":"4. Result Visualization","text":"<pre><code># Show MBTI type\nst.success(f\"Your personality type is: **{result['mbti_type']}**\")\n\n# Show probabilities\nprobs = result['probabilities']\n\ncol1, col2, col3, col4 = st.columns(4)\nwith col1:\n    st.metric(\"E/I\", f\"{probs['E_I']:.0%}\", \n              \"E\" if probs['E_I'] &gt; 0.5 else \"I\")\nwith col2:\n    st.metric(\"S/N\", f\"{probs['S_N']:.0%}\",\n              \"S\" if probs['S_N'] &gt; 0.5 else \"N\")\n# ...\n\n# Progress bars\nst.progress(probs['E_I'], text=\"Extraversion\")\nst.progress(probs['S_N'], text=\"Sensing\")\n# ...\n</code></pre>"},{"location":"configuration/data/#service-communication","title":"Service Communication","text":""},{"location":"configuration/data/#local-development","title":"Local Development","text":"<pre><code># Terminal 1: API\nuv run python src/mbti_classifier/api.py\n# Running on http://localhost:8000\n\n# Terminal 2: UI\nuv run python src/mbti_classifier/ui.py\n# Running on http://localhost:8501\n</code></pre> <p>Flow: 1. User opens <code>http://localhost:8501</code> 2. Streamlit runs in browser 3. User enters text and clicks \"Classify\" 4. Streamlit sends POST to <code>http://localhost:8000/predict</code> 5. FastAPI processes and returns result 6. Streamlit displays result</p>"},{"location":"configuration/data/#docker-production","title":"Docker (Production)","text":"<pre><code># docker-compose.yaml\nservices:\n  api:\n    build:\n      context: .\n      dockerfile: dockerfiles/api.dockerfile\n    ports:\n      - \"8000:8000\"\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8000/health\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n      start_period: 120s\n\n  ui:\n    build:\n      context: .\n      dockerfile: dockerfiles/ui.dockerfile\n    ports:\n      - \"8501:8501\"\n    environment:\n      - API_URL=http://api:8000  # Uses service name\n    depends_on:\n      api:\n        condition: service_healthy\n</code></pre> <p>Key differences: - Networking: Docker Compose creates an internal network - Service Discovery: UI accesses API via <code>http://api:8000</code> (service name) - Healthcheck: UI waits for API to be healthy before starting - Isolation: Each service in its own container</p>"},{"location":"configuration/data/#error-handling","title":"Error Handling","text":""},{"location":"configuration/data/#api","title":"API","text":"<pre><code>from fastapi import HTTPException\n\n@app.post(\"/predict\")\ndef predict(request: PredictRequest):\n    try:\n        mbti_type = classifier.predict(request.text)\n        return {\"mbti_type\": mbti_type, ...}\n    except Exception as e:\n        raise HTTPException(\n            status_code=500,\n            detail=f\"Prediction error: {str(e)}\"\n        )\n</code></pre>"},{"location":"configuration/data/#ui","title":"UI","text":"<pre><code>try:\n    response = requests.post(f\"{API_URL}/predict\", json={\"text\": text})\n    response.raise_for_status()\n    result = response.json()\nexcept requests.exceptions.ConnectionError:\n    st.error(\"\u274c Cannot connect to API. Is it running?\")\nexcept requests.exceptions.Timeout:\n    st.error(\"\u23f1\ufe0f The API took too long to respond.\")\nexcept requests.exceptions.HTTPError as e:\n    st.error(f\"\u274c HTTP Error: {e.response.status_code}\")\nexcept Exception as e:\n    st.error(f\"\u274c Unexpected error: {str(e)}\")\n</code></pre>"},{"location":"configuration/data/#testing","title":"Testing","text":""},{"location":"configuration/data/#api-tests","title":"API Tests","text":"<pre><code># tests/unit/test_api.py\nfrom fastapi.testclient import TestClient\nfrom src.mbti_classifier.api import app\n\nclient = TestClient(app)\n\ndef test_health():\n    response = client.get(\"/health\")\n    assert response.status_code == 200\n    assert response.json() == {\"status\": \"healthy\"}\n\ndef test_predict():\n    response = client.post(\n        \"/predict\",\n        json={\"text\": \"I love solving complex problems.\"}\n    )\n    assert response.status_code == 200\n    data = response.json()\n    assert \"mbti_type\" in data\n    assert len(data[\"mbti_type\"]) == 4\n</code></pre>"},{"location":"configuration/data/#ui-tests","title":"UI Tests","text":"<pre><code># tests/integration/test_ui.py\nimport requests\n\ndef test_ui_api_integration():\n    \"\"\"Test that UI can communicate with API.\"\"\"\n    # Assume API is running\n    API_URL = \"http://localhost:8000\"\n\n    # Call API as UI does\n    response = requests.post(\n        f\"{API_URL}/predict\",\n        json={\"text\": \"Test text\"}\n    )\n\n    assert response.status_code == 200\n    assert \"mbti_type\" in response.json()\n</code></pre>"},{"location":"configuration/data/#deployment","title":"Deployment","text":""},{"location":"configuration/data/#local","title":"Local","text":"<pre><code># Terminal 1\nuv run invoke api\n\n# Terminal 2\nuv run invoke ui\n</code></pre>"},{"location":"configuration/data/#docker","title":"Docker","text":"<pre><code># Build\nuv run invoke docker-build\n\n# Start services\nuv run invoke docker-up\n\n# Check logs\nuv run invoke docker-logs --service=api\nuv run invoke docker-logs --service=ui\n\n# Stop\nuv run invoke docker-down\n</code></pre>"},{"location":"configuration/data/#cloud-example-with-flyio","title":"Cloud (example with Fly.io)","text":"<pre><code># API\nfly launch --dockerfile=dockerfiles/api.dockerfile --name mbti-api\n\n# UI\nfly launch --dockerfile=dockerfiles/ui.dockerfile --name mbti-ui \\\n    --env API_URL=[https://mbti-api.fly.dev](https://mbti-api.fly.dev)\n</code></pre>"},{"location":"configuration/data/#monitoring","title":"Monitoring","text":""},{"location":"configuration/data/#api-metrics","title":"API Metrics","text":"<p>FastAPI provides automatic metrics at <code>/docs</code>:</p> <pre><code>http://localhost:8000/docs\n</code></pre> <p>Useful metrics: - Requests per second - Average latency - 4xx/5xx errors</p>"},{"location":"configuration/data/#ui-analytics","title":"UI Analytics","text":"<p>Streamlit Cloud provides automatic analytics:</p> <pre><code># Optional: add Google Analytics\nst.markdown(\"\"\"\n&lt;script async src=\"[https://www.googletagmanager.com/gtag/js?id=GA_ID](https://www.googletagmanager.com/gtag/js?id=GA_ID)\"&gt;&lt;/script&gt;\n\"\"\", unsafe_allow_html=True)\n</code></pre>"},{"location":"configuration/data/#performance","title":"Performance","text":""},{"location":"configuration/data/#api-optimization","title":"API Optimization","text":"<pre><code># 1. Batch predictions (more efficient)\n@app.post(\"/predict_batch\")\ndef predict_batch(request: PredictBatchRequest):\n    # Process in batch instead of one by one\n    texts = request.texts\n    predictions = classifier.predict_batch(texts)  # Vectorized\n    return {\"predictions\": predictions}\n\n# 2. Caching (for repeated requests)\nfrom functools import lru_cache\n\n@lru_cache(maxsize=128)\ndef cached_predict(text: str) -&gt; str:\n    return classifier.predict(text)\n</code></pre>"},{"location":"configuration/data/#ui-optimization","title":"UI Optimization","text":"<pre><code># 1. Cache in Streamlit\n@st.cache_data\ndef load_data():\n    # Data that doesn't change\n    return ...\n\n# 2. Session state (avoid recomputation)\nif \"result\" not in st.session_state:\n    st.session_state.result = None\n</code></pre>"},{"location":"configuration/data/#security","title":"Security","text":""},{"location":"configuration/data/#api_1","title":"API","text":"<pre><code># 1. Rate limiting (requires slowapi)\nfrom slowapi import Limiter\nfrom slowapi.util import get_remote_address\n\nlimiter = Limiter(key_func=get_remote_address)\napp.state.limiter = limiter\n\n@app.post(\"/predict\")\n@limiter.limit(\"100/minute\")\ndef predict(request: PredictRequest):\n    ...\n\n# 2. API Key authentication\nfrom fastapi import Header, HTTPException\n\nasync def verify_api_key(x_api_key: str = Header()):\n    if x_api_key != os.getenv(\"API_KEY\"):\n        raise HTTPException(status_code=401, detail=\"Invalid API key\")\n\n@app.post(\"/predict\", dependencies=[Depends(verify_api_key)])\ndef predict(request: PredictRequest):\n    ...\n</code></pre>"},{"location":"configuration/data/#ui_1","title":"UI","text":"<pre><code># Input sanitization\nimport html\n\ndef sanitize_input(text: str) -&gt; str:\n    \"\"\"Sanitize user input.\"\"\"\n    return html.escape(text)\n\n# Usage:\nsafe_text = sanitize_input(user_input)\nresult = predict(safe_text)\n</code></pre>"},{"location":"configuration/data/#troubleshooting","title":"Troubleshooting","text":""},{"location":"configuration/data/#ui-cannot-connect-to-api","title":"UI cannot connect to API","text":"<p>Problem: <pre><code>Error connecting to the API: Connection refused\n</code></pre></p> <p>Solutions:</p> <pre><code># 1. Verify that API is running\ncurl http://localhost:8000/health\n\n# 2. Verify API_URL in UI\n# In ui.py:\nAPI_URL = os.getenv(\"API_URL\", \"http://localhost:8000\")\n\n# 3. In Docker, verify networking\ndocker compose logs api\ndocker compose exec ui ping api\n</code></pre>"},{"location":"configuration/data/#api-returns-500","title":"API returns 500","text":"<p>Problem: <pre><code>{\"detail\": \"Internal Server Error\"}\n</code></pre></p> <p>Solutions:</p> <pre><code># 1. Check API logs\ndocker compose logs api\n\n# 2. Verify model exists\nls -lh models/best.ckpt\n\n# 3. Direct test\ncurl -X POST http://localhost:8000/predict \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"text\": \"test\"}'\n</code></pre>"},{"location":"configuration/data/#prediction-is-very-slow","title":"Prediction is very slow","text":"<p>Problem: Each prediction takes &gt;10 seconds</p> <p>Solutions:</p> <pre><code># 1. Move model to GPU (in api.py)\nimport torch\nclassifier = classifier.to('cuda' if torch.cuda.is_available() else 'cpu')\n\n# 2. Use batch prediction\n# Accumulate requests and process in batch\n\n# 3. Reduce max_length\n# In MBTIClassifier, tokenize with max_length=256 instead of 512\n</code></pre>"},{"location":"configuration/data/#references","title":"References","text":"<ul> <li>FastAPI Documentation</li> <li>Streamlit Documentation</li> <li>Docker Compose Networking</li> <li>API Development</li> <li>UI Development</li> </ul>"},{"location":"configuration/hydra/","title":"Hydra Configuration","text":"<p>This page explains how the Hydra-based configuration system works in the project.</p>"},{"location":"configuration/hydra/#what-is-hydra","title":"What is Hydra?","text":"<p>Hydra is a configuration framework that enables:</p> <ul> <li>\u2705 Modular Configuration: Splits configs into small, reusable files.</li> <li>\u2705 Composition: Combines multiple configs.</li> <li>\u2705 CLI Overrides: Changes parameters without editing files.</li> <li>\u2705 Multi-run: Runs experiments with different configurations automatically.</li> <li>\u2705 Instantiation: Creates Python objects directly from YAML.</li> </ul>"},{"location":"configuration/hydra/#configuration-structure","title":"Configuration Structure","text":"<pre><code>configs/\n\u251c\u2500\u2500 train.yaml              # Main config\n\u251c\u2500\u2500 train_quick.yaml        # Config for quick testing\n\u251c\u2500\u2500 train_cpu.yaml          # Config for CPU\n\u251c\u2500\u2500 train_production.yaml   # Config for production\n\u251c\u2500\u2500 data/\n\u2502   \u2514\u2500\u2500 default.yaml        # DataModule config\n\u251c\u2500\u2500 model/\n\u2502   \u251c\u2500\u2500 default.yaml        # Model config\n\u2502   \u2514\u2500\u2500 quick.yaml          # Model for quick tests\n\u251c\u2500\u2500 trainer/\n\u2502   \u251c\u2500\u2500 default.yaml        # Trainer config\n\u2502   \u251c\u2500\u2500 quick.yaml          # Quick Trainer\n\u2502   \u2514\u2500\u2500 cpu.yaml            # Trainer for CPU\n\u251c\u2500\u2500 callbacks/\n\u2502   \u2514\u2500\u2500 default.yaml        # Callbacks (early stopping, checkpoints)\n\u2514\u2500\u2500 logger/\n    \u251c\u2500\u2500 csv.yaml            # CSV logger\n    \u2514\u2500\u2500 wandb.yaml          # Weights &amp; Biases logger\n</code></pre>"},{"location":"configuration/hydra/#main-config-trainyaml","title":"Main Config: <code>train.yaml</code>","text":"<pre><code># @package _global_\n\ndefaults:\n  - data: default\n  - model: default\n  - trainer: default\n  - callbacks: default\n  - logger: csv\n  - _self_\n\n# Seed for reproducibility\nseed: 42\n\n# Paths\ndata_dir: data\nmodel_dir: models\nlog_dir: logs\n\n# Output directory (Hydra creates this automatically)\nhydra:\n  run:\n    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}\n  job:\n    chdir: false  # Do not change working directory\n</code></pre>"},{"location":"configuration/hydra/#composition-with-defaults","title":"Composition with <code>defaults</code>","text":"<pre><code>defaults:\n  - data: default        # Loads configs/data/default.yaml\n  - model: default       # Loads configs/model/default.yaml\n  - trainer: default     # Loads configs/trainer/default.yaml\n  - callbacks: default   # Loads configs/callbacks/default.yaml\n  - logger: csv          # Loads configs/logger/csv.yaml\n  - _self_               # Applies this file at the end\n</code></pre> <p>Priority Order (last one wins):</p> <ol> <li><code>configs/data/default.yaml</code></li> <li><code>configs/model/default.yaml</code></li> <li><code>configs/trainer/default.yaml</code></li> <li><code>configs/callbacks/default.yaml</code></li> <li><code>configs/logger/csv.yaml</code></li> <li><code>train.yaml</code> (due to <code>_self_</code>)</li> <li>CLI overrides</li> </ol>"},{"location":"configuration/hydra/#component-configs","title":"Component Configs","text":""},{"location":"configuration/hydra/#data-module-configsdatadefaultyaml","title":"Data Module: <code>configs/data/default.yaml</code>","text":"<pre><code>_target_: src.mbti_classifier.data.MBTIDataModule\nraw_data_path: data/raw/mbti_1.csv\nprocessed_data_path: data/processed/processed_mbti.csv\nbatch_size: 16\nmax_length: 512\nnum_workers: 4\n</code></pre> <p><code>_target_</code> indicates the class to instantiate:</p> <pre><code># Hydra does this automatically:\nfrom src.mbti_classifier.data import MBTIDataModule\n\ndatamodule = MBTIDataModule(\n    raw_data_path=\"data/raw/mbti_1.csv\",\n    processed_data_path=\"data/processed/processed_mbti.csv\",\n    batch_size=16,\n    max_length=512,\n    num_workers=4\n)\n</code></pre>"},{"location":"configuration/hydra/#model-configsmodeldefaultyaml","title":"Model: <code>configs/model/default.yaml</code>","text":"<pre><code>_target_: src.mbti_classifier.model.MBTIModel\nmodel_name: distilbert-base-uncased\nlearning_rate: 2e-5\ndropout_rate: 0.1\nweight_decay: 0.01\n</code></pre>"},{"location":"configuration/hydra/#trainer-configstrainerdefaultyaml","title":"Trainer: <code>configs/trainer/default.yaml</code>","text":"<pre><code>_target_: lightning.pytorch.Trainer\nmax_epochs: 10\naccelerator: auto\ndevices: auto\nprecision: 16-mixed\nlog_every_n_steps: 10\nenable_checkpointing: true\ndeterministic: true\n</code></pre> <p>Important: <code>_target_: lightning.pytorch.Trainer</code> directly instantiates the PyTorch Lightning Trainer.</p>"},{"location":"configuration/hydra/#callbacks-configscallbacksdefaultyaml","title":"Callbacks: <code>configs/callbacks/default.yaml</code>","text":"<pre><code>model_checkpoint:\n  _target_: lightning.pytorch.callbacks.ModelCheckpoint\n  dirpath: ${model_dir}\n  filename: best\n  monitor: val_loss\n  mode: min\n  save_top_k: 1\n  save_last: false\n  verbose: true\n\nearly_stopping:\n  _target_: lightning.pytorch.callbacks.EarlyStopping\n  monitor: val_loss\n  patience: 5\n  mode: min\n  verbose: true\n</code></pre> <p>List of callbacks:</p> <pre><code># Hydra instantiates each one:\ncallbacks = [\n    ModelCheckpoint(dirpath=\"models\", filename=\"best\", ...),\n    EarlyStopping(monitor=\"val_loss\", patience=5, ...)\n]\n</code></pre>"},{"location":"configuration/hydra/#logger-configsloggercsvyaml","title":"Logger: <code>configs/logger/csv.yaml</code>","text":"<pre><code>_target_: lightning.pytorch.loggers.CSVLogger\nsave_dir: ${log_dir}\nname: mbti_classifier\n</code></pre>"},{"location":"configuration/hydra/#usage-in-code","title":"Usage in Code","text":""},{"location":"configuration/hydra/#train-script-srcmbti_classifiertrainpy","title":"Train Script: <code>src/mbti_classifier/train.py</code>","text":"<pre><code>import hydra\nfrom omegaconf import DictConfig\nfrom hydra.utils import instantiate\n\n@hydra.main(version_base=None, config_path=\"../../configs\", config_name=\"train\")\ndef main(cfg: DictConfig):\n    # cfg contains the entire composed configuration\n\n    # Instantiate components\n    datamodule = instantiate(cfg.data)\n    model = instantiate(cfg.model)\n    trainer = instantiate(\n        cfg.trainer,\n        callbacks=[instantiate(cb) for cb in cfg.callbacks.values()],\n        logger=instantiate(cfg.logger)\n    )\n\n    # Train\n    trainer.fit(model, datamodule)\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"configuration/hydra/#hydramain-decorator","title":"<code>@hydra.main</code> Decorator","text":"<pre><code>@hydra.main(\n    version_base=None,           # No deprecation warnings\n    config_path=\"../../configs\",  # Path relative to configs/\n    config_name=\"train\"           # Name without .yaml\n)\n</code></pre> <p>Effect:</p> <ol> <li>Hydra loads <code>configs/train.yaml</code>.</li> <li>Processes <code>defaults</code> (loads data, model, etc. configs).</li> <li>Applies CLI overrides.</li> <li>Passes the composed config to <code>main(cfg)</code>.</li> </ol>"},{"location":"configuration/hydra/#instantiate-function","title":"<code>instantiate()</code> Function","text":"<pre><code>from hydra.utils import instantiate\n\n# Example 1: Simple Class\ncfg = {\"_target_\": \"builtins.dict\", \"a\": 1, \"b\": 2}\nobj = instantiate(cfg)\n# obj = dict(a=1, b=2)\n\n# Example 2: Custom Class\ncfg = {\n    \"_target_\": \"src.mbti_classifier.data.MBTIDataModule\",\n    \"batch_size\": 16\n}\nobj = instantiate(cfg)\n# obj = MBTIDataModule(batch_size=16)\n\n# Example 3: List of Objects\ncfg = {\n    \"cb1\": {\"_target_\": \"lightning.pytorch.callbacks.EarlyStopping\", \"patience\": 5},\n    \"cb2\": {\"_target_\": \"lightning.pytorch.callbacks.ModelCheckpoint\", \"monitor\": \"val_loss\"}\n}\ncallbacks = [instantiate(cb) for cb in cfg.values()]\n</code></pre>"},{"location":"configuration/hydra/#cli-overrides","title":"CLI Overrides","text":""},{"location":"configuration/hydra/#simple-override","title":"Simple Override","text":"<pre><code># Change batch size\nuv run python src/mbti_classifier/train.py data.batch_size=32\n\n# Change learning rate\nuv run python src/mbti_classifier/train.py model.learning_rate=5e-5\n\n# Change max epochs\nuv run python src/mbti_classifier/train.py trainer.max_epochs=20\n\n# Multiple overrides\nuv run python src/mbti_classifier/train.py \\\n    data.batch_size=32 \\\n    model.learning_rate=5e-5 \\\n    trainer.max_epochs=20\n</code></pre>"},{"location":"configuration/hydra/#config-group-override","title":"Config Group Override","text":"<pre><code># Use quick config instead of default\nuv run python src/mbti_classifier/train.py model=quick trainer=quick\n\n# Use wandb logger instead of csv\nuv run python src/mbti_classifier/train.py logger=wandb\n\n# Use CPU config\nuv run python src/mbti_classifier/train.py trainer=cpu\n</code></pre>"},{"location":"configuration/hydra/#full-file-override","title":"Full File Override","text":"<pre><code># Use train_quick.yaml instead of train.yaml\nuv run python src/mbti_classifier/train.py --config-name=train_quick\n\n# Equivalent to:\nuv run python src/mbti_classifier/train.py \\\n    model=quick \\\n    trainer=quick \\\n    trainer.max_epochs=1\n</code></pre>"},{"location":"configuration/hydra/#pre-defined-configs","title":"Pre-defined Configs","text":""},{"location":"configuration/hydra/#1-quick-test-train_quickyaml","title":"1. Quick Test: <code>train_quick.yaml</code>","text":"<pre><code>defaults:\n  - train  # Inherits from train.yaml\n  - override model: quick\n  - override trainer: quick\n  - _self_\n\n# Additional overrides\ntrainer:\n  max_epochs: 1  # Only 1 epoch\n  limit_train_batches: 10  # Only 10 batches\n  limit_val_batches: 5     # Only 5 validation batches\n</code></pre> <p>Usage:</p> <pre><code>uv run python src/mbti_classifier/train.py --config-name=train_quick\n# Finishes in ~2 minutes to validate pipeline\n</code></pre>"},{"location":"configuration/hydra/#2-cpu-only-train_cpuyaml","title":"2. CPU Only: <code>train_cpu.yaml</code>","text":"<pre><code>defaults:\n  - train\n  - override trainer: cpu\n  - _self_\n\ntrainer:\n  precision: 32  # No mixed precision on CPU\n</code></pre> <p>Usage:</p> <pre><code>uv run python src/mbti_classifier/train.py --config-name=train_cpu\n</code></pre>"},{"location":"configuration/hydra/#3-production-train_productionyaml","title":"3. Production: <code>train_production.yaml</code>","text":"<pre><code>defaults:\n  - train\n  - override logger: wandb\n  - _self_\n\ntrainer:\n  max_epochs: 20\n  precision: 16-mixed\n\ncallbacks:\n  model_checkpoint:\n    save_top_k: 3  # Save top 3 models\n  early_stopping:\n    patience: 10  # More patience\n</code></pre> <p>Usage:</p> <pre><code>uv run python src/mbti_classifier/train.py --config-name=train_production\n</code></pre>"},{"location":"configuration/hydra/#multi-run","title":"Multi-Run","text":"<p>Runs multiple experiments automatically:</p> <pre><code># Sweep over batch sizes\nuv run python src/mbti_classifier/train.py -m \\\n    data.batch_size=8,16,32\n\n# Sweep over learning rates\nuv run python src/mbti_classifier/train.py -m \\\n    model.learning_rate=1e-5,2e-5,5e-5\n\n# Grid search (9 combinations)\nuv run python src/mbti_classifier/train.py -m \\\n    data.batch_size=8,16,32 \\\n    model.learning_rate=1e-5,2e-5,5e-5\n</code></pre> <p>Output:</p> <pre><code>outputs/\n\u251c\u2500\u2500 2026-01-21/\n\u2502   \u251c\u2500\u2500 10-30-15/  # batch_size=8, lr=1e-5\n\u2502   \u251c\u2500\u2500 10-35-22/  # batch_size=8, lr=2e-5\n\u2502   \u251c\u2500\u2500 10-40-18/  # batch_size=8, lr=5e-5\n\u2502   \u251c\u2500\u2500 10-45-30/  # batch_size=16, lr=1e-5\n\u2502   \u2514\u2500\u2500 ...\n</code></pre>"},{"location":"configuration/hydra/#working-directory","title":"Working Directory","text":"<p>By default, Hydra changes the working directory to <code>outputs/.../</code>:</p> <pre><code># Problem with chdir=true (default)\nwith open(\"data/raw/file.csv\", \"r\") as f:  # Error: file not found\n    ...\n\n# Hydra is in outputs/2026-01-21/10-30-15/\n# It looks for: outputs/2026-01-21/10-30-15/data/raw/file.csv\n</code></pre> <p>**Solution: Disable <code>chdir**</code></p> <pre><code># configs/train.yaml\nhydra:\n  job:\n    chdir: false  # Keep original working directory\n</code></pre> <p>Now relative paths work:</p> <pre><code>with open(\"data/raw/file.csv\", \"r\") as f:  # \u2705 Works\n    ...\n</code></pre>"},{"location":"configuration/hydra/#accessing-configuration","title":"Accessing Configuration","text":""},{"location":"configuration/hydra/#dictconfig-vs-dict","title":"DictConfig vs Dict","text":"<pre><code>from omegaconf import DictConfig, OmegaConf\n\n@hydra.main(...)\ndef main(cfg: DictConfig):\n    # cfg is a DictConfig (not a normal dict)\n\n    # Access with dot notation\n    print(cfg.data.batch_size)  # 16\n\n    # Access with []\n    print(cfg[\"data\"][\"batch_size\"])  # 16\n\n    # Convert to normal dict\n    cfg_dict = OmegaConf.to_container(cfg, resolve=True)\n    print(type(cfg_dict))  # &lt;class 'dict'&gt;\n</code></pre>"},{"location":"configuration/hydra/#string-interpolation","title":"String Interpolation","text":"<pre><code>data_dir: data\nraw_data_path: ${data_dir}/raw/mbti_1.csv  # Interpolates data_dir\nprocessed_data_path: ${data_dir}/processed/processed_mbti.csv\n</code></pre> <p>Result:</p> <pre><code>cfg.data.raw_data_path  # \"data/raw/mbti_1.csv\"\ncfg.data.processed_data_path  # \"data/processed/processed_mbti.csv\"\n</code></pre>"},{"location":"configuration/hydra/#special-values","title":"Special Values","text":"<pre><code># Current timestamp\noutput_dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}\n# output_dir = \"outputs/2026-01-21/10-30-15\"\n\n# Access environment variables\napi_key: ${oc.env:WANDB_API_KEY}\n\n# Default value if it doesn't exist\napi_key: ${oc.env:WANDB_API_KEY,default_key}\n</code></pre>"},{"location":"configuration/hydra/#debugging","title":"Debugging","text":""},{"location":"configuration/hydra/#view-final-configuration","title":"View Final Configuration","text":"<pre><code># Print config without running\nuv run python src/mbti_classifier/train.py --cfg job\n\n# With overrides\nuv run python src/mbti_classifier/train.py --cfg job \\\n    data.batch_size=32 \\\n    model.learning_rate=5e-5\n</code></pre>"},{"location":"configuration/hydra/#validation","title":"Validation","text":"<pre><code>from omegaconf import OmegaConf\n\n@hydra.main(...)\ndef main(cfg: DictConfig):\n    # Print full config\n    print(OmegaConf.to_yaml(cfg))\n\n    # Validate required fields\n    assert cfg.data.batch_size &gt; 0, \"batch_size must be &gt; 0\"\n    assert cfg.model.learning_rate &gt; 0, \"learning_rate must be &gt; 0\"\n</code></pre>"},{"location":"configuration/hydra/#best-practices","title":"Best Practices","text":""},{"location":"configuration/hydra/#1-use-relative-paths","title":"1. Use Relative Paths","text":"<pre><code># \u2705 Good: relative to project root\nraw_data_path: data/raw/mbti_1.csv\n\n# \u274c Bad: absolute path\nraw_data_path: /Users/username/project/data/raw/mbti_1.csv\n</code></pre>"},{"location":"configuration/hydra/#2-separate-concerns","title":"2. Separate Concerns","text":"<pre><code># \u2705 Good: separate configs by component\nconfigs/\n\u251c\u2500\u2500 data/default.yaml\n\u251c\u2500\u2500 model/default.yaml\n\u2514\u2500\u2500 trainer/default.yaml\n\n# \u274c Bad: everything in one file\nconfigs/train.yaml  # 200 lines\n</code></pre>"},{"location":"configuration/hydra/#3-use-smart-defaults","title":"3. Use Smart Defaults","text":"<pre><code># \u2705 Good: reasonable defaults\nbatch_size: 16  # Works on most GPUs\nmax_length: 512  # Standard for BERT\n\n# \u274c Bad: extreme defaults\nbatch_size: 256  # OOM on small GPUs\nmax_length: 2048  # Too long\n</code></pre>"},{"location":"configuration/hydra/#4-document-configs","title":"4. Document Configs","text":"<pre><code># Batch size for training\n# Reduce if OOM, increase for better performance\nbatch_size: 16\n\n# Max sequence length\n# 512 is standard for DistilBERT\n# Reduce for short texts\nmax_length: 512\n</code></pre>"},{"location":"configuration/hydra/#full-examples","title":"Full Examples","text":""},{"location":"configuration/hydra/#train-with-wandb","title":"Train with Wandb","text":"<pre><code># 1. Export API key\nexport WANDB_API_KEY=your_key_here\n\n# 2. Train with wandb logger\nuv run python src/mbti_classifier/train.py \\\n    logger=wandb \\\n    logger.project=mbti-classifier \\\n    logger.name=experiment-1\n</code></pre>"},{"location":"configuration/hydra/#quick-test-on-cpu","title":"Quick Test on CPU","text":"<pre><code>uv run python src/mbti_classifier/train.py \\\n    --config-name=train_quick \\\n    trainer.accelerator=cpu \\\n    data.num_workers=0\n</code></pre>"},{"location":"configuration/hydra/#hyperparameter-sweep","title":"Hyperparameter Sweep","text":"<pre><code>uv run python src/mbti_classifier/train.py -m \\\n    model.learning_rate=1e-5,2e-5,5e-5 \\\n    model.dropout_rate=0.1,0.2,0.3 \\\n    trainer.max_epochs=10\n</code></pre>"},{"location":"configuration/hydra/#references","title":"References","text":"<ul> <li>Hydra Documentation</li> <li>OmegaConf Documentation</li> <li>PyTorch Lightning + Hydra</li> </ul>"},{"location":"configuration/training/","title":"Training Configuration","text":"<p>This page documents all configuration options for training the model.</p>"},{"location":"configuration/training/#available-configurations","title":"Available Configurations","text":""},{"location":"configuration/training/#1-default-training-trainyaml","title":"1. Default Training (<code>train.yaml</code>)","text":"<p>Standard configuration for GPU training:</p> <pre><code>defaults:\n  - data: default\n  - model: default\n  - trainer: default\n  - callbacks: default\n  - logger: csv\n  - _self_\n\nseed: 42\n\ndata_dir: data\nmodel_dir: models\nlog_dir: logs\n\nhydra:\n  run:\n    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}\n  job:\n    chdir: false\n</code></pre> <p>Usage: <pre><code>uv run python src/mbti_classifier/train.py\n</code></pre></p> <p>Duration: ~2 hours on V100 GPU Typical results: 75-80% total accuracy</p>"},{"location":"configuration/training/#2-quick-training-train_quickyaml","title":"2. Quick Training (<code>train_quick.yaml</code>)","text":"<p>For quick pipeline testing:</p> <pre><code>defaults:\n  - train\n  - override model: quick\n  - override trainer: quick\n  - _self_\n\ntrainer:\n  max_epochs: 1\n  limit_train_batches: 10\n  limit_val_batches: 5\n</code></pre> <p>Usage: <pre><code>uv run python src/mbti_classifier/train.py --config-name=train_quick\n</code></pre></p> <p>Duration: ~2 minutes Purpose: Validate that everything works without errors</p>"},{"location":"configuration/training/#3-cpu-training-train_cpuyaml","title":"3. CPU Training (<code>train_cpu.yaml</code>)","text":"<p>To train without a GPU:</p> <pre><code>defaults:\n  - train\n  - override trainer: cpu\n  - _self_\n\ntrainer:\n  accelerator: cpu\n  devices: 1\n  precision: 32\n</code></pre> <p>Usage: <pre><code>uv run python src/mbti_classifier/train.py --config-name=train_cpu\n</code></pre></p> <p>Duration: ~8-10 hours When to use: You do not have a GPU available</p>"},{"location":"configuration/training/#4-production-training-train_productionyaml","title":"4. Production Training (<code>train_production.yaml</code>)","text":"<p>For optimal training with Weights &amp; Biases:</p> <pre><code>defaults:\n  - train\n  - override logger: wandb\n  - _self_\n\ntrainer:\n  max_epochs: 20\n  precision: 16-mixed\n  gradient_clip_val: 1.0\n\ncallbacks:\n  model_checkpoint:\n    save_top_k: 3\n    save_last: true\n  early_stopping:\n    patience: 10\n    min_delta: 0.001\n\nlogger:\n  project: mbti-classifier\n  name: production-run\n  log_model: true\n</code></pre> <p>Usage: <pre><code>export WANDB_API_KEY=your_key\nuv run python src/mbti_classifier/train.py --config-name=train_production\n</code></pre></p> <p>Duration: ~4-5 hours Results: Best possible accuracy with early stopping</p>"},{"location":"configuration/training/#data-configuration","title":"Data Configuration","text":""},{"location":"configuration/training/#default-data-configsdatadefaultyaml","title":"Default Data (<code>configs/data/default.yaml</code>)","text":"<pre><code>_target_: src.mbti_classifier.data.MBTIDataModule\nraw_data_path: data/raw/mbti_1.csv\nprocessed_data_path: data/processed/processed_mbti.csv\nbatch_size: 16\nmax_length: 512\nnum_workers: 4\n</code></pre>"},{"location":"configuration/training/#parameters-explained","title":"Parameters Explained","text":"Parameter Default Description Recommended Range <code>batch_size</code> 16 Samples per batch 8-32 (depends on GPU) <code>max_length</code> 512 Maximum sequence length 128-512 <code>num_workers</code> 4 Parallel workers for DataLoader 0-8"},{"location":"configuration/training/#adjust-for-your-hardware","title":"Adjust for your Hardware","text":"<pre><code># Small GPU (8GB)\nuv run python src/mbti_classifier/train.py \\\n    data.batch_size=8 \\\n    data.max_length=256\n\n# Large GPU (16GB+)\nuv run python src/mbti_classifier/train.py \\\n    data.batch_size=32 \\\n    data.max_length=512\n\n# CPU (no GPU)\nuv run python src/mbti_classifier/train.py \\\n    --config-name=train_cpu \\\n    data.batch_size=4 \\\n    data.num_workers=0\n</code></pre>"},{"location":"configuration/training/#model-configuration","title":"Model Configuration","text":""},{"location":"configuration/training/#default-model-configsmodeldefaultyaml","title":"Default Model (<code>configs/model/default.yaml</code>)","text":"<pre><code>_target_: src.mbti_classifier.model.MBTIModel\nmodel_name: distilbert-base-uncased\nlearning_rate: 2e-5\ndropout_rate: 0.1\nweight_decay: 0.01\n</code></pre>"},{"location":"configuration/training/#parameters-explained_1","title":"Parameters Explained","text":"Parameter Default Description When to change <code>model_name</code> distilbert-base-uncased Hugging Face base model Change to <code>bert-base-uncased</code> for better accuracy (slower) <code>learning_rate</code> 2e-5 Learning rate Increase if underfitting, decrease if overfitting <code>dropout_rate</code> 0.1 Dropout for regularization Increase (0.2-0.3) if overfitting <code>weight_decay</code> 0.01 L2 regularization Increase if overfitting"},{"location":"configuration/training/#quick-model-configsmodelquickyaml","title":"Quick Model (<code>configs/model/quick.yaml</code>)","text":"<pre><code>_target_: src.mbti_classifier.model.MBTIModel\nmodel_name: distilbert-base-uncased\nlearning_rate: 5e-5  # Higher for fast convergence\ndropout_rate: 0.1\nweight_decay: 0.01\n</code></pre> <p>Difference: Learning rate 2.5x higher for quick testing.</p>"},{"location":"configuration/training/#trainer-configuration","title":"Trainer Configuration","text":""},{"location":"configuration/training/#default-trainer-configstrainerdefaultyaml","title":"Default Trainer (<code>configs/trainer/default.yaml</code>)","text":"<pre><code>_target_: lightning.pytorch.Trainer\nmax_epochs: 10\naccelerator: auto\ndevices: auto\nprecision: 16-mixed\nlog_every_n_steps: 10\nenable_checkpointing: true\ndeterministic: true\ngradient_clip_val: null\naccumulate_grad_batches: 1\n</code></pre>"},{"location":"configuration/training/#parameters-explained_2","title":"Parameters Explained","text":""},{"location":"configuration/training/#basics","title":"Basics","text":"Parameter Default Description <code>max_epochs</code> 10 Maximum number of epochs <code>accelerator</code> auto Accelerator type (auto detects GPU/CPU/MPS) <code>devices</code> auto Number of devices (auto uses all available)"},{"location":"configuration/training/#precision","title":"Precision","text":"Parameter Default Options Notes <code>precision</code> 16-mixed 32, 16-mixed, bf16-mixed 16-mixed reduces memory by 50% <p>Example:</p> <pre><code># Float32 (higher precision, more memory)\nuv run python src/mbti_classifier/train.py trainer.precision=32\n\n# Mixed precision (recommended)\nuv run python src/mbti_classifier/train.py trainer.precision=16-mixed\n\n# BFloat16 (if GPU supports it)\nuv run python src/mbti_classifier/train.py trainer.precision=bf16-mixed\n</code></pre>"},{"location":"configuration/training/#logging","title":"Logging","text":"Parameter Default Description <code>log_every_n_steps</code> 10 Log metrics every N steps <pre><code># Log more frequently\nuv run python src/mbti_classifier/train.py trainer.log_every_n_steps=5\n\n# Log less frequently (faster)\nuv run python src/mbti_classifier/train.py trainer.log_every_n_steps=50\n</code></pre>"},{"location":"configuration/training/#gradient-clipping","title":"Gradient Clipping","text":"<p>Prevents exploding gradients:</p> <pre><code>gradient_clip_val: 1.0  # Clip gradients to [-1, 1]\n</code></pre> <pre><code>uv run python src/mbti_classifier/train.py trainer.gradient_clip_val=1.0\n</code></pre>"},{"location":"configuration/training/#gradient-accumulation","title":"Gradient Accumulation","text":"<p>Simulates a larger batch size:</p> <pre><code>accumulate_grad_batches: 4  # Effective batch = batch_size * 4\n</code></pre> <pre><code># Effective batch of 64 with small GPU\nuv run python src/mbti_classifier/train.py \\\n    data.batch_size=16 \\\n    trainer.accumulate_grad_batches=4\n</code></pre>"},{"location":"configuration/training/#debugging","title":"Debugging","text":"<pre><code># Fast training for debug\nfast_dev_run: true  # Only 1 batch of train/val/test\n\n# Limit batches\nlimit_train_batches: 10  # Only 10 batches per epoch\nlimit_val_batches: 5\nlimit_test_batches: 5\n\n# Overfit on 1 batch (to test)\noverfit_batches: 1\n</code></pre> <pre><code># Fast dev run\nuv run python src/mbti_classifier/train.py trainer.fast_dev_run=true\n\n# Limit batches\nuv run python src/mbti_classifier/train.py \\\n    trainer.limit_train_batches=10 \\\n    trainer.limit_val_batches=5\n</code></pre>"},{"location":"configuration/training/#cpu-trainer-configstrainercpuyaml","title":"CPU Trainer (<code>configs/trainer/cpu.yaml</code>)","text":"<pre><code>_target_: lightning.pytorch.Trainer\nmax_epochs: 10\naccelerator: cpu\ndevices: 1\nprecision: 32  # No mixed precision on CPU\nlog_every_n_steps: 10\nenable_checkpointing: true\ndeterministic: true\n</code></pre>"},{"location":"configuration/training/#quick-trainer-configstrainerquickyaml","title":"Quick Trainer (<code>configs/trainer/quick.yaml</code>)","text":"<pre><code>_target_: lightning.pytorch.Trainer\nmax_epochs: 1\naccelerator: auto\ndevices: auto\nprecision: 16-mixed\nlog_every_n_steps: 5\nenable_checkpointing: false\nlimit_train_batches: 10\nlimit_val_batches: 5\n</code></pre>"},{"location":"configuration/training/#callbacks-configuration","title":"Callbacks Configuration","text":""},{"location":"configuration/training/#default-callbacks-configscallbacksdefaultyaml","title":"Default Callbacks (<code>configs/callbacks/default.yaml</code>)","text":"<pre><code>model_checkpoint:\n  _target_: lightning.pytorch.callbacks.ModelCheckpoint\n  dirpath: ${model_dir}\n  filename: best\n  monitor: val_loss\n  mode: min\n  save_top_k: 1\n  save_last: false\n  verbose: true\n\nearly_stopping:\n  _target_: lightning.pytorch.callbacks.EarlyStopping\n  monitor: val_loss\n  patience: 5\n  mode: min\n  verbose: true\n</code></pre>"},{"location":"configuration/training/#model-checkpoint","title":"Model Checkpoint","text":"<p>Saves the best models:</p> Parameter Default Description <code>dirpath</code> models Directory to save to <code>filename</code> best Filename <code>monitor</code> val_loss Metric to monitor <code>mode</code> min Minimize or maximize metric <code>save_top_k</code> 1 Save top K models <code>save_last</code> false Save last checkpoint <p>Examples:</p> <pre><code># Save top 3 models\nuv run python src/mbti_classifier/train.py \\\n    callbacks.model_checkpoint.save_top_k=3\n\n# Monitor accuracy instead of loss\nuv run python src/mbti_classifier/train.py \\\n    callbacks.model_checkpoint.monitor=val_acc_total \\\n    callbacks.model_checkpoint.mode=max\n\n# Also save the last model\nuv run python src/mbti_classifier/train.py \\\n    callbacks.model_checkpoint.save_last=true\n</code></pre>"},{"location":"configuration/training/#early-stopping","title":"Early Stopping","text":"<p>Stops automatically if it doesn't improve:</p> Parameter Default Description <code>monitor</code> val_loss Metric to monitor <code>patience</code> 5 Epochs without improvement before stopping <code>mode</code> min Minimize or maximize <code>min_delta</code> 0.0 Minimum improvement considered <p>Examples:</p> <pre><code># More patience\nuv run python src/mbti_classifier/train.py \\\n    callbacks.early_stopping.patience=10\n\n# Require minimum improvement of 0.001\nuv run python src/mbti_classifier/train.py \\\n    callbacks.early_stopping.min_delta=0.001\n\n# Disable early stopping\nuv run python src/mbti_classifier/train.py \\\n    callbacks.early_stopping=null\n</code></pre>"},{"location":"configuration/training/#logger-configuration","title":"Logger Configuration","text":""},{"location":"configuration/training/#csv-logger-configsloggercsvyaml","title":"CSV Logger (<code>configs/logger/csv.yaml</code>)","text":"<pre><code>_target_: lightning.pytorch.loggers.CSVLogger\nsave_dir: ${log_dir}\nname: mbti_classifier\n</code></pre> <p>Saves logs to <code>logs/mbti_classifier/version_X/metrics.csv</code>.</p>"},{"location":"configuration/training/#wandb-logger-configsloggerwandbyaml","title":"Wandb Logger (<code>configs/logger/wandb.yaml</code>)","text":"<pre><code>_target_: lightning.pytorch.loggers.WandbLogger\nproject: mbti-classifier\nname: null\nsave_dir: ${log_dir}\nlog_model: false\n</code></pre> <p>Setup:</p> <pre><code># 1. Install wandb\nuv add wandb\n\n# 2. Login\nwandb login\n\n# 3. Train\nuv run python src/mbti_classifier/train.py logger=wandb\n</code></pre> <p>Options:</p> <pre><code># Custom name\nuv run python src/mbti_classifier/train.py \\\n    logger=wandb \\\n    logger.name=experiment-1\n\n# Custom project\nuv run python src/mbti_classifier/train.py \\\n    logger=wandb \\\n    logger.project=my-mbti-project\n\n# Save model to wandb\nuv run python src/mbti_classifier/train.py \\\n    logger=wandb \\\n    logger.log_model=true\n</code></pre>"},{"location":"configuration/training/#full-examples","title":"Full Examples","text":""},{"location":"configuration/training/#1-basic-training","title":"1. Basic Training","text":"<pre><code>uv run python src/mbti_classifier/train.py\n</code></pre>"},{"location":"configuration/training/#2-quick-test","title":"2. Quick Test","text":"<pre><code>uv run python src/mbti_classifier/train.py --config-name=train_quick\n</code></pre>"},{"location":"configuration/training/#3-cpu-training","title":"3. CPU Training","text":"<pre><code>uv run python src/mbti_classifier/train.py --config-name=train_cpu\n</code></pre>"},{"location":"configuration/training/#4-custom-hyperparameters","title":"4. Custom Hyperparameters","text":"<pre><code>uv run python src/mbti_classifier/train.py \\\n    data.batch_size=32 \\\n    model.learning_rate=5e-5 \\\n    model.dropout_rate=0.2 \\\n    trainer.max_epochs=15 \\\n    callbacks.early_stopping.patience=8\n</code></pre>"},{"location":"configuration/training/#5-production-with-wandb","title":"5. Production with Wandb","text":"<pre><code>export WANDB_API_KEY=your_key\n\nuv run python src/mbti_classifier/train.py \\\n    --config-name=train_production \\\n    logger.name=production-v1 \\\n    logger.log_model=true\n</code></pre>"},{"location":"configuration/training/#6-hyperparameter-sweep","title":"6. Hyperparameter Sweep","text":"<pre><code>uv run python src/mbti_classifier/train.py -m \\\n    model.learning_rate=1e-5,2e-5,5e-5 \\\n    model.dropout_rate=0.1,0.2,0.3\n</code></pre> <p>Runs 9 combinations (3 x 3).</p>"},{"location":"configuration/training/#troubleshooting","title":"Troubleshooting","text":""},{"location":"configuration/training/#out-of-memory","title":"Out of Memory","text":"<pre><code># Reduce batch size\nuv run python src/mbti_classifier/train.py data.batch_size=8\n\n# Reduce max_length\nuv run python src/mbti_classifier/train.py data.max_length=256\n\n# Gradient accumulation (large effective batch, small memory)\nuv run python src/mbti_classifier/train.py \\\n    data.batch_size=8 \\\n    trainer.accumulate_grad_batches=4\n</code></pre>"},{"location":"configuration/training/#training-too-slow","title":"Training Too Slow","text":"<pre><code># Use mixed precision\nuv run python src/mbti_classifier/train.py trainer.precision=16-mixed\n\n# Reduce logging\nuv run python src/mbti_classifier/train.py trainer.log_every_n_steps=50\n\n# Fewer workers (if CPU is the bottleneck)\nuv run python src/mbti_classifier/train.py data.num_workers=2\n</code></pre>"},{"location":"configuration/training/#not-converging","title":"Not Converging","text":"<pre><code># Increase learning rate\nuv run python src/mbti_classifier/train.py model.learning_rate=5e-5\n\n# More epochs\nuv run python src/mbti_classifier/train.py trainer.max_epochs=20\n\n# Reduce regularization\nuv run python src/mbti_classifier/train.py \\\n    model.dropout_rate=0.05 \\\n    model.weight_decay=0.001\n</code></pre>"},{"location":"configuration/training/#overfitting","title":"Overfitting","text":"<pre><code># Increase dropout\nuv run python src/mbti_classifier/train.py model.dropout_rate=0.3\n\n# Increase weight decay\nuv run python src/mbti_classifier/train.py model.weight_decay=0.05\n\n# More aggressive early stopping\nuv run python src/mbti_classifier/train.py \\\n    callbacks.early_stopping.patience=3 \\\n    callbacks.early_stopping.min_delta=0.001\n</code></pre>"},{"location":"configuration/training/#references","title":"References","text":"<ul> <li>Hydra Configuration</li> <li>Model Architecture</li> <li>Data Pipeline</li> <li>PyTorch Lightning Trainer</li> </ul>"},{"location":"development/docker/","title":"Docker","text":"<p>This guide covers using Docker and Docker Compose for containerized deployment.</p>"},{"location":"development/docker/#overview","title":"Overview","text":"<p>The project uses three separate Docker images:</p> Image Purpose Port Profile <code>mbti-train</code> Model training - <code>training</code> <code>mbti-api</code> FastAPI server 8000 default <code>mbti-ui</code> Streamlit UI 8501 default"},{"location":"development/docker/#docker-compose-setup","title":"Docker Compose Setup","text":""},{"location":"development/docker/#architecture","title":"Architecture","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502   train     \u2502 (one-time job)\n\u2502   service   \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2502 Saves to ./models/\n       \u2193\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510        HTTP         \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502     api     \u2502 \u2190\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2502    ui    \u2502\n\u2502  (port 8000)\u2502                     \u2502(port 8501\u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2191\n       \u2502 Loads from ./models/ (read-only)\n</code></pre>"},{"location":"development/docker/#shared-volumes","title":"Shared Volumes","text":"<pre><code>volumes:\n  - ./models:/app/models    # Train writes, API reads\n  - ./data:/app/data        # Dataset caching\n  - ./configs:/app/configs  # Hydra configurations\n</code></pre> <p>Why bind mounts? - Direct access from host - Changes visible in real-time - Easy debugging - Compatible with DVC</p>"},{"location":"development/docker/#basic-usage","title":"Basic Usage","text":""},{"location":"development/docker/#build-images","title":"Build Images","text":"<pre><code># Build all images\nuv run invoke docker-build\n\n# Or manually\ndocker compose build\n\n# Build specific service\ndocker compose build api\n</code></pre>"},{"location":"development/docker/#start-services","title":"Start Services","text":"<pre><code># Start API + UI\nuv run invoke docker-up\n\n# Or manually\ndocker compose up -d api ui\n\n# Check status\ndocker compose ps\n</code></pre>"},{"location":"development/docker/#view-logs","title":"View Logs","text":"<pre><code># All services\nuv run invoke docker-logs\n\n# Specific service\nuv run invoke docker-logs --service=api\nuv run invoke docker-logs --service=ui\n\n# Follow logs in real-time\ndocker compose logs -f api\n</code></pre>"},{"location":"development/docker/#stop-services","title":"Stop Services","text":"<pre><code># Stop all\nuv run invoke docker-down\n\n# Or manually\ndocker compose down\n\n# Stop specific service\ndocker compose stop ui\n</code></pre>"},{"location":"development/docker/#training-workflow","title":"Training Workflow","text":""},{"location":"development/docker/#train-in-container","title":"Train in Container","text":"<pre><code># Train with default config\nuv run invoke docker-train\n\n# Or manually\ndocker compose run --rm train\n\n# Train with custom config\ndocker compose run --rm train \\\n  --config-name train_production\n</code></pre> <p>What happens: 1. Starts <code>mbti-train</code> container 2. Downloads/uses cached data from <code>./data/</code> 3. Trains model 4. Saves checkpoint to <code>./models/best.ckpt</code> 5. Container exits (<code>--rm</code> removes it)</p>"},{"location":"development/docker/#use-trained-model","title":"Use Trained Model","text":"<p>After training, restart API to load new model:</p> <pre><code>uv run invoke docker-restart --service=api\n\n# Or manually\ndocker compose restart api\n</code></pre>"},{"location":"development/docker/#development-workflow","title":"Development Workflow","text":""},{"location":"development/docker/#option-1-local-development","title":"Option 1: Local Development","text":"<pre><code># Develop without Docker\nuv run invoke train\nuv run invoke api     # Terminal 1\nuv run invoke ui      # Terminal 2\n</code></pre>"},{"location":"development/docker/#option-2-hybrid-api-in-docker-dev-locally","title":"Option 2: Hybrid (API in Docker, dev locally)","text":"<pre><code># Start only API\ndocker compose up -d api\n\n# Develop locally\ncode src/mbti_classifier/ui.py\nuv run invoke ui\n</code></pre>"},{"location":"development/docker/#option-3-full-docker","title":"Option 3: Full Docker","text":"<pre><code># Build and run everything\ndocker compose build\ndocker compose up -d\n</code></pre>"},{"location":"development/docker/#advanced-configuration","title":"Advanced Configuration","text":""},{"location":"development/docker/#environment-variables","title":"Environment Variables","text":"<p>Set in <code>docker-compose.yaml</code>:</p> <pre><code>api:\n  environment:\n    - PYTHONUNBUFFERED=1          # Real-time logs\n    - TRANSFORMERS_CACHE=/app/.cache  # Model cache\n\nui:\n  environment:\n    - API_URL=http://api:8000     # Internal networking\n</code></pre>"},{"location":"development/docker/#health-checks","title":"Health Checks","text":"<p>API has health check configured:</p> <pre><code>healthcheck:\n  test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8000/health\"]\n  interval: 30s\n  timeout: 10s\n  retries: 5\n  start_period: 120s  # Model loading time\n</code></pre> <p>Why 120s start period? - DistilBERT downloads on first run (~250MB) - Model loading from checkpoint (~800MB) - Prevents false \"unhealthy\" status</p>"},{"location":"development/docker/#service-dependencies","title":"Service Dependencies","text":"<p>UI waits for API to be healthy:</p> <pre><code>ui:\n  depends_on:\n    api:\n      condition: service_healthy\n</code></pre> <p>This prevents connection errors on startup.</p>"},{"location":"development/docker/#docker-images","title":"Docker Images","text":""},{"location":"development/docker/#base-image","title":"Base Image","text":"<p>All services use: <pre><code>FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim\n</code></pre></p> <p>Why this image? - Includes <code>uv</code> pre-installed - Debian-based (better compatibility than Alpine) - Optimized for Python projects</p>"},{"location":"development/docker/#api-image","title":"API Image","text":"<p>Special additions:</p> <pre><code>RUN apt-get update &amp;&amp; apt-get install -y \\\n    curl \\           # For health checks\n    build-essential \\ # For PyTorch compilation\n    g++ \\\n    &amp;&amp; rm -rf /var/lib/apt/lists/*\n</code></pre> <p>Why build tools? - PyTorch may compile optimizations at runtime - Without g++, you get: <code>InvalidCxxCompiler</code> error</p>"},{"location":"development/docker/#image-sizes","title":"Image Sizes","text":"<pre><code>REPOSITORY      TAG       SIZE\nmbti-train      latest    ~1.2 GB  (PyTorch + Lightning + WandB)\nmbti-api        latest    ~900 MB  (PyTorch + Transformers)\nmbti-ui         latest    ~600 MB  (Streamlit + Plotly)\n</code></pre>"},{"location":"development/docker/#debugging","title":"Debugging","text":""},{"location":"development/docker/#enter-running-container","title":"Enter Running Container","text":"<pre><code># Interactive shell in API container\ndocker compose exec api bash\n\n# Check model exists\ndocker compose exec api ls -lh /app/models/\n\n# Test tokenizer\ndocker compose exec api python -c \"from transformers import AutoTokenizer; print(AutoTokenizer.from_pretrained('distilbert-base-uncased'))\"\n</code></pre>"},{"location":"development/docker/#view-container-logs","title":"View Container Logs","text":"<pre><code># Last 100 lines\ndocker compose logs --tail=100 api\n\n# Continuous follow\ndocker compose logs -f ui\n\n# Since specific time\ndocker compose logs --since 5m api\n</code></pre>"},{"location":"development/docker/#inspect-container","title":"Inspect Container","text":"<pre><code># Full container details\ndocker compose exec api env\n\n# Network info\ndocker inspect mbti-api | grep IPAddress\n\n# Volume mounts\ndocker inspect mbti-api | grep -A 10 Mounts\n</code></pre>"},{"location":"development/docker/#production-deployment","title":"Production Deployment","text":""},{"location":"development/docker/#build-for-production","title":"Build for Production","text":"<pre><code># Multi-platform build (for different architectures)\ndocker buildx build --platform linux/amd64,linux/arm64 \\\n  -t mbti-api:latest \\\n  -f dockerfiles/api.dockerfile .\n</code></pre>"},{"location":"development/docker/#push-to-registry","title":"Push to Registry","text":"<pre><code># Tag for registry\ndocker tag mbti-api:latest ghcr.io/mlopsgroup70/mbti-api:v1.0\n\n# Push\ndocker push ghcr.io/mlopsgroup70/mbti-api:v1.0\n</code></pre>"},{"location":"development/docker/#deploy-on-server","title":"Deploy on Server","text":"<pre><code># On remote server\ngit clone https://github.com/mlopsgroup70/mlops_group70.git\ncd mlops_group70\n\n# Authenticate DVC\ngcloud auth application-default login\n\n# Pull data and models\nuv run dvc pull\n\n# Start services\ndocker compose up -d api ui\n</code></pre>"},{"location":"development/docker/#optimization-tips","title":"Optimization Tips","text":""},{"location":"development/docker/#reduce-build-time","title":"Reduce Build Time","text":"<pre><code># Cache dependencies separately\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-install-project\n\n# Then copy code\nCOPY src/ src/\nRUN uv sync --frozen\n</code></pre>"},{"location":"development/docker/#multi-stage-builds","title":"Multi-stage Builds","text":"<p>For even smaller images:</p> <pre><code># Build stage\nFROM python:3.12 AS builder\nCOPY . .\nRUN pip install build &amp;&amp; python -m build\n\n# Runtime stage\nFROM python:3.12-slim\nCOPY --from=builder /dist/*.whl .\nRUN pip install *.whl\n</code></pre>"},{"location":"development/docker/#use-dockerignore","title":"Use .dockerignore","text":"<p>Already configured:</p> <pre><code>__pycache__/\n*.pyc\n.git/\ndata/       # Don't copy data, use volumes\nmodels/     # Don't copy models, use volumes\n.venv/\ntests/\n</code></pre>"},{"location":"development/docker/#troubleshooting","title":"Troubleshooting","text":""},{"location":"development/docker/#port-already-in-use","title":"Port already in use","text":"<pre><code># Find process\nlsof -ti:8000\n\n# Kill it\nlsof -ti:8000 | xargs kill -9\n</code></pre>"},{"location":"development/docker/#container-exits-immediately","title":"Container exits immediately","text":"<pre><code># Check exit code\ndocker compose ps\n\n# View logs\ndocker compose logs train\n</code></pre>"},{"location":"development/docker/#out-of-disk-space","title":"Out of disk space","text":"<pre><code># Remove unused images\ndocker image prune -a\n\n# Remove unused volumes\ndocker volume prune\n\n# Full cleanup\ndocker system prune -a --volumes\n</code></pre>"},{"location":"development/docker/#permission-denied-on-volumes","title":"Permission denied on volumes","text":"<pre><code># Fix ownership (Linux)\nsudo chown -R $USER:$USER ./models ./data\n</code></pre>"},{"location":"development/docker/#next-steps","title":"Next Steps","text":"<ul> <li>Learn about DVC for data versioning</li> <li>Explore Testing in Docker</li> <li>Read API Reference</li> </ul>"},{"location":"development/dvc/","title":"Data Version Control (DVC)","text":"<p>This guide covers using DVC for versioning datasets and models with Google Cloud Storage.</p>"},{"location":"development/dvc/#overview","title":"Overview","text":"<p>DVC (Data Version Control) is Git for data. It:</p> <ul> <li>Tracks large files without bloating Git repos</li> <li>Versions datasets and models with Git commits</li> <li>Enables collaboration via cloud storage</li> <li>Ensures reproducibility with checksums</li> </ul>"},{"location":"development/dvc/#architecture","title":"Architecture","text":"<pre><code>Local Repository                  Google Cloud Storage\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510              \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 Git              \u2502              \u2502 mlops70_bucket      \u2502\n\u2502 \u251c\u2500\u2500 data/raw.dvc \u2502\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2502 \u2514\u2500\u2500 dvcstore/       \u2502\n\u2502 \u251c\u2500\u2500 models.dvc   \u2502   metadata   \u2502     \u2514\u2500\u2500 files/md5/  \u2502\n\u2502 \u2514\u2500\u2500 .dvc/config  \u2502              \u2502         \u251c\u2500\u2500 34/...  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518              \u2502         \u251c\u2500\u2500 f3/...  \u2502\n                                  \u2502         \u2514\u2500\u2500 ...     \u2502\n                                  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n\nLocal Cache                        \n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 .dvc/cache/      \u2502 (linked to GCS)\n\u2502 \u251c\u2500\u2500 files/       \u2502\n\u2502 \u2514\u2500\u2500 tmp/         \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"development/dvc/#setup","title":"Setup","text":""},{"location":"development/dvc/#initialize-dvc","title":"Initialize DVC","text":"<p>Already done in this project:</p> <pre><code># Check DVC status\nuv run dvc status\n\n# View configuration\ncat .dvc/config\n</code></pre> <p>Configuration: <pre><code>[core]\n    remote = gcs\n[remote \"gcs\"]\n    url = gs://mlops70_bucket/dvcstore\n</code></pre></p>"},{"location":"development/dvc/#authenticate","title":"Authenticate","text":""},{"location":"development/dvc/#option-a-gcloud-cli-recommended-for-local","title":"Option A: gcloud CLI (Recommended for local)","text":"<pre><code># First time only\ngcloud auth application-default login\n\n# Test access\ngsutil ls gs://mlops70_bucket/\n</code></pre>"},{"location":"development/dvc/#option-b-service-account-cicd","title":"Option B: Service Account (CI/CD)","text":"<pre><code># Set environment variable\nexport GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json\n\n# Or in .env\necho \"GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json\" &gt;&gt; .env\n</code></pre> <p>Warning</p> <p>Never commit service account keys to Git. They're in <code>.gitignore</code>.</p>"},{"location":"development/dvc/#basic-workflow","title":"Basic Workflow","text":""},{"location":"development/dvc/#pull-data","title":"Pull Data","text":"<p>Download datasets and models:</p> <pre><code># Pull everything\nuv run dvc pull\n\n# Or via invoke\nuv run invoke dvc-pull\n\n# Pull specific file\nuv run dvc pull data/raw.dvc\n</code></pre> <p>What happens: 1. Reads <code>data/raw.dvc</code> metadata 2. Downloads files from GCS 3. Reconstructs <code>data/raw/</code> directory 4. Verifies checksums</p>"},{"location":"development/dvc/#check-status","title":"Check Status","text":"<pre><code># See what's changed\nuv run dvc status\n\n# Or via invoke\nuv run invoke dvc-status\n</code></pre> <p>Output: <pre><code>Data and pipelines are up to date.\n</code></pre></p> <p>Or if changes: <pre><code>data/raw.dvc:\n    modified:           data/raw/mbti_1.csv\n</code></pre></p>"},{"location":"development/dvc/#push-changes","title":"Push Changes","text":"<p>After training a new model:</p> <pre><code># 1. Add to DVC\nuv run dvc add models\n\n# 2. Commit metadata to Git\ngit add models.dvc .gitignore\ngit commit -m \"Train model v2\"\n\n# 3. Push data to GCS\nuv run dvc push\n\n# Or via invoke\nuv run invoke dvc-push\n\n# 4. Push Git changes\ngit push\n</code></pre>"},{"location":"development/dvc/#file-structure","title":"File Structure","text":""},{"location":"development/dvc/#dvc-metadata-files","title":"DVC Metadata Files","text":"<pre><code># models.dvc\nouts:\n- md5: f3dc2086c25e37c552d4000a242aee83.dir\n  size: 803623819\n  nfiles: 2\n  hash: md5\n  path: models\n</code></pre> <p>Fields: - <code>md5</code>: Content hash (identifies version) - <code>size</code>: Total size in bytes - <code>nfiles</code>: Number of files in directory - <code>path</code>: Local path</p>"},{"location":"development/dvc/#storage-layout","title":"Storage Layout","text":"<pre><code>gs://mlops70_bucket/dvcstore/\n\u2514\u2500\u2500 files/\n    \u2514\u2500\u2500 md5/\n        \u251c\u2500\u2500 34/           # Hash prefix for organization\n        \u2502   \u2514\u2500\u2500 6acadd... # Actual file content\n        \u251c\u2500\u2500 f3/\n        \u2502   \u2514\u2500\u2500 dc2086... # Model checkpoint\n        \u2514\u2500\u2500 ...\n</code></pre> <p>Why this structure? - Deduplication: Identical files share one copy - Scalability: Distributes files across directories - Integrity: Filename IS the hash</p>"},{"location":"development/dvc/#advanced-usage","title":"Advanced Usage","text":""},{"location":"development/dvc/#track-new-data","title":"Track New Data","text":"<pre><code># Add new dataset\nuv run dvc add data/external/new_dataset.csv\n\n# Commit\ngit add data/external/new_dataset.csv.dvc data/external/.gitignore\ngit commit -m \"Add external dataset\"\n\n# Push\nuv run dvc push\n</code></pre>"},{"location":"development/dvc/#checkout-specific-version","title":"Checkout Specific Version","text":"<pre><code># Switch to older commit\ngit checkout abc123\n\n# Get data from that version\nuv run dvc checkout\n\n# Return to latest\ngit checkout main\nuv run dvc checkout\n</code></pre>"},{"location":"development/dvc/#compare-versions","title":"Compare Versions","text":"<pre><code># Show DVC diff between commits\nuv run dvc diff HEAD~1\n\n# Output:\n# Modified:\n#     data/processed.dvc\n</code></pre>"},{"location":"development/dvc/#remove-tracking","title":"Remove Tracking","text":"<pre><code># Stop tracking with DVC\nuv run dvc remove models.dvc\n\n# Start tracking with Git instead\ngit rm models.dvc\ngit add models/\n</code></pre>"},{"location":"development/dvc/#collaboration-workflow","title":"Collaboration Workflow","text":""},{"location":"development/dvc/#team-member-cloning","title":"Team Member Cloning","text":"<pre><code># 1. Clone repo\ngit clone https://github.com/mlopsgroup70/mlops_group70.git\ncd mlops_group70\n\n# 2. Install dependencies\nuv sync --dev\n\n# 3. Authenticate\ngcloud auth application-default login\n\n# 4. Pull data\nuv run dvc pull\n</code></pre>"},{"location":"development/dvc/#updating-data","title":"Updating Data","text":"<p>Person A: <pre><code># 1. Modify data\npython preprocess.py --new-method\n\n# 2. Track changes\nuv run dvc add data/processed\n\n# 3. Commit and push\ngit add data/processed.dvc\ngit commit -m \"Improve preprocessing\"\nuv run dvc push\ngit push\n</code></pre></p> <p>Person B: <pre><code># 1. Pull Git changes\ngit pull\n\n# 2. Pull new data\nuv run dvc pull\n\n# 3. Verify\nls -lh data/processed/\n</code></pre></p>"},{"location":"development/dvc/#integration-with-training","title":"Integration with Training","text":""},{"location":"development/dvc/#before-training","title":"Before Training","text":"<pre><code># Ensure latest data\nuv run dvc pull\n</code></pre>"},{"location":"development/dvc/#after-training","title":"After Training","text":"<pre><code># Track new model\nuv run dvc add models\n\n# Commit experiment\ngit add models.dvc\ngit commit -m \"Experiment: lr=5e-5, batch_size=32\n\nMetrics:\n- Val F1: 0.85\n- Test F1: 0.83\"\n\n# Share with team\nuv run dvc push\ngit push\n</code></pre>"},{"location":"development/dvc/#in-docker","title":"In Docker","text":"<pre><code># Pull before building\nuv run dvc pull\n\n# Build with latest data\ndocker compose build train\n\n# Train\ndocker compose run --rm train\n\n# Track new model\nuv run dvc add models\n</code></pre>"},{"location":"development/dvc/#verification","title":"Verification","text":""},{"location":"development/dvc/#check-data-integrity","title":"Check Data Integrity","text":"<pre><code># Verify local files match DVC\nuv run dvc status\n\n# Recalculate checksums\nuv run dvc status --cloud\n</code></pre>"},{"location":"development/dvc/#inspect-remote","title":"Inspect Remote","text":"<pre><code># List files in GCS\ngsutil ls -r gs://mlops70_bucket/dvcstore/\n\n# Check specific file\ngsutil ls gs://mlops70_bucket/dvcstore/files/md5/f3/\n</code></pre>"},{"location":"development/dvc/#dry-run","title":"Dry Run","text":"<pre><code># See what would be pushed\nuv run dvc push --dry-run\n\n# See what would be pulled\nuv run dvc pull --dry-run\n</code></pre>"},{"location":"development/dvc/#storage-management","title":"Storage Management","text":""},{"location":"development/dvc/#cache-management","title":"Cache Management","text":"<pre><code># Show cache usage\nuv run dvc cache dir\n\n# Clean unused cache\nuv run dvc gc --workspace\n\n# Clean all except current\nuv run dvc gc --cloud\n</code></pre>"},{"location":"development/dvc/#fetch-vs-pull","title":"Fetch vs Pull","text":"<pre><code># Fetch: Download to cache only\nuv run dvc fetch\n\n# Pull: Fetch + checkout to workspace\nuv run dvc pull\n</code></pre>"},{"location":"development/dvc/#cicd-integration","title":"CI/CD Integration","text":""},{"location":"development/dvc/#github-actions-example","title":"GitHub Actions Example","text":"<pre><code>name: Train Model\n\non:\n  push:\n    paths:\n      - 'data/**'\n\njobs:\n  train:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n\n      - name: Setup Python\n        uses: actions/setup-python@v4\n        with:\n          python-version: '3.12'\n\n      - name: Setup DVC\n        run: pip install dvc[gs]\n\n      - name: Configure GCS credentials\n        env:\n          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GCS_KEY }}\n        run: echo \"$GOOGLE_APPLICATION_CREDENTIALS\" &gt; key.json\n\n      - name: Pull data\n        run: dvc pull\n\n      - name: Train\n        run: uv run invoke train\n\n      - name: Push model\n        run: |\n          dvc add models\n          git config user.name \"GitHub Actions\"\n          git config user.email \"actions@github.com\"\n          git add models.dvc\n          git commit -m \"Auto: Update model\"\n          dvc push\n          git push\n</code></pre>"},{"location":"development/dvc/#troubleshooting","title":"Troubleshooting","text":""},{"location":"development/dvc/#authentication-fails","title":"Authentication fails","text":"<pre><code># Re-authenticate\ngcloud auth application-default login\n\n# Check credentials\ngcloud auth list\n\n# Test bucket access\ngsutil ls gs://mlops70_bucket/\n</code></pre>"},{"location":"development/dvc/#files-not-found-in-remote","title":"Files not found in remote","text":"<pre><code># Check what's in remote\nuv run dvc status --cloud\n\n# Force push\nuv run dvc push --force\n</code></pre>"},{"location":"development/dvc/#checksum-mismatch","title":"Checksum mismatch","text":"<pre><code># Recalculate local checksums\nuv run dvc status --recalculate\n\n# If corrupted, re-pull\nuv run dvc pull --force\n</code></pre>"},{"location":"development/dvc/#large-cache-size","title":"Large cache size","text":"<pre><code># Show cache size\ndu -sh .dvc/cache/\n\n# Remove old versions\nuv run dvc gc --workspace --cloud\n</code></pre>"},{"location":"development/dvc/#best-practices","title":"Best Practices","text":""},{"location":"development/dvc/#do","title":"\u2705 Do","text":"<ul> <li>Pull before training: <code>dvc pull</code> \u2192 <code>train</code> \u2192 <code>dvc push</code></li> <li>Commit meaningful messages: Include metrics in commit</li> <li>Use .dvcignore: Ignore temporary files</li> <li>Regular garbage collection: Clean old versions</li> </ul>"},{"location":"development/dvc/#dont","title":"\u274c Don't","text":"<ul> <li>Don't commit large files to Git: Use DVC instead</li> <li>Don't share credentials: Use gcloud CLI or CI secrets</li> <li>Don't skip <code>dvc push</code>: Team won't have your data</li> <li>Don't modify .dvc files manually: Use DVC commands</li> </ul>"},{"location":"development/dvc/#monitoring","title":"Monitoring","text":""},{"location":"development/dvc/#track-usage","title":"Track Usage","text":"<pre><code># See what's tracked\nuv run dvc list . -R --dvc-only\n\n# Show file sizes\nuv run dvc status --verbose\n</code></pre>"},{"location":"development/dvc/#gcs-costs","title":"GCS Costs","text":"<p>Monitor bucket usage in Google Cloud Console: - Storage: ~\\(0.02/GB/month - Download: ~\\)0.12/GB - Operations: Minimal</p>"},{"location":"development/dvc/#next-steps","title":"Next Steps","text":"<ul> <li>Explore Docker integration</li> <li>Learn about Testing with DVC</li> <li>Read Configuration</li> </ul>"},{"location":"development/testing/","title":"Testing Guide","text":"<p>This page describes the project's testing strategy, how to execute tests, and how to add new ones.</p>"},{"location":"development/testing/#test-structure","title":"Test Structure","text":"<pre><code>tests/\n\u251c\u2500\u2500 __init__.py\n\u251c\u2500\u2500 conftest.py              # Shared fixtures (pytest)\n\u251c\u2500\u2500 unit/\n\u2502   \u251c\u2500\u2500 __init__.py\n\u2502   \u251c\u2500\u2500 test_data_module.py  # DataModule tests\n\u2502   \u251c\u2500\u2500 test_model.py        # Model tests\n\u2502   \u2514\u2500\u2500 test_api.py          # API tests\n\u2514\u2500\u2500 integration/\n    \u251c\u2500\u2500 __init__.py\n    \u2514\u2500\u2500 test_pipeline.py     # End-to-end tests\n</code></pre>"},{"location":"development/testing/#testing-commands","title":"Testing Commands","text":""},{"location":"development/testing/#run-all-tests","title":"Run All Tests","text":"<pre><code># With uv\nuv run pytest tests/\n\n# Without uv (if pytest is installed globally)\npytest tests/\n</code></pre>"},{"location":"development/testing/#run-tests-by-category","title":"Run Tests by Category","text":"<pre><code># Unit tests only\nuv run pytest tests/unit/\n\n# Integration tests only\nuv run pytest tests/integration/\n\n# Specific file\nuv run pytest tests/unit/test_model.py\n\n# Specific test\nuv run pytest tests/unit/test_model.py::test_model_forward_pass\n</code></pre>"},{"location":"development/testing/#useful-options","title":"Useful Options","text":"<pre><code># Verbose output (shows all tests)\nuv run pytest tests/ -v\n\n# Show print statements\nuv run pytest tests/ -s\n\n# Stop on first failure\nuv run pytest tests/ -x\n\n# Stop after N failures\nuv run pytest tests/ --maxfail=3\n\n# Run tests in parallel (requires pytest-xdist)\nuv run pytest tests/ -n auto\n\n# Show coverage\nuv run pytest tests/ --cov=src/mbti_classifier --cov-report=html\n</code></pre>"},{"location":"development/testing/#markers-tags","title":"Markers (Tags)","text":"<pre><code># Run fast tests only\nuv run pytest tests/ -m \"not slow\"\n\n# Run slow tests only\nuv run pytest tests/ -m slow\n\n# Run GPU tests (if available)\nuv run pytest tests/ -m gpu\n</code></pre>"},{"location":"development/testing/#fixtures","title":"Fixtures","text":"<p>Fixtures in <code>conftest.py</code> are available for all tests:</p>"},{"location":"development/testing/#example-conftestpy","title":"Example: <code>conftest.py</code>","text":"<pre><code>import pytest\nimport torch\nfrom transformers import DistilBertTokenizer\nfrom src.mbti_classifier.data import MBTIDataModule\nfrom src.mbti_classifier.model import MBTIModel\n\n@pytest.fixture\ndef tokenizer():\n    \"\"\"DistilBERT Tokenizer.\"\"\"\n    return DistilBertTokenizer.from_pretrained('distilbert-base-uncased')\n\n@pytest.fixture\ndef sample_data():\n    \"\"\"Sample data for testing.\"\"\"\n    return {\n        'posts': ['This is a test post', 'Another test'],\n        'E_I': [1, 0],\n        'S_N': [0, 1],\n        'T_F': [1, 1],\n        'J_P': [0, 1]\n    }\n\n@pytest.fixture\ndef model():\n    \"\"\"Model for testing.\"\"\"\n    return MBTIModel(\n        model_name='distilbert-base-uncased',\n        learning_rate=2e-5,\n        dropout_rate=0.1\n    )\n\n@pytest.fixture\ndef datamodule():\n    \"\"\"DataModule for testing.\"\"\"\n    return MBTIDataModule(\n        raw_data_path='data/raw/mbti_1.csv',\n        processed_data_path='data/processed/processed_mbti.csv',\n        batch_size=2,\n        max_length=128,\n        num_workers=0\n    )\n</code></pre>"},{"location":"development/testing/#usage-in-tests","title":"Usage in Tests","text":"<pre><code>def test_model_output_shape(model, tokenizer):\n    \"\"\"Test that the model output has the correct shape.\"\"\"\n    # model and tokenizer come from fixtures\n\n    # Prepare input\n    text = \"This is a test\"\n    encoded = tokenizer(\n        text,\n        max_length=128,\n        padding='max_length',\n        truncation=True,\n        return_tensors='pt'\n    )\n\n    # Forward pass\n    output = model(encoded['input_ids'], encoded['attention_mask'])\n\n    # Verify shape\n    assert output.shape == (1, 4), f\"Expected shape (1, 4), got {output.shape}\"\n</code></pre>"},{"location":"development/testing/#unit-tests","title":"Unit Tests","text":""},{"location":"development/testing/#data-module-tests-test_data_modulepy","title":"Data Module Tests: <code>test_data_module.py</code>","text":"<pre><code>import pytest\nimport pandas as pd\nfrom src.mbti_classifier.data import MBTIDataset, MBTIDataModule\n\nclass TestMBTIDataset:\n    def test_dataset_length(self, sample_data, tokenizer):\n        \"\"\"Test that the dataset has the correct length.\"\"\"\n        df = pd.DataFrame(sample_data)\n        dataset = MBTIDataset(df, tokenizer, max_length=128)\n\n        assert len(dataset) == len(df)\n\n    def test_dataset_item_structure(self, sample_data, tokenizer):\n        \"\"\"Test that each item has the correct keys.\"\"\"\n        df = pd.DataFrame(sample_data)\n        dataset = MBTIDataset(df, tokenizer, max_length=128)\n\n        item = dataset[0]\n\n        assert 'input_ids' in item\n        assert 'attention_mask' in item\n        assert 'labels' in item\n\n    def test_dataset_item_shapes(self, sample_data, tokenizer):\n        \"\"\"Test that tensors have the correct shapes.\"\"\"\n        df = pd.DataFrame(sample_data)\n        dataset = MBTIDataset(df, tokenizer, max_length=128)\n\n        item = dataset[0]\n\n        assert item['input_ids'].shape == (128,)\n        assert item['attention_mask'].shape == (128,)\n        assert item['labels'].shape == (4,)\n\nclass TestMBTIDataModule:\n    def test_datamodule_setup(self, datamodule):\n        \"\"\"Test that the datamodule initializes correctly.\"\"\"\n        datamodule.prepare_data()\n        datamodule.setup('fit')\n\n        assert datamodule.train_dataset is not None\n        assert datamodule.val_dataset is not None\n\n    def test_dataloader_batch_size(self, datamodule):\n        \"\"\"Test that dataloaders have the correct batch size.\"\"\"\n        datamodule.prepare_data()\n        datamodule.setup('fit')\n\n        train_loader = datamodule.train_dataloader()\n        batch = next(iter(train_loader))\n\n        assert batch['input_ids'].shape[0] == datamodule.batch_size\n</code></pre>"},{"location":"development/testing/#model-tests-test_modelpy","title":"Model Tests: <code>test_model.py</code>","text":"<pre><code>import pytest\nimport torch\nfrom src.mbti_classifier.model import MBTIModel\n\nclass TestMBTIModel:\n    def test_model_initialization(self):\n        \"\"\"Test that the model initializes correctly.\"\"\"\n        model = MBTIModel(\n            model_name='distilbert-base-uncased',\n            learning_rate=2e-5,\n            dropout_rate=0.1\n        )\n\n        assert model is not None\n        assert len(model.classifiers) == 4\n\n    def test_forward_pass(self, model):\n        \"\"\"Test that the forward pass works.\"\"\"\n        # Dummy input\n        input_ids = torch.randint(0, 1000, (2, 128))\n        attention_mask = torch.ones(2, 128)\n\n        # Forward\n        output = model(input_ids, attention_mask)\n\n        # Verify shape\n        assert output.shape == (2, 4)\n\n        # Verify range (after sigmoid, must be [0, 1])\n        assert torch.all(output &gt;= 0)\n        assert torch.all(output &lt;= 1)\n\n    def test_training_step(self, model):\n        \"\"\"Test that the training step works.\"\"\"\n        # Dummy batch\n        batch = {\n            'input_ids': torch.randint(0, 1000, (2, 128)),\n            'attention_mask': torch.ones(2, 128),\n            'labels': torch.tensor([[1, 0, 1, 0], [0, 1, 0, 1]], dtype=torch.float)\n        }\n\n        # Training step\n        loss = model.training_step(batch, 0)\n\n        # Verify that loss is a scalar tensor\n        assert isinstance(loss, torch.Tensor)\n        assert loss.ndim == 0\n        assert loss.item() &gt; 0\n\n    @pytest.mark.slow\n    def test_full_training_epoch(self, model, datamodule):\n        \"\"\"Test a full training epoch.\"\"\"\n        import lightning.pytorch as pl\n\n        datamodule.prepare_data()\n        datamodule.setup('fit')\n\n        trainer = pl.Trainer(\n            max_epochs=1,\n            limit_train_batches=10,\n            limit_val_batches=5,\n            logger=False,\n            enable_checkpointing=False\n        )\n\n        trainer.fit(model, datamodule)\n\n        # Verify that it trained without errors\n        assert trainer.current_epoch == 1\n</code></pre>"},{"location":"development/testing/#api-tests-test_apipy","title":"API Tests: <code>test_api.py</code>","text":"<pre><code>import pytest\nfrom fastapi.testclient import TestClient\nfrom src.mbti_classifier.api import app\n\n@pytest.fixture\ndef client():\n    \"\"\"Testing client for the API.\"\"\"\n    return TestClient(app)\n\nclass TestAPI:\n    def test_health_endpoint(self, client):\n        \"\"\"Test the health endpoint.\"\"\"\n        response = client.get(\"/health\")\n\n        assert response.status_code == 200\n        assert response.json() == {\"status\": \"healthy\"}\n\n    def test_predict_endpoint(self, client):\n        \"\"\"Test the prediction endpoint.\"\"\"\n        payload = {\n            \"text\": \"I love solving complex problems and analyzing data.\"\n        }\n\n        response = client.post(\"/predict\", json=payload)\n\n        assert response.status_code == 200\n        data = response.json()\n\n        assert \"mbti_type\" in data\n        assert len(data[\"mbti_type\"]) == 4\n        assert all(c in \"EISNTFJP\" for c in data[\"mbti_type\"])\n\n    def test_predict_batch_endpoint(self, client):\n        \"\"\"Test the batch prediction endpoint.\"\"\"\n        payload = {\n            \"texts\": [\n                \"I love meeting new people!\",\n                \"I prefer staying home and reading.\"\n            ]\n        }\n\n        response = client.post(\"/predict_batch\", json=payload)\n\n        assert response.status_code == 200\n        data = response.json()\n\n        assert \"predictions\" in data\n        assert len(data[\"predictions\"]) == 2\n\n    def test_predict_invalid_input(self, client):\n        \"\"\"Test that the API rejects invalid inputs.\"\"\"\n        payload = {\"text\": \"\"}  # Empty text\n\n        response = client.post(\"/predict\", json=payload)\n\n        assert response.status_code == 422  # Validation error\n</code></pre>"},{"location":"development/testing/#integration-tests","title":"Integration Tests","text":""},{"location":"development/testing/#full-pipeline-test-test_pipelinepy","title":"Full Pipeline Test: <code>test_pipeline.py</code>","text":"<pre><code>import pytest\nimport os\nfrom pathlib import Path\nfrom src.mbti_classifier.data import download_mbti_data, preprocess_mbti_data\nfrom src.mbti_classifier.model import MBTIClassifier\n\n@pytest.mark.slow\nclass TestFullPipeline:\n    def test_data_download_and_preprocessing(self, tmp_path):\n        \"\"\"Test data download and preprocessing.\"\"\"\n        raw_path = tmp_path / \"raw\" / \"mbti_1.csv\"\n        processed_path = tmp_path / \"processed\" / \"processed_mbti.csv\"\n\n        # Download\n        download_mbti_data(str(raw_path))\n        assert raw_path.exists()\n\n        # Preprocess\n        preprocess_mbti_data(str(raw_path), str(processed_path))\n        assert processed_path.exists()\n\n    @pytest.mark.gpu\n    def test_training_pipeline(self, tmp_path):\n        \"\"\"Test full training pipeline.\"\"\"\n        import lightning.pytorch as pl\n        from src.mbti_classifier.data import MBTIDataModule\n        from src.mbti_classifier.model import MBTIModel\n\n        # Configure paths\n        model_dir = tmp_path / \"models\"\n        model_dir.mkdir()\n\n        # Initialize components\n        datamodule = MBTIDataModule(\n            raw_data_path='data/raw/mbti_1.csv',\n            processed_data_path='data/processed/processed_mbti.csv',\n            batch_size=8,\n            max_length=128,\n            num_workers=0\n        )\n\n        model = MBTIModel(\n            model_name='distilbert-base-uncased',\n            learning_rate=2e-5\n        )\n\n        # Trainer\n        trainer = pl.Trainer(\n            max_epochs=1,\n            limit_train_batches=10,\n            limit_val_batches=5,\n            default_root_dir=str(tmp_path),\n            logger=False,\n            enable_checkpointing=True\n        )\n\n        # Train\n        trainer.fit(model, datamodule)\n\n        # Verify that checkpoint was saved\n        checkpoints = list(Path(tmp_path).glob(\"**/*.ckpt\"))\n        assert len(checkpoints) &gt; 0\n\n    def test_inference_pipeline(self):\n        \"\"\"Test inference pipeline.\"\"\"\n        # Load pre-trained model\n        classifier = MBTIClassifier.load_from_checkpoint('models/best.ckpt')\n\n        # Predict\n        text = \"I love analyzing complex problems and finding elegant solutions.\"\n        mbti_type = classifier.predict(text)\n\n        # Verify\n        assert isinstance(mbti_type, str)\n        assert len(mbti_type) == 4\n        assert all(c in \"EISNTFJP\" for c in mbti_type)\n</code></pre>"},{"location":"development/testing/#custom-markers","title":"Custom Markers","text":""},{"location":"development/testing/#define-markers-pytestini","title":"Define Markers: <code>pytest.ini</code>","text":"<pre><code>[pytest]\nmarkers =\n    slow: marks tests as slow (deselect with '-m \"not slow\"')\n    gpu: marks tests that require GPU\n    integration: marks integration tests\n    unit: marks unit tests\n</code></pre>"},{"location":"development/testing/#usage-in-tests_1","title":"Usage in Tests","text":"<pre><code>import pytest\n\n@pytest.mark.slow\ndef test_long_running_operation():\n    \"\"\"This test takes a long time.\"\"\"\n    # ...\n\n@pytest.mark.gpu\ndef test_gpu_training():\n    \"\"\"This test requires GPU.\"\"\"\n    if not torch.cuda.is_available():\n        pytest.skip(\"GPU not available\")\n    # ...\n\n@pytest.mark.integration\ndef test_api_integration():\n    \"\"\"Integration test.\"\"\"\n    # ...\n</code></pre>"},{"location":"development/testing/#run-with-markers","title":"Run with Markers","text":"<pre><code># Fast tests only (exclude slow)\nuv run pytest -m \"not slow\"\n\n# GPU tests only\nuv run pytest -m gpu\n\n# Integration tests only\nuv run pytest -m integration\n\n# Unit + fast\nuv run pytest -m \"unit and not slow\"\n</code></pre>"},{"location":"development/testing/#coverage","title":"Coverage","text":""},{"location":"development/testing/#generate-coverage-report","title":"Generate Coverage Report","text":"<pre><code># Coverage with HTML report\nuv run pytest tests/ --cov=src/mbti_classifier --cov-report=html\n\n# Open report\nopen htmlcov/index.html  # macOS\n# Or navigate to htmlcov/index.html in your browser\n</code></pre>"},{"location":"development/testing/#terminal-coverage","title":"Terminal Coverage","text":"<pre><code>uv run pytest tests/ --cov=src/mbti_classifier --cov-report=term-missing\n</code></pre> <p>Output:</p> <pre><code>Name                          Stmts   Miss  Cover   Missing\n-----------------------------------------------------------\nsrc/mbti_classifier/data.py    150     10    93%   45-47, 89\nsrc/mbti_classifier/model.py   120      5    96%   67-68\nsrc/mbti_classifier/api.py      80      8    90%   112-115\n-----------------------------------------------------------\nTOTAL                          350     23    93%\n</code></pre>"},{"location":"development/testing/#minimum-coverage","title":"Minimum Coverage","text":"<pre><code># Fail if coverage &lt; 80%\nuv run pytest tests/ --cov=src/mbti_classifier --cov-fail-under=80\n</code></pre>"},{"location":"development/testing/#best-practices","title":"Best Practices","text":""},{"location":"development/testing/#1-use-fixtures-for-common-setup","title":"1. Use Fixtures for Common Setup","text":"<pre><code># \u2705 Good\n@pytest.fixture\ndef model():\n    return MBTIModel(model_name='distilbert-base-uncased')\n\ndef test_forward(model):\n    # model is already initialized\n    output = model(...)\n\n# \u274c Bad\ndef test_forward():\n    model = MBTIModel(model_name='distilbert-base-uncased')  # Repetitive\n    output = model(...)\n</code></pre>"},{"location":"development/testing/#2-independent-tests","title":"2. Independent Tests","text":"<pre><code># \u2705 Good: each test is independent\ndef test_add_user():\n    db = Database()\n    db.add_user('Alice')\n    assert db.count() == 1\n\ndef test_remove_user():\n    db = Database()\n    db.add_user('Bob')\n    db.remove_user('Bob')\n    assert db.count() == 0\n\n# \u274c Bad: tests depend on order\ndb = Database()\n\ndef test_add_user():\n    db.add_user('Alice')\n    assert db.count() == 1\n\ndef test_remove_user():\n    db.remove_user('Alice')  # Assumes test_add_user already ran\n    assert db.count() == 0\n</code></pre>"},{"location":"development/testing/#3-use-parametrize-for-multiple-cases","title":"3. Use Parametrize for Multiple Cases","text":"<pre><code># \u2705 Good: parametrize\n@pytest.mark.parametrize(\"text,expected\", [\n    (\"I love parties!\", \"E\"),\n    (\"I prefer staying home\", \"I\"),\n    (\"Big gatherings are fun\", \"E\")\n])\ndef test_extraversion(text, expected):\n    result = classify_ei(text)\n    assert result[0] == expected\n\n# \u274c Bad: repetitive tests\ndef test_extraversion_case1():\n    result = classify_ei(\"I love parties!\")\n    assert result[0] == \"E\"\n\ndef test_extraversion_case2():\n    result = classify_ei(\"I prefer staying home\")\n    assert result[0] == \"I\"\n</code></pre>"},{"location":"development/testing/#4-descriptive-names","title":"4. Descriptive Names","text":"<pre><code># \u2705 Good\ndef test_model_returns_four_probabilities():\n    ...\n\ndef test_api_rejects_empty_text():\n    ...\n\n# \u274c Bad\ndef test_model():\n    ...\n\ndef test_api():\n    ...\n</code></pre>"},{"location":"development/testing/#cicd-integration","title":"CI/CD Integration","text":""},{"location":"development/testing/#github-actions","title":"GitHub Actions","text":"<pre><code># .github/workflows/test.yml\nname: Tests\n\non: [push, pull_request]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n\n    steps:\n    - uses: actions/checkout@v3\n\n    - name: Install uv\n      run: curl -LsSf [https://astral.sh/uv/install.sh](https://astral.sh/uv/install.sh) | sh\n\n    - name: Install dependencies\n      run: uv sync\n\n    - name: Run tests\n      run: uv run pytest tests/ --cov=src/mbti_classifier --cov-report=xml\n\n    - name: Upload coverage\n      uses: codecov/codecov-action@v3\n      with:\n        file: ./coverage.xml\n</code></pre>"},{"location":"development/testing/#troubleshooting","title":"Troubleshooting","text":""},{"location":"development/testing/#problem-tests-are-not-discovered","title":"Problem: Tests are not discovered","text":"<p>Solution: - Verify that files start with <code>test_</code> - Verify that classes start with <code>Test</code> - Verify that <code>__init__.py</code> exists in the tests directory</p>"},{"location":"development/testing/#problem-fixture-not-found","title":"Problem: Fixture not found","text":"<p>Solution: - Verify that fixture is in <code>conftest.py</code> - Verify fixture scope</p>"},{"location":"development/testing/#problem-slow-tests","title":"Problem: Slow tests","text":"<p>Solution: <pre><code># Run in parallel\nuv run pytest tests/ -n auto\n\n# Exclude slow tests\nuv run pytest tests/ -m \"not slow\"\n</code></pre></p>"},{"location":"development/testing/#references","title":"References","text":"<ul> <li>Pytest Documentation</li> <li>Pytest Fixtures</li> <li>Pytest Markers</li> <li>Coverage.py</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#prerequisites","title":"Prerequisites","text":"<p>Before you begin, ensure you have the following installed:</p> <ul> <li>Python 3.12+</li> <li>uv - Ultra-fast Python package manager</li> <li>Git</li> <li>Google Cloud SDK (for DVC remote storage)</li> </ul>"},{"location":"getting-started/installation/#installing-uv","title":"Installing uv","text":"<p>=== \"macOS\"     <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre></p> <p>=== \"Linux\"     <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre></p> <p>=== \"Windows\"     <pre><code>powershell -c \"irm https://astral.sh/uv/install.ps1 | iex\"\n</code></pre></p>"},{"location":"getting-started/installation/#installing-google-cloud-sdk","title":"Installing Google Cloud SDK","text":"<p>Only required if you need to pull data/models from DVC:</p> <p>=== \"macOS\"     <pre><code>brew install google-cloud-sdk\n</code></pre></p> <p>=== \"Linux\"     <pre><code>curl https://sdk.cloud.google.com | bash\n</code></pre></p> <p>=== \"Windows\"     Download the installer from Google Cloud SDK</p>"},{"location":"getting-started/installation/#clone-the-repository","title":"Clone the Repository","text":"<pre><code>git clone https://github.com/mlopsgroup70/mlops_group70.git\ncd mlops_group70\n</code></pre>"},{"location":"getting-started/installation/#install-dependencies","title":"Install Dependencies","text":"<pre><code># Create virtual environment and install all dependencies\nuv sync --dev\n\n# Install pre-commit hooks for code quality\nuv run pre-commit install --install-hooks\n</code></pre> <p>Note</p> <p>You don't need to manually activate the virtual environment. <code>uv run</code> handles this automatically.</p>"},{"location":"getting-started/installation/#pull-data-and-models-optional","title":"Pull Data and Models (Optional)","text":"<p>If you want to use pre-trained models and preprocessed data:</p> <pre><code># Authenticate with Google Cloud (first time only)\ngcloud auth application-default login\n\n# Pull data and models from Google Cloud Storage\nuv run dvc pull\n</code></pre> <p>This downloads: - Raw dataset from <code>mlops70_bucket</code> - Preprocessed data - Trained model checkpoints</p>"},{"location":"getting-started/installation/#verify-installation","title":"Verify Installation","text":"<pre><code># Run a quick test\nuv run pytest tests/unit/ -v\n\n# Check that all commands are available\nuv run invoke --list\n</code></pre> <p>You should see output listing all available tasks.</p>"},{"location":"getting-started/installation/#whats-next","title":"What's Next?","text":"<ul> <li>Continue to the Quick Start Guide to train your first model</li> <li>Learn about Configuration to customize training</li> <li>Explore the Architecture to understand the system</li> </ul>"},{"location":"getting-started/installation/#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/installation/#uv-sync-fails","title":"uv sync fails","text":"<p>If you encounter errors during <code>uv sync</code>:</p> <pre><code># Clear cache and retry\nuv cache clean\nuv sync --dev\n</code></pre>"},{"location":"getting-started/installation/#dvc-authentication-issues","title":"DVC authentication issues","text":"<p>If <code>dvc pull</code> fails:</p> <pre><code># Re-authenticate\ngcloud auth application-default login\n\n# Verify access to bucket\ngsutil ls gs://mlops70_bucket/\n</code></pre>"},{"location":"getting-started/installation/#import-errors","title":"Import errors","text":"<p>Ensure you're using <code>uv run</code> to execute Python scripts:</p> <pre><code># \u274c Wrong\npython src/mbti_classifier/train.py\n\n# \u2705 Correct\nuv run python src/mbti_classifier/train.py\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>This guide will get you up and running with the MBTI classifier in minutes.</p>"},{"location":"getting-started/quickstart/#option-1-using-pre-trained-model","title":"Option 1: Using Pre-trained Model","text":"<p>The fastest way to start making predictions.</p>"},{"location":"getting-started/quickstart/#1-pull-data-and-model","title":"1. Pull Data and Model","text":"<pre><code># Authenticate with Google Cloud\ngcloud auth application-default login\n\n# Download pre-trained model and data\nuv run dvc pull\n</code></pre>"},{"location":"getting-started/quickstart/#2-start-the-api","title":"2. Start the API","text":"<pre><code># Start FastAPI server\nuv run invoke api\n</code></pre> <p>The API will be available at http://localhost:8000</p>"},{"location":"getting-started/quickstart/#3-start-the-ui","title":"3. Start the UI","text":"<p>In a new terminal:</p> <pre><code># Start Streamlit interface\nuv run invoke ui\n</code></pre> <p>Access the UI at http://localhost:8501</p>"},{"location":"getting-started/quickstart/#4-make-a-prediction","title":"4. Make a Prediction","text":"<p>Open http://localhost:8501 in your browser and try:</p> <pre><code>I enjoy spending quiet evenings reading philosophy books and pondering \nthe meaning of existence. Abstract concepts fascinate me more than \nconcrete details.\n</code></pre> <p>Expected result: INFP or similar Introverted, Intuitive type.</p>"},{"location":"getting-started/quickstart/#option-2-train-from-scratch","title":"Option 2: Train from Scratch","text":"<p>For full control and experimentation.</p>"},{"location":"getting-started/quickstart/#1-prepare-data","title":"1. Prepare Data","text":"<pre><code># Download and preprocess MBTI dataset\nuv run invoke preprocess-data\n</code></pre> <p>This will: - Download raw data from Kaggle - Clean text and create binary labels - Save to <code>data/processed/processed_mbti.csv</code></p>"},{"location":"getting-started/quickstart/#2-train-the-model","title":"2. Train the Model","text":"<pre><code># Quick training (2 epochs for testing)\nuv run python src/mbti_classifier/train.py --config-name train_quick\n\n# Full training (default config)\nuv run invoke train\n\n# Production training (20 epochs)\nuv run python src/mbti_classifier/train.py --config-name train_production\n</code></pre> <p>Training progress is logged to: - Console - WandB (if configured) - <code>logs/</code> directory</p>"},{"location":"getting-started/quickstart/#3-verify-model","title":"3. Verify Model","text":"<pre><code># Model saved to models/best.ckpt\nls -lh models/\n\n# Run tests to verify\nuv run pytest tests/integration/test_pipeline.py\n</code></pre>"},{"location":"getting-started/quickstart/#4-version-with-dvc","title":"4. Version with DVC","text":"<pre><code># Track new model\nuv run dvc add models\n\n# Commit metadata\ngit add models.dvc\ngit commit -m \"Train model v1\"\n\n# Push to cloud storage\nuv run dvc push\n</code></pre>"},{"location":"getting-started/quickstart/#option-3-using-docker","title":"Option 3: Using Docker","text":"<p>Production-ready deployment with Docker Compose.</p>"},{"location":"getting-started/quickstart/#1-build-images","title":"1. Build Images","text":"<pre><code># Build all Docker images\nuv run invoke docker-build\n</code></pre>"},{"location":"getting-started/quickstart/#2-train-optional","title":"2. Train (Optional)","text":"<pre><code># Train model in container\nuv run invoke docker-train\n</code></pre>"},{"location":"getting-started/quickstart/#3-start-services","title":"3. Start Services","text":"<pre><code># Start API + UI\nuv run invoke docker-up\n\n# View logs\nuv run invoke docker-logs\n</code></pre> <p>Access: - API: http://localhost:8000/docs - UI: http://localhost:8501</p>"},{"location":"getting-started/quickstart/#4-stop-services","title":"4. Stop Services","text":"<pre><code>uv run invoke docker-down\n</code></pre>"},{"location":"getting-started/quickstart/#testing-the-api","title":"Testing the API","text":""},{"location":"getting-started/quickstart/#curl-example","title":"cURL Example","text":"<pre><code>curl -X POST \"http://localhost:8000/predict\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"text\": \"I love meeting new people and being the center of attention at parties!\"\n  }'\n</code></pre>"},{"location":"getting-started/quickstart/#python-example","title":"Python Example","text":"<pre><code>import requests\n\nresponse = requests.post(\n    \"http://localhost:8000/predict\",\n    json={\"text\": \"I prefer facts and details over theories and possibilities.\"}\n)\n\nprint(response.json())\n# {\n#   \"mbti_type\": \"ISTJ\",\n#   \"dimensions\": [...],\n#   \"probabilities\": {...}\n# }\n</code></pre>"},{"location":"getting-started/quickstart/#configuration-overrides","title":"Configuration Overrides","text":"<p>Customize training on the fly:</p> <pre><code># Override learning rate and batch size\nuv run python src/mbti_classifier/train.py \\\n  model.learning_rate=5e-5 \\\n  data.batch_size=32\n\n# Use CPU instead of GPU\nuv run python src/mbti_classifier/train.py \\\n  trainer.accelerator=cpu\n\n# Change maximum epochs\nuv run python src/mbti_classifier/train.py \\\n  trainer.max_epochs=15\n</code></pre>"},{"location":"getting-started/quickstart/#common-workflows","title":"Common Workflows","text":""},{"location":"getting-started/quickstart/#development-iteration","title":"Development Iteration","text":"<pre><code># 1. Make code changes\n# 2. Run fast tests\nuv run pytest -m \"not slow\"\n\n# 3. Format code\nuv run ruff format .\n\n# 4. Quick training test\nuv run python src/mbti_classifier/train.py --config-name train_quick\n\n# 5. Full testing\nuv run invoke test\n</code></pre>"},{"location":"getting-started/quickstart/#data-updates","title":"Data Updates","text":"<pre><code># 1. Update data\n# (modify data/raw/ or re-run preprocessing)\n\n# 2. Track changes\nuv run dvc add data/processed\n\n# 3. Commit and push\ngit add data/processed.dvc\ngit commit -m \"Update dataset\"\nuv run dvc push\n</code></pre>"},{"location":"getting-started/quickstart/#next-steps","title":"Next Steps","text":"<ul> <li>Learn about Architecture</li> <li>Explore Configuration</li> <li>Read API Reference</li> <li>Set up Docker for production</li> </ul>"},{"location":"getting-started/quickstart/#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/quickstart/#model-not-found-error","title":"Model not found error","text":"<pre><code># Ensure model exists\nls models/best.ckpt\n\n# If using DVC, pull first\nuv run dvc pull\n</code></pre>"},{"location":"getting-started/quickstart/#port-already-in-use","title":"Port already in use","text":"<pre><code># Kill process on port 8000\nlsof -ti:8000 | xargs kill -9\n\n# Or use different port\nuv run invoke api --port 8080\n</code></pre>"},{"location":"getting-started/quickstart/#out-of-memory-during-training","title":"Out of memory during training","text":"<pre><code># Reduce batch size\nuv run python src/mbti_classifier/train.py data.batch_size=8\n\n# Use gradient accumulation\nuv run python src/mbti_classifier/train.py \\\n  trainer.accumulate_grad_batches=4\n</code></pre>"}]}